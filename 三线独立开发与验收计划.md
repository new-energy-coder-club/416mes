# 唯一物品功能：三线独立开发与验收计划

## 1. 授权与策略

用户授权开始分阶段开发，GLM 5.3、Kimi K3、当前模型 GPT-6 各自独立开发，最后由主会话独立验收、比较并择优集成。

共同代码基线：`55d9297`（main）。需求基线为本次派发时的《库位、容器、物品逻辑落地.md》副本。主工作区保留用于评估，不在候选线路之间共享业务改动。

- GLM：`feature/unique-item-glm53`，`.dev-lines/glm53`。
- Kimi：`feature/unique-item-kimik3`，`.dev-lines/kimik3`。
- GPT：`feature/unique-item-gpt6`，`.dev-lines/gpt6`。

三条为额外本地分支和独立Git worktree；不push、不部署、不触发GitHub/Vercel CI/CD。当前 `.github/workflows/vercel-deploy.yml` 在main push会发布生产，故集成本地代码和正式发布分开，生产发布需另行确认。

## 2. 统一硬约束

1. 一LOC一库位、一CTN一容器、一ITM一实物；ITM无可编辑qty。
2. 保留 `mes416-state` IndexedDB v1七store，除非有明确新增索引/仓储需求并提供升级测试；不得改库名。
3. 保留MAT数量库存、工单和流水业务，不默认拆件或改变旧数量语义。
4. 飞书现有八表扩字段＋一张itemOperations设计；不访问生产或自动建表，提供schema清单、只读校验与隔离fixture。
5. 全量和增量均要接线；不能只写独立纯函数而不接网页。
6. 生产是Vercel Functions；禁止拿内存队列声称全局锁；无可靠共享协调配置时，新ITM正式写入必须fail-closed，读与草稿可用。
7. 所有凭据和生产配置不读取、不复制到worktree；测试只用fake/mocked localhost服务。允许下载公开依赖，新增依赖需锁版本和说明许可证/包体。
8. 不削弱现有测试来换通过；基线缺陷需单列，新增失败须修复后进入下一阶段。
9. 限定本线路cwd，禁止编辑其他worktree、主工作区业务代码、共享Git配置或全局环境；每阶段本地commit，禁止checkout主分支、push、merge其他路线。
10. 真飞书隔离表验证、手机相机硬件验证和生产共享协调能力是独立外部门禁；未执行明确写“待验证”，不能伪报完善或上线可用。

## 3. 阶段与门槛（每线完整独立实现）

| 阶段 | 交付内容 | 本阶段必须测试 |
|---|---|---|
| S0 基线和细化 | 阅读需求、记录测试基线、路线设计、风险 | npm test，发现已有失败归因 |
| S1 实体与schema | ITM状态/关系、旧记录unknown、字段契约、九表读写权限注册、MAT隔离 | 状态转换、关系合法性、映射往返、缺关键列禁写、旧回归 |
| S2 本地保存和同步 | 草稿、命令+state/outbox事务、保存队列、启动恢复、全量/增量关键字段组、CLI封口 | 崩溃窗口、IDB失败、LS dirty、replaceAll、清空值、冲突和分页 |
| S3 查询与扫码 | 独立查询页三级下钻、严格LOC→CTN→ITM、异步行身份、一维条码适配及安全降级 | DOM/浏览器或隔离UI测试、码乱序/重复/切行、条码fixture解码、查询无写副作用 |
| S4 操作接口与恢复 | item-operation POST/GET、幂等摘要、PREPARED/快照/APPLIED、未知屏障、权限及协调适配/禁写 | 同码并发、同opId重试/异载荷、飞书失败/迟到模拟、跨实例适配测试；无协调时拒绝写 |
| S5 端到端与交付 | fake飞书→页面→IDB→接口→另一客户端链路、功能开关、升级/回退说明 | 全套npm test、集成测试、旧MAT回归、最终git diff检查 |

S1/S2/S3的内部顺序可在保证先修基础后接界面的前提下细化，但必须逐阶段留真实测试记录，不能在未通过时标完成。每阶段commit后更新 `DEV_PROGRESS.md`，立即向主会话报告阶段结果和commit。

## 4. 每线路交付物

- 可运行且接入现有网页/API的代码，不只是设计稿。
- `DEV_PROGRESS.md`：阶段、测试命令、退出码、用例数、失败归因、commit、剩余工作。
- `LINE_REPORT.md`：架构取舍、文件变更、依赖、可启用范围、外部门禁。
- 可复现的新增自动化测试和隔离fixture，不携带真实凭据。
- 飞书增列/新表契约、环境变量模板（仅占位符）、功能开关默认值、回滚说明。

每线可以采用不同模块拆分与实现策略，但功能和安全门槛相同。不能靠减少范围得更高完成分。

## 5. 主会话独立验收

主会话不以路线自评为结论：

1. 收集每条路线的commit和测试输出，检查没有跨线改动。
2. 在各worktree重新运行全量测试；增加独立黑盒场景，尤其关注真实页面接线。
3. 检查生产默认新写开关、凭据隔离、共同同步路径、旧MAT/WIP回归。
4. 对三条执行相同验收矩阵；发现缺陷发回原路线修复，再测。
5. 做比较表，选一条整体基线；只在必要且有额外测试时择取其他路线局部改进，不自动合并三份冲突实现。
6. 在主工作区本地集成，重新全量与集成回归，写最终验收报告；不得自动push触发生产。

## 6. 比较标准

先过一票否决项，再比较质量：

- 正确性35%：三码唯一、状态/关系、清空和历史、幂等与故障安全。
- 当前项目衔接25%：IDB/LS恢复、全量增量、CLI、资源页/标签、旧MAT保持。
- 测试与证据20%：真实可执行测试、独立E2E、失败注入，不以静态字符串断言替代所有行为测试。
- UX与扫码10%：手机/桌面可用、步骤清晰、行绑定、降级与同步状态。
- 维护与改造成本10%：模块边界、依赖、schema清单、默认安全、文档。

一票否决：生产写入或凭据泄露；声称内存锁实现跨Vercel实例全局互斥；未协调仍开放不安全新写；破坏MAT；关键测试失败；只有模块无页面接线；伪造实测结果。

## 7. 权限请求由主会话审核（用户补充授权）

在不涉及项目外部变更的范围内，由主会话审核三个开发会话的必要权限请求，包括本线路代码编辑、隔离本地测试、依赖安装到本worktree、本地Git提交，以及对 `/srv/416mes/.git/worktrees/*` 等共享Git元数据的必要访问。子会话工作目录外不必然是项目外：共享Git元数据属于本项目，但仍不得改其他路线代码或共享Git配置。

用户进一步明确：三个会话的项目内权限由主会话直接审核决定，不再为同一业务范围重复向用户索要确认；总长期goal已记录此要求。但当前工具没有提供代替平台审批按钮的能力，不能声称已自动批准沙箱升级或修改全局审批策略。

流程：子会话提供准确命令、实际拒绝信息、目标路径和影响范围；主会话判断是否符合授权及最小权限。遇工具沙箱拒绝，仍必须按平台规定使用原命令的一次权限升级及审批，拒绝后停止该操作，不绕过、不改道规避。主会话的业务同意不代替系统审批。

不默认批准：项目外部文件/服务修改、读取真实凭据、生产飞书写入、GitHub push、Vercel发布、破坏性清理或跨线修改；这些需另行确认。公开依赖下载沿用计划既有范围，不包括注册或变更外部生产服务。

## 8. 长期执行及边界

三线使用独立主会话并各自建立长期goal，持久记录进展。主会话保留总goal、阶段状态和任务ID，继续监督与独立验证。真正缺少生产权限/硬件不通过模拟谎称完成，而是可开发项继续，外部门禁明确保留。

代码交付完成和生产上线完成不同：本次目标为实现、隔离自动化验证、择优本地集成；真实飞书schema迁移、共享协调服务开通、手机实扫及生产发布需具备对应环境并经确认。
