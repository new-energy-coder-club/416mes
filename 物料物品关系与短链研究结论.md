# 物料台账与物品表关系 + 短链自动生成：研究结论

> 两个方向各派了研究子代理实测核查（报告：`MAT-ITM合并研究报告.md`、短链研究摘要见 §3），本文件是主会话综合结论与待你拍板的决策点。未改业务代码。

## 1. 物料台账要不要换成关联物品表？——**不换，打通关联**

### 为什么不能换

两张表字段看着重叠，但语义完全不同：

| | 物料台账（MAT） | 物品（ITM） |
|---|---|---|
| 回答 | 这类东西还剩多少 | 具体是哪一件、在哪 |
| 数量 | qty + 库存流水账本 | 数量恒为 1，领域层硬拒绝 qty |
| 典型物 | LED灯珠 20 个、耗材 | 电烙铁、万用表、钥匙 |
| 下游 | WIP 工单领料、盘点、安全库存、Excel、闲鱼同步 | 三码作业、物品操作日志 |

物料台账的下游依赖非常深：WIP 工单从建单校验到执行扣减到冲销全链吃「物料码+数量」，Excel 9 表往返、闲鱼同步、盘点审计都在上面。「直接换掉」等于推翻这些已验收的链路——不建议。

### 数量型物品怎么办（如 PJ-LD-001 灯珠 20 个）

**不逐件贴码**。建议双轨边界（可以调）：

- 纯数量管理（HC 耗材、DZ 电子件、QT 其他）：留在物料台账；
- 重要单件（GZ 贵重件、TS 调试设备、GJ 工具、DJ 电机、JG 机构件）：MAT 记总账 + ITM 贴码管具体单件，单件数只做展示、不强行等于库存数。

### 打通关联（你真正要的「关联」）

飞书物品表**已经有**「关联物料码」列，代码也放行了，但界面从来没用过。最小实现：

1. 物品详情显示关联物料（名称/规格/库存）；
2. 物品建档时可填物料码；
3. 物料台账行加「已建档单件 n」列，点击跳到物品页看具体单件。

这一步零 schema 变更、不动任何数量逻辑，就是「关联物品表」的最小落地。

## 2. 短链 ↔ 物品码自动生成（方案核查 + 实测）

《物品二维码短链方案.md》的算法经实测成立，但发现一个方案没察觉的关键缺口：

> **现在新物品建档码是 `WP-` + 随机串（如 WP-a3f2…），不是数字序号。短链离线解码算法只认 WP-数字 → 不改取号，以后所有新物品的短链都只能联网查表，方案的核心卖点（离线可解）落空。**

### 必须做的取号改造

- 新物品自动号改顺序号：读物品表最大 `WP-\d+` 号 → 下一个（衔接现有 WP-001~003，下一个是 WP-004）；
- 取号必须严格过滤 `/^WP-(\d+)$/`（随机码会被 parseInt 误读污染最大号）；
- 优先服务端发号：注册请求不带码，服务端读到快照后算 max+1 一次写入，消灭并发撞号窗口；
- 保留「编码可选输入框」沿用扫到的实物码。

### 短码算法修正（实测翻车点）

- 40bit 拼值不能用位运算（JS int32 截断会解码全错），必须用乘除/取模；
- 原乘数 8121 太小，相邻序号短码前两位肉眼连号；换 20740581 散布良好；
- 算法常数印上标签就永不可改，测试锁死期望值；
- 短链全长 42 字符恰好卡满二维码 V3-M——URL 一个字符都不能再加。

### 飞书「物品短链」表可以先不建

结构化短码本地就能解码，新表只剩审计/随机兜底/统计等残余角色。MVP 完全不需要：生成（本地算）→ 打印 → 扫码（本地解码或 /i/ 转跳）全通。

### 最小可行链路（MVP）

```
取号顺序化 → lib/item-link.js 短码算法+测试锁死 → /i/{8位} 转跳端点
→ 标签页二维码内容换短链 + 生成/复制面板 → 4 个扫码入口接短链解析
→ #item/WP-00N 深链落地查询页
```

微信/相机扫出来直接落在物品详情卡，**不依赖飞书**；已打印的 ITM: 二维码和 Code128 条形码继续可读。

## 3. 需要你拍板

| # | 决策点 | 我的建议 |
|---|---|---|
| 1 | 物料台账与物品表 | 不合并，打通 materialCode 关联（阶段0先做） |
| 2 | 数量型物料逐件贴码？ | 不贴，保留数量管理 |
| 3 | 物品取号改顺序号？ | 改（短链前提） |
| 4 | 飞书是否新建「物品短链」表 | v1 不建 |
| 5 | 实施顺序 | 先做关联打通 + 取号顺序化，再做短链 MVP |

回复确认或调整这几个点，我就按「关联打通 → 取号顺序化 → 短链 MVP」的顺序实施。
