# 扫码与移动端修复交付报告（v2.36.0）

## 修复的四个问题

### 1. 手机底部导航缺「物品作业」（原：ITM作业）
- 根因：`index.html` 该按钮缺 `data-primary="1"`，折叠态只显示主入口。
- 修复：加 `data-primary` + 短名「物品作业」，生产 390px 实测底部导航直接可见，无需点「全部」。

### 2/3. 扫码交互重写
- 新增 `lib/scan-camera.js` 统一全屏扫码浮层（复用扫码工作台 camOverlay 视觉与焦点/ESC/错误常驻模式），查询页和作业页不再内嵌摄像头画面。
- 识别到码 → **确认卡**：显示码制（二维码/Code128/Code39/EAN-13…）、码值、命中对象摘要，点「确定填入」才生效；「重新扫描」可放弃。
- 画面出现多个码 → 候选列表逐个「改选」，一次只填一个。
- 作业页确认前校验当前步骤类型（要扫库位却扫到物品 → 确认卡禁用「确定填入」并提示）。
- 连续帧去重不重复弹；无命中约 3 秒后给可见提示；迟到授权/播放失败都释放设备。
- 条形码码制从仅 Code128 扩到 **Code128 / Code39 / Code93 / EAN-13 / EAN-8 / UPC-A / UPC-E / ITF / Codabar**。
- 扫码枪/手输逻辑不变，始终可用。

### 4. 移动端按钮变形
- 作业页按钮组窄屏两列等分、主操作独占一行全宽、触控 ≥48px；实测 390px 无整页横向溢出（body=375 ≤ viewport）。

## 测试验证（每阶段先测后提交）

| 阶段 | 提交 | 验证 |
|---|---|---|
| A 导航+按钮 | 385eca9 | 390px 真实浏览器截图核查，导航/按钮布局实测通过 |
| B 统一浮层 | a7f746e | scan-camera 组件 8 项生命周期测试 + item-ui 接线回归全绿 |
| C 确认卡/多码/码制 | e66356e + d3c8866 | Code128/Code39/EAN-13 合成像素 fixture 用真实 ZXing 解码通过；多码候选 DOM 测试通过；全量 npm test 606/606；Chromium 整页 1/1 |

期间发现并修复了两个测试自身问题（Promise 授权死锁、Code39 fixture 字符间隔错误）——均通过真实解码失败暴露，未弱化断言。

## 生产核验（v2.36.0 已上线）
- `APP_VERSION=2.36.0`、`lib/scan-camera.js` 已在生产可取；
- 390px 移动视口底部导航：物品作业直接可见；
- 点击「相机扫码」打开全屏浮层，无摄像头权限时错误常驻显示（headless 环境预期行为）。

## 你实机验证清单（阶段 D）
1. 手机打开作业页 → 相机扫码：扫库位二维码 → 确认卡出现 → 确定填入；
2. 扫容器二维码、物品条形码（Code128 标签），确认各自码制显示正确；
3. 画面里同时对准两个码 → 候选列表切换后确定；
4. 扫错类型（当前要库位却扫物品）→ 确认卡应禁止确定；
5. 查询页扫任意码 → 确认填入查询框后立即查询；
6. 若某实物条形码扫不出，把标签拍照发我，我按实际码制再补。

## 未变边界
扫码工作台（旧 MAT/WIP）保留原 camOverlay 路径不动；扫码/持久化/飞书试运行协议未改；真实命中率以你的实测为准。
