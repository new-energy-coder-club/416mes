# 416MES 开发需求书（最终实现版）

| 项目 | 内容 |
| --- | --- |
| 系统名称 | 416MES 仓储管理系统（社团制造执行/仓储一体化单机版） |
| 文档版本 | v1.1（Final / 按已实现功能反写 + SOR 动态调整需求新增） |
| 日期 | 2026-09-14 |
| 适用对象 | New Energy Coder 社团（玉衡 A416 实验室 + B/C 区仓库 + 天权楼/401 场地） |
| 交付形态 | 单文件网页应用 index.html + 门户 home.html + Node 同步脚本 + 飞书多维表云端后端 |
| 本次变更 | 新增需求：需求 SOR 动态调整；最终目标：任何零件 30 S 内可以知道精确位置 |

## 1. 项目背景与目标

### 1.1 背景

社团仓储管理存在以下痛点：

- 物料散落在天权楼、A416 实验室、401 空地、B/C 区货架，无统一台账；
- 领料/补货/退料口头操作，库存数量与实物对不上，无人可追溯；
- 无标签体系，找料靠记忆，新人无法上手；
- 数据存于个人电脑/微信群，无备份、无审计。

### 1.2 建设目标

1. **一物一码、一位一码**：物料/库位/容器/物品/手册/工单全部贴码，扫码即查、扫码即作业；
2. **工单闭环**：没有扫码动作，库存数就不变；所有库存变动留痕可回放校验；
3. **零成本运行**：双击 HTML 即用，无需安装、无需服务器，首期硬件投入 ¥600 内；
4. **数据不丢**：本地 localStorage + Excel/JSON 双向导出 + 飞书多维表云端真源，三层备份；
5. **新人零学习成本**：Excel 模板导出什么样、导入就什么样。
6. **30 S 精确定位（最终目标）**：任何零件在 30 S 内可以知道精确位置（精确到库位 / 容器 / 模块区，即"架-层-位"级别），无需翻箱倒柜、无需询问原经手人；本条为**需求 SOR 动态调整**项——SOR（需求条目）随业务变化可持续迭代、可增删细化，但"任何零件 30 S 内定位"作为最终目标不降级。

### 1.3 范围界定（In / Out of Scope）

| 范围内 | 范围外 |
| --- | --- |
| 仓储台账、扫码出入库、四类库存工单（LL/BH/JH/TL）、盘点、标签打印 | 生产排产（APS）、设备 OEE、质检 SPC 等传统工厂 MES 模块 |
| NEC 社团轻量任务工单（RW/CG/WX/HD/QT） | 多人并发实时协同（云端以飞书准实时同步实现） |
| 排版工具（看板/海报/贴纸）、NEC 图库 | 财务成本核算（仅记录物料成本字段作参考） |
| 飞书 / InvenTree / 闲鱼 三类外部同步 | ERP 对接、权限体系（操作人为自选身份，非鉴权） |

## 2. 总体架构

### 2.1 架构图

```text
┌────────────────────────────────────────────────────────────┐
│ 门户 home.html（模块入口 + 在线自检）                        │
├────────────────────────────────────────────────────────────┤
│ 作业端 index.html（单文件，localStorage: mes416_state_v1）   │
│   ├─ 物料台账 / 库位容器 / 编码生成 / 标签打印               │
│   ├─ 扫码出入库（扫码枪/摄像头/手输）                        │
│   ├─ 工单 + 盘点 + 库存流水（审计）                          │
│   ├─ 人员台账 / NEC小工单 / 排版工具(iframe)                 │
│   └─ Excel(SheetJS) / JSON 双向导入导出                      │
├──────────────┬───────────────┬─────────────────────────────┤
│ 飞书真源      │ InvenTree     │ 闲鱼同步                    │
│ feishu-server │ inventree-sync│ xianyu-sync.py              │
│ .mjs :8000    │ .mjs → :8001  │ (.venv-xianyu 隔离环境)      │
│ /api/feishu-  │               │ → 合并进台账 xlsx            │
│ sync (Vercel) │               │                             │
└──────────────┴───────────────┴─────────────────────────────┘
```

### 2.2 三种运行模式

| 模式 | 启动方式 | 数据行为 |
| --- | --- | --- |
| 真源模式（推荐） | `node feishu-server.mjs` → http://localhost:8000 | 启动自动从飞书 8 表拉取合并（飞书为准）；工单执行/盘点/手工调整直写飞书（含流水）；断网进离线队列，恢复后冲刷 |
| 公网模式 | https://mes.newenergycoder.club | 自动改走 Cloudflare Tunnel 后端 api.newenergycoder.club（同一 feishu-server，配置见 `.cloudflared/config.yml`）；`file://` 双击时 CORS 放行走线上接口 |
| 纯离线模式 | 双击 index.html / `python -m http.server` | 仅 localStorage，功能不变，不实时上云 |

## 3. 功能需求（按最终实现）

### 3.1 物料台账

- 字段：物料码、名称、规格、分类（下拉）、库存数量、安全库存、库位、容器、模块区、成本、图片链接、闲鱼XY编号；
- 台账搜索：按编码/名称/规格/库位/容器/模块区即时过滤；
- 手工改数留痕：直接改库存必填调整原因，取消则还原；负库存红底告警；
- 安全库存预警：库存低于阈值黄底提示；
- 模块区挂接：「模块区」列下拉候选（M-01 连接器 / M-02 电机 / M-03 电池 / M-04 防护设备 / M-05 端子线材），扫 `LOC:M-0x` 区卡直接列出该区物料。

### 3.2 库位 / 容器 / 模块区主数据

- 首次打开自动载入种子：B 区 64 货架库位 + 12 工位格 + 40 容器 + 2 示例物料；
- C 区角钢货架（150×30×200cm 四层×6 架）内置进系统：自动入库 106 库位 + 82 容器（缺则补、不覆盖已有改动）；层号从上往下编，第 4 层为预留位；
- 模块区为正式主数据：可增（编码生成页自动生成 M-xx）可删（标签打印页勾选删除），随 Excel 导出/导入。

### 3.3 编码生成

- 「⚡ 空白编码自动编号」按 `分类-3位流水`（如 JG-001）生成物料码；
- 库位按货架/层/位参数一键批量补齐，空位提前打印、贴上即启用；
- 场地码下拉：TQ / YH / K401 / B，支持块位码批量生成；
- 工单号按「类型+日期+3位流水」自动递增。

### 3.4 扫码出入库闭环（核心规则）

| 机读串 | 行为 |
| --- | --- |
| `MAT:GJ-SD-001` | 查物料库存/位置 |
| `LOC:B-01-03-04` / `LOC:M-01` / `CTN:XK-001` | 查该位置存放什么 / 该区物料清单 |
| `WIP:LL…`（领料） | 打开工单，确认数量后「确认执行」扣减库存 |
| `WIP:BH…` / `WIP:TL…`（补货/退料） | 执行后增加库存 |
| `NEC:RW…` | 打开 NEC 小工单 |

约束：库存不足或未建档拦截；工单执行一次后闭环，不可重复执行；支持扫码枪、手机摄像头（jsQR）、手输回车三种录入方式。

### 3.5 工单管理（库存工单）

- 类型：LL 领料 / BH 补货 / JH 拣货 / TL 退料；
- 工单打印随货走，扫 `WIP:` 码执行才动库存；
- 执行自动写库存流水（时间/操作人/类型/±数量/余量/关联单/原因）。

### 3.6 盘点模式

扫码页勾选「📦 盘点模式」，扫物料码输实盘数，差异自动生成流水。

### 3.7 不可变审计日志

| 能力 | 需求 |
| --- | --- |
| 追加写只增不改 | 流水每条带 seq 单调序号 + ISO 时间戳 + 设备 ID，永不截断；扫码日志同样只增不改（界面仅显示最近 200 条） |
| 回放校验 | 「🔄 回放校验」从首条余量反推期初，逐条重算链式余量并比对，篡改/丢数即刻定位（物料+类型+应为/实为） |
| 审计导出 | 「⬇ 导出审计日志（JSON）」全量流水按时间正序输出 |

### 3.8 人员台账

编号 MB-xxx 自动生成不可改；字段：姓名/学号/部门 SIG/职务/电话/备注；姓名自动同步到右上角「操作人」下拉与 NEC 工单「负责人」候选；支持搜索、增删、Excel「人员」sheet 导出导入、JSON 备份合并。

### 3.9 NEC 小工单

- 字段：标题/类型/负责人/优先级/截止/关联物料/备注；状态流：待处理→进行中→已完成/已取消；
- 编码：类型+日期+3位流水（`RW20260909003`）；类型：RW 任务 / LL 领料 / CG 采购 / WX 维修 / HD 活动 / QT 其他；
- 勾选后打印紫色 NEC 工单标签（机读串 `NEC:编码`）；
- 随 Excel 台账导出/导入（sheet「NEC工单」）；
- 飞书同步：`node nec-sync.mjs init/push/status` —— 按工单号判重（有变才更新），新建和状态变更发群通知（markdown 卡片），`--idempotency-key` 防重发，`--dry-run` 只打印计划。

### 3.10 标签打印（七类标签，NEC26 主视觉）

| 类型 | 色块 | 编码规则 | 机读串 |
| --- | --- | --- | --- |
| 库位码 | 蓝 | `B-01-03-04` / `W01-G02` | `LOC:` |
| 容器码 | 绿 | `A4SH-015` / `SLG-028` / `XK-036` / `ZZX-102` | `CTN:` |
| 物料码 | 黄 | `GJ-SD-001`（大类-中类-流水） | `MAT:` |
| 物品码 | 青 | `WP-001` | `ITM:` |
| 手册码 | 玫红 | `SC-001` | `MAN:` |
| 工单码 | 红 | `LL20260909007` | `WIP:` |
| NEC工单 | 紫 | `RW20260909003` | `NEC:` |

- 统一 NEC26「BUILT TO DAWN」视觉：海军蓝渐变条 + 白 Logo + 类型色块 + 底部标语条；Logo base64 内嵌，离线可打印；
- 纸张：60×40mm 三防热敏标签 或 A4 整版（3×7=21 张/页）；模块区卡为 A4 每页一张大卡；
- 自适应排版：属性文字 8.5–18pt 自动占满空白，编码自动填满左栏（字号 70%–120% 可微调）；
- 打印强化：纯黑条 + `print-color-adjust:exact`，忘开"背景图形"也能出黑条；
- 适配汉印 N41 热敏打印机；每张标签右下角自动带打印日期。

### 3.11 排版工具 + NEC 图库

- 「排版工具」页签内嵌 Zonlin 自动排版工具 1.5（`typesetting/`，含字体，分发时必须一起拷贝）；
- 数据桥：「📋 复制排版文本」直接填入画板 Text 框并触发重排；「⬇ 二维码 PNG」自动加入画板图层；
- 桥接不可用时降级为剪贴板/手动导入；
- NEC 图库面板（`nec-gallery.js`）：5 专辑 / 752 张 ROBOCON 赛场图，点击直接加入画板图层，CORS 受限时降级为新标签页打开；
- 导出 PNG/JPEG/SVG（A4/A3 按 300 PPI 预设）。

### 3.12 Excel / JSON 双向

- 「导出 Excel」9 sheet 双向模板（物料台账/库位/容器/人员/物品/手册/工单记录/NEC工单/库存流水），列结构固定 + 填表说明 + DEMO 行；
- 导入容错：缺编码行跳过并计数、未知 sheet 明确列出忽略，不静默吞数据；
- 「导出备份 JSON」全量（含流水）；「导入合并」按编码去重、流水号取最大防撞号、数量差异自动写流水。

## 4. 集成需求

### 4.1 飞书多维表后端（云端真源）

- Base：「416MES 仓储管理后端」（8 张表，列结构与 Excel 模板一致，编码列为主字段）；base_token 与表 ID 存于 `feishu-backend.config.json`（本地，gitignore）。
- 上云去重业务键：物料码/库位码/容器码/编号/物品码/手册码/工单号/流水号；
- 库存流水只增不改：无 seq 旧数据跳过不推，云端流水号 `#000123` 幂等；
- 网页端「☁ 云端同步」：Vercel Serverless Function `api/feishu-sync.js`（tenant_access_token 读 8 表），环境变量 FEISHU_APP_ID / FEISHU_APP_SECRET，与导入合并共用同一套合并逻辑；
- 已验证完整往返：导出 → push → pull → 导入合并，日期/时间戳/余量逐字节一致。

### 4.2 InvenTree 同步（路线 B）

本地 InvenTree（http://localhost:8001）：扫码页「☁ InvenTree 实时同步」设置一次后，工单执行/盘点/手工调整自动写入（扫码即写），失败可从操作日志发现并对账。详见 `INVENTREE-INTEGRATION.md`。

### 4.3 闲鱼数据同步

字段映射：`outer_id` → 物料码、`product_id` → 闲鱼XY编号、标题 → 名称、`stock` → 库存、售价/100 → 成本、首图 → 图片链接、分类默认 QT。合并模式不覆盖本地已维护的分类/库位/容器/安全库存，空值不覆盖旧值（数量/成本除外，0 表示售罄）。凭据存 `xianyu-sync.config.json`（gitignore），依赖装于隔离环境 `.venv-xianyu/`。

### 4.4 历史数据导入

飞书表「A416零件位置明细」→ 区域父记录映射 YH-NN 库位、物品猜分类 + `分类-流水` 物料码；导入为整表替换，需先在 Excel 审阅合并。

```bash
node feishu-sync.mjs status                              # 连通性 + 各表记录数
node feishu-sync.mjs push 416MES_备份_xxx.json            # 本地 → 飞书，按业务键 upsert，幂等
node feishu-sync.mjs pull [输出.json]                     # 飞书 → 本地，与「导入合并」同构
node inventree-sync.mjs push                             # 物料/库位/容器/库存 → InvenTree，MAT:/LOC:/CTN: 自动绑定
PY=.venv-xianyu/Scripts/python.exe
$PY xianyu-sync.py auth / pull --dry-run / pull --merge 台账.xlsx
node feishu-import.mjs --dry-run                         # 预览映射
node feishu-import.mjs                                   # 生成 416MES_导入_A416零件_YYYYMMDD.xlsx
```

## 5. 数据需求

### 5.1 场地与分类体系

- **场地码（一级）**：TQ 天权楼（机构类零件）/ YH 玉衡 A416（贵重件）/ K401 401 空地（整机+调试设备）/ B B 区默认货架区。
- **分类码（二级，物料码前缀）**：JG 机构件 / DJ 电机驱动 / DZ 电子件 / GZ 贵重件 / TS 调试设备 / GJ 工具 / HC 耗材 / QT 其他。
- **位置码（三级）**：货架场地 `YH-01-03-04`（场地-架-层-位）；开放空地 `K401-A03`（场地-通道-块位，地贴/立柱贴码）。
- **容器类型缩写**：A4SH=A4 四抽收纳盒 / SLG=三连格文件盒 / XK=斜口零件盒 / ZZX=6040 周转箱 / SC4=四层四格牛皮纸收纳盒 / KF=开放式收纳格。

### 5.2 存储

- 浏览器 localStorage（键 `mes416_state_v1`）；
- 内嵌依赖：qrcode-generator（MIT）、SheetJS、jsQR，无网络依赖；
- 源码维护：直接编辑 `index.html`（以 git 历史为准）；`.build/` 下 part1.html / app.js 为历史快照，⚠ 禁止 `cat` 重拼生成 index.html。

## 6. 非功能需求

| 类别 | 需求 |
| --- | --- |
| 离线可用 | 双击 HTML 全功能可用（打印/扫码/台账/导出），仅云端同步需网络 |
| 断网容错 | 真源模式断网自动进离线队列，恢复后冲刷 |
| 可审计 | 流水只增不改 + seq + 设备 ID + 回放校验 + 审计 JSON 导出 |
| 数据安全 | 密钥类配置（feishu/xianyu/inventree）全部 gitignore；定期导出备份 |
| 打印兼容 | 汉印 N41 + 60×40mm 三防纸；打印对话框需设边距=无、缩放=100%、取消页眉页脚、背景图形=开 |
| 移动端 | 手机摄像头扫码（📷 摄像头扫码）直接识读 `MAT:` / `LOC:` 码 |
| 定位时效（SOR 动态调整项） | **任何零件 30 S 内可确定精确位置**：扫 `MAT:` 码或台账搜索即在 30 S 内返回库位/容器/模块区（含精确到架-层-位）；台账无记录时须给出最近一次扫码/流水的时间与位置，禁止"查不到即为空"；该条目作为 SOR 动态调整项，允许随业务细化，但响应时效不高于 30 S |
| 成本 | N41 ¥500 + 四色三防纸约 ¥80，首批总投入 ≤ ¥600 |

## 7. 维护规则（运营约束）

1. **编码不变，属性可变**：改名/换规格/调货位只改台账字段，编码永不重打；标签只在物理载体报废时作废；
2. **台账即主数据**：Excel 导出可直接导入飞书多维表，也可编辑后回导系统；
3. **空位提前批量打印**：贴上即启用；
4. **打印登记**：三防纸正常用 2 年以上，褪色即重打并扫码核对编码一致；
5. **工单闭环**：工单打印随货走，扫 `WIP:` 码执行才动库存。

## 8. 验收标准

| # | 验收项 | 通过标准 |
| --- | --- | --- |
| 1 | 离线启动 | 双击 index.html，种子数据自动载入，全部页签可用 |
| 2 | 扫码闭环 | 扫 MAT/LOC/CTN 正确查询；扫 WIP 执行后库存准确增减，二次执行被拦截 |
| 3 | 审计 | 回放校验全绿；导出审计 JSON 含 seq/时间/设备 ID |
| 4 | 打印 | 七类标签 60×40/A4 版面正确，手机扫码可识读机读串 |
| 5 | Excel 双向 | 导出 9 sheet → 原样导入，数据逐字节一致 |
| 6 | 飞书闭环 | push/pull 幂等，往返无重复记录、无字段漂移 |
| 7 | 盘点 | 盘点差异自动写流水，余量链正确 |
| 8 | NEC 工单 | 状态流转正常，飞书 push 判重正确，群通知不重复 |
| 9 | 30 S 定位（最终目标） | 随机抽查任一零件（含已领出/在容器内的），从提出查询到给出精确位置（库位/容器/模块区，精确到架-层-位）用时 ≤ 30 S；查台账无果时须定位到最近一次扫码/流水的时间与位置 |
