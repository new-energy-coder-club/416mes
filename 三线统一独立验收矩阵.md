# 三线统一独立验收矩阵

状态：主会话独立验收规格；尚未执行候选验收。适用于GLM/Kimi/GPT三个相同基线的worktree。候选自测不是本表通过证据。

## 1. 执行与证据规范

每个候选先记录Git HEAD、dirty状态、Node版本、依赖锁文件。所有HTTP只允许隔离localhost fake服务，禁止自动加载生产飞书凭据；真实条码相机/飞书隔离表验证单列。

主会话将在候选可运行后补适配脚本，不要求候选暴露相同私有函数名；接口、页面行为和数据契约必须能映射到下述相同场景。每条记录：候选commit、命令、退出码、预期、实际、日志/截图路径、是否重试、剩余门禁。

记分以行为证据为准：PASS/FAIL/NOT_RUN/EXTERNAL_GATE。NOT_RUN不能计PASS。编译通过不能替代交互可用；DOM字符串包含某按钮不能替代按钮已接入逻辑。

## 2. 固定夹具

locations：L-A(active)、L-B(active)、L-X(disabled)。
containers：C-A(loc=L-A, active, version=2)、C-B(loc=L-B, active, version=5)、C-U(loc空, active)、C-X(disabled)。
items：I-P(pending，无容器)、I-A(in_stock,C-A,v3)、I-B(in_stock,C-B,v1)、I-O(out，无容器)、I-U(历史name/spec/loc，无新字段)、I-R(retired)。名称允许相同，所有code字符串保留前导零。
MAT：M-KEEP qty=7，有旧流水和工单夹具；所有ITM场景前后该MAT数量及流水身份不变。
远端配置两类：九表与关键列齐全；分别移除container/status/version/lastOpId/操作ID任一列。fake认证使用假值。

## 3. 一票否决检查

| ID | 检查 | 通过条件 |
|---|---|---|
| G01 | 生产隔离 | 无真实凭据或生产写入，无自动push/deploy |
| G02 | 默认写保护 | 无可靠协调/认证配置的ITM POST拒绝，飞书无副作用 |
| G03 | 旧业务回归 | 原npm test通过，MAT/WIP语义未被ITM取代 |
| G04 | 实际页面接线 | 页面可加载、可进入新查询/作业，核心用户路径可操作 |
| G05 | 真实持久化 | 刷新保留草稿/命令，IDB失败不虚报完成 |
| G06 | 唯一物品 | ITM不能编辑qty或同码产生多个实物 |
| G07 | 不伪造全局锁 | 实现与说明不将内存锁/飞书版本视作跨实例CAS |
| G08 | 自测未削弱 | 原测试不被删除/跳过以掩盖新回归，fixture修改有解释 |

任一FAIL：不选为主路线，修复后重测。共享协调和外部硬件未验证可作为外部门禁，前提是生产新写默认关闭且文档如实说明。

## 4. 功能行为矩阵

| ID | 输入/操作 | 预期 |
|---|---|---|
| M01 | 加载I-U旧记录 | unknown而非in_stock；旧loc显示历史线索 |
| M02 | LOC L-A→CTN C-A→ITM I-P | 草稿可准备入库，数量固定1，不自动远端写 |
| M03 | 首步CTN/ITM或裸码歧义 | 提示当前步骤，不调用MAT/WIP执行 |
| M04 | L-A→C-B | 归属冲突，不自动迁移容器 |
| M05 | L-A→C-A→I-B出库 | 来源不符，拒绝 |
| M06 | I-A再次入库、I-O再次出库、I-R入库 | 拒绝非法状态 |
| M07 | 修改上游LOC/CTN | 提示并清下游，不残留上一件 |
| M08 | 相机/检索返回前切行 | 旧结果不能填写新行 |
| M09 | 同码视频多帧、Enter重复、双击确认 | 一条草稿/一个稳定命令身份，不自动累加 |
| M10 | 码001与1 | 按码规则区分，不能数值化 |
| M11 | 查询I-A | C-A/L-A、状态及同步时间；不写操作 |
| M12 | 查询C-A | L-A及I-A清单，点击下钻 |
| M13 | 查询L-A | C-A清单与真实ITM计数，旧MAT不冒充ITM |
| M14 | 模糊同名搜索 | 返回候选而非选择第一条 |
| M15 | moveContainer模拟C-A→L-B | ITM当前位置随容器，无逐件入出库，无MAT变更 |
| M16 | 出库确认container清空→拉回 | 空值保留，不用旧本地值补回 |
| M17 | 普通档案改名 | 不附带陈旧status/container覆盖受控字段 |
| M18 | 飞书手改受控字段 | 冲突隔离，不能普通LWW自动接受 |

## 5. 存储及同步矩阵

| ID | 场景 | 预期 |
|---|---|---|
| P01 | 确认入队时IDB put失败 | 命令/state全无或保留明确草稿，未发网络 |
| P02 | 普通save在命令持久化前后交错 | 无旧快照覆盖最新命令 |
| P03 | records replaceAll后恢复 | 操作缓存和独立draft仍在 |
| P04 | IDB新命令+LS旧dirty启动 | 新命令不丢、不自动当成功、不盲重发 |
| P05 | 同origin两个标签 | 不产生互相覆盖；若限制单作业标签则明确阻断 |
| P06 | 全量拉取新字段 | 受控字段组一起接收/冲突，不能与本地拼接 |
| P07 | 增量拉取新字段 | 结果与全量一致，基线/水位同步持久 |
| P08 | 飞书第九表缺页 | 不自动删本地操作历史、不推进完整水位 |
| P09 | 缺关键列/单选值 | 禁ITM正式写入，旧查询/普通业务可用 |
| P10 | CLI/导入/恢复旁路 | 不直接写受控字段，不自动重发未知命令 |
| P11 | 普通飞书新增无状态物品 | unknown，可查，不可自动出库 |
| P12 | 新建码冲突/多个远端同code | 隔离，不取任意一条；不回收旧物品码 |

## 6. 服务端及故障矩阵

协调部分使用可注入的隔离模拟适配器验证协议，并独立检查生产适配器启用条件；模拟全通过不代表实际共享设施已验证。

| ID | 场景 | 预期 |
|---|---|---|
| S01 | 同opId同载荷重复 | 一次业务效果，原结果可查询 |
| S02 | 同opId异载荷 | 幂等冲突，不执行第二种内容 |
| S03 | 两handler实例同时出库I-A | 在配置的共享测试适配器下一次合法接受；无适配器均拒绝 |
| S04 | PREPARED成功，实体写失败 | 保留可恢复状态，不假报APPLIED |
| S05 | 实体成功，APPLIED更新失败 | 原命令重查/恢复不重复出库 |
| S06 | 飞书写超时但迟到成功 | 持久屏障，不因一次读不到或TTL到期重放冲突命令 |
| S07 | handler中断后新实例恢复 | 从持久记录恢复，不靠旧闭包 |
| S08 | 容器移库与成员换箱交叉 | 同一仲裁协议，关系不撕裂 |
| S09 | 无认证/越权/伪造operator | 服务端拒绝，审计身份不相信请求体 |
| S10 | 查询未知opId | 明确未知，不等于未执行，前端不自动换ID |
| S11 | GET/查询页 | 无库存副作用 |
| S12 | 新旧schema/客户端重叠 | 旧受控写被拒，不能绕行通用upsert |

## 7. 一维条码与UI验收

- 使用公开/合成Code128唯一ITM标签fixture验证实际解码适配器，而非只mock返回文本；若库依赖浏览器API则用合适浏览器测试。
- QR仍可识别LOC/CTN；EAN/UPC型号码不能自动绑定任意实物。
- 无相机权限、解码库失败、无BarcodeDetector时给扫码枪/手输回退，保留当前草稿。
- 桌面/手机窄视口验证当前步骤、当前行、确认按钮和结果状态可见。
- Android/iPhone/飞书内置浏览器真机实扫为EXTERNAL_GATE，必须在交付报告明示。

## 8. 统一回收与评分表

每条以最终commit为单位运行同一批矩阵，修复后重跑受影响项及全量回归。每线由主会话填写：

| 路线 | commit | 原测试 | 独立行为测试 | 一票否决 | 外部门禁 | 正确性35 | 衔接25 | 证据20 | UX10 | 维护10 |
|---|---|---|---|---|---|---|---|---|---|---|
| GLM | 待回收 | 未复测 | 未执行 | 待查 | 待查 | — | — | — | — | — |
| Kimi | 待回收 | 未复测 | 未执行 | 待查 | 待查 | — | — | — | — | — |
| GPT | 待回收 | 未复测 | 未执行 | 待查 | 待查 | — | — | — | — | — |

选择一条完整基线，不默认偏向当前模型路线。不选路线保留分支供追溯。主工作区本地集成后再完整复测；不push触发生产CI/CD。
