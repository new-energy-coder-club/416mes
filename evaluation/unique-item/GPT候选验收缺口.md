# GPT候选验收缺口与集成前条件

依据候选LINE_REPORT（S5准备冻结）与主会话实跑证据，不等于最终择优结论。

## 已有可验证基础

- 唯一实体模型、九表字段、受控普通写保护；
- IDB事务、全量/增量受控组、迟到ACK、未知命令保留；
- 独立查询、只读扫码、作业表格、实际Code128解码；
- 服务端受控命令、实际飞书仓储适配、模拟共享协调协议；
- 主会话Node全量577通过，Chromium整页拒写路径通过，fake飞书真实Repository成功链通过。

## 不能当外部配置掩盖的代码缺口

1. JSON导入将itemRecovery隐藏存储，普通用户看不到待复核命令；需至少可见警告+原opId只读列表，禁止自动重发。
2. retire缺UI；若出库/归还满足本轮核心CRUD，可明确列管理增强，但不能称作废维护全部完成。
3. LOC只能启用不能受控停用/重新启用；表里已有disabled选项时需明确不支持入口，防止运营手改绕过。
4. 冲突有存储/阻断但无专用修复向导；至少必须可见原因、对象、原值/观察值、检查入口，不能永久无解锁路径而不说明。
5. 旧批量生成器扩列后云端新建会拒绝，需明确UI阻断/引导，不能本机显示生成成功让用户打印未正式确认码。
6. 完整Chromium成功链还未执行；现有分层成功链不能覆盖全部启动/UI桥接副作用。

## 真实外部门禁

- Vercel持久共享协调和身份提供者未配置；默认禁新写是正确安全行为，但正式多设备业务尚不可开放。
- 用户已建飞书操作表但tableId/schema/ACL未核验，不能访问生产测试。
- 真实手机相机/飞书内置浏览器/实物标签和打印未验。

## 固定提交后验收动作

1. 核对git clean及HEAD；独立对抗脚本全部复跑。
2. 核查新增依赖和schema对用户手工列清单一致性。
3. 对代码缺口逐项分类：集成前必须补/后续增强/需用户确认；不把NOT_RUN计PASS。
4. 本地集成前先保留候选分支，不push、不发布。
