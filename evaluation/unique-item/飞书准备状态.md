# 真实飞书准备状态（开发只读登记）

## 用户已提供事实

- 链接指向Base `NpWBb0RXYayqfosYqnScWdlDnMb`。
- 截图显示原有物料台账、库位、容器、人员、物品、手册、工单记录、库存流水仍在。
- 左侧已新增并选中“物品操作”，应作为同一Base第九张数据表，不是另一套数据源。

## 尚未核验

- “物品操作”Table ID（tbl...）未取得；Base分享链接不含table参数。
- 各字段名称、类型、单选选项、日期设置、系统最后更新时间及读写权限未核验。
- 应用凭据对新表访问权未验证，未读取凭据。
- 未向此Base发送开发测试写入，也未将此Base标识硬编码到任何新候选默认配置。

## 已提示用户的安全事项

截图共享弹窗显示“互联网获得链接的人可编辑”，建议限制为受邀访问/只读。本会话未擅自修改该权限，也不能据截图推断实际应用权限已齐全。

## 接入门禁

仅拿到tableId并完成schema/ACL只读核对后，才能配置独立隔离环境验证。生产迁移、正式写入和Vercel发布需另外确认；用户建表不等于批准代理执行生产写入。

候选必须与已给用户清单保持一致：操作类型使用文本；JSON存文本；phase四值；items状态五值；LOC/CTN active/disabled，空值客户端unknown，不偷偷要求新增unknown单选。
