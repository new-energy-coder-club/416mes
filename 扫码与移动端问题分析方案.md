# 扫码与移动端问题：根因分析与分阶段解决方案

> 依据：当前 main（2.35.0）源码核查 + 你提供的两张手机截图。三个专项分析代理调用均失败，本方案由主会话直接核查代码得出。本轮只做方案，不改代码。

## 一、四个问题的代码级根因

### 问题1：手机底部导航缺「ITM作业」——已实锤的一行 bug

- `index.html:1092`：`<button data-tab="item-work">ITM作业</button>` 缺少 `data-primary="1"`。
- `index.html:940`：手机折叠态 `nav.tabs button:not([data-primary]){display:none}`，所以它被隐藏，只能点「☰ 全部」。
- 修复就是加标记，并把按钮文案定为短名「物品作业」，避免窄屏挤压。

### 问题2/3：扫码扫不出来 / 扫出来逻辑不对

**a) 条形码支持面太窄（实锤）**：`lib/item-barcode.js:4` 的 ZXing hints 只写了 `CODE_128`。如果实物标签是 Code39、EAN-13、ITF 等其他一维码，必然扫不出。而且 `decode()` 每帧未命中都抛异常，`lib/item-ui.js:83` 静默吞掉——用户得不到任何「正在解码/未支持码制」反馈，表现就是“扫不出来”。

**b) 两个 ITM 页面没有复用扫码工作台的成熟交互（实锤）**：扫码工作台有全屏浮层 `camOverlay`（`index.html:1664`，含扫描线动画、错误常驻、ESC关闭、焦点管理），识别后 520ms 再填入并弹全屏结果卡（`scanFullscreen`，2870 行）。而 ITM 查询页/作业页各自在页面下方内嵌 `<video>`，直接怼出摄像头画面，没有确认弹窗——这就是你说的“下面直接出现一个摄像头界面，不好”。

**c) 识别成功即直接执行（实锤）**：查询页 `queryScan()` 扫到就查询（item-ui.js:62），作业页 `accept()` 扫到就填行（77 行），都没有「这是哪个码，确定吗」确认环节，更没有多码候选。

**d) 作业页扫码比查询页更不可靠的嫌疑点**：作业页每 250ms 同步跑 ZXing 解码（主线程阻塞），查询页 300ms 且异步；加上码制受限和错误全吞，实测“扫不出来”与代码行为一致。真实命中率必须靠实物实测，本方案不预断。

### 问题4：移动端按钮变形

截图中「填入本步骤/相机扫码/关闭相机/重扫本行/确认本行/保存草稿/恢复最近草稿」七个按钮挤在一起换行变形。根因：这组按钮没有统一的 toolbar 分组样式约束，长文案 + 窄屏导致各自换行；且主按钮「确认并加入待提交」与其他辅助按钮同权，视觉上找不到重点。

## 二、统一扫码组件设计（核心方案）

新建一个共享扫码组件 `lib/scan-camera.js`（纯 UI/相机，不含业务），三个调用方共用：**扫码工作台、查询页、作业页**。

```
scanCamera.open({ onConfirm(result), onCancel, hint })
```

交互协议（统一）：

1. 点击「相机扫码」→ 打开全屏浮层（复用现有 camOverlay 的样式/焦点/ESC/错误常驻模式，不是页面内嵌 video）；
2. 浮层内并行尝试解码：**jsQR 解二维码** + **ZXing 解一维码**，hints 扩到 CODE_128 / CODE_39 / EAN-13 / EAN-8 / ITF / UPC 等实物常见码制；
3. 识别到码 → 暂停取流帧 → 浮层内弹确认卡：显示码类型（二维码/Code128 等）、码值、命中对象摘要（如「物品 WP-001 · 电烙铁」）、按钮「确定填入 / 重新扫描」；
4. **多个码**：画面里同帧命中多个时确认卡显示候选列表，用户一次选一个（ZXing 单帧单结果；多码支持 = 连续帧收集不同码值去重后列候选，而不是强行一帧多码）；
5. 同一码连续帧去重，已弹确认卡时不再重复触发；
6. 用户点「确定填入」才回调 `onConfirm`：
   - 查询页：填入查询框并立即查询；
   - 作业页：先按当前步骤校验类型（当前要扫库位却扫到物品码 → 确认卡上就提示类型不符，不允许确定），通过才写入当前行；
7. 关闭/切页/识别失败都恢复原状态，草稿不丢；扫码枪和手输全程等价可用。

技术要点：

- ZXing 解码保持现有合成像素 fixture 测试；hints 扩码制后补 Code39/EAN-13 合成 fixture；
- 解码失败不再静默：浮层内显示「未识别到码 / 支持的码制：二维码、Code128…」轻提示，不刷屏；
- 每帧解码节流（二维码与一维码交替或同帧都试，失败的跳过本帧），不在主线程死循环；
- 相机 token 绑定行/会话的现有防串行机制（cameraGeneration/row token）保留，确认卡也挂到 token 上，迟到结果不落错行。

## 三、分阶段实施与验收门槛

### 阶段 A：导航与移动端硬伤（最小、最快见效）

- 「ITM作业」加 `data-primary="1"` 并改短名「物品作业」；查询页已有 primary。
- 检查底部导航在 390px 的 4 个主入口是否都显示、不溢出。
- 作业页按钮分组：主操作「确认并加入待提交」独占一行全宽；相机/重扫一组；草稿保存/恢复一组；触控 ≥44px、16px 字号。
- 验收：桌面 1024 + 390px 真实浏览器截图对比；`npm test` 全绿；现有 item-ui 测试不破坏。

### 阶段 B：统一扫码浮层（组件化，不动业务协议）

- 抽出 `lib/scan-camera.js` 浮层组件；扫码工作台先切换到该组件（行为不变回归）。
- 查询页/作业页相机入口改为调用该组件，删除内嵌 video。
- 验收：Chromium mock 视频流 + 可控 canvas 帧测试（打开/关闭/ESC/切页停流、迟到授权释放）；`npm run test:browser` 全绿；三个页面共用组件的静态断言。

### 阶段 C：确认卡与码制扩展

- 识别后确认卡（单码）+ 多码候选列表（跨帧去重收集）；
- 查询页确定→查询；作业页确定→步骤校验→填行；
- ZXing hints 扩 Code39/EAN-13 等，新增对应合成 fixture 测试。
- 验收：合成像素 fixture 实际解码（已有模式）；DOM 测试确认卡流程（确认才回填、重扫不回填、类型不符禁确定）；`npm test` 全绿。

### 阶段 D：实物实机验证（你来做）

- 打印几张测试标签（LOC 二维码、CTN 二维码、ITM Code128 条形码），手机浏览器和飞书内置浏览器各扫一遍；
- 记录：识别成功率、光线影响、确认卡是否符合预期；
- 我根据你的实测结果调整码制/阈值/界面。

## 四、不做的事

- 不改动作业状态机、opId、持久化协议（本次纯交互与识别层）；
- 不引入新 UI 框架；ZXing/jsQR 版本不动（只扩 hints）；
- 不碰飞书写入逻辑和试运行模式边界；
- 真机命中率不预先承诺，阶段 D 以你的实测为准。

## 五、建议的优先级

A（导航+按钮变形）→ B（统一浮层）→ C（确认卡+码制）→ D（实机验证）。

A 是确定的一行级修复，B/C 是本次体验的主战场，D 由你验证。三个阶段各自独立测试，可以分开发布也可以一起发。
