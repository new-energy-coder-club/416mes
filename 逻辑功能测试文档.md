# 416MES 逻辑功能测试文档

> 目的：逐项验证网页端业务功能 + 网页端↔飞书端数据同步 + CRUD，**记录每一步的真实结果**。
> 结论只有两种：**通过**（附证据）或 **不通过**（附现象，进「问题清单」分阶段修）。
> 环境：https://mes.newenergycoder.club （线上，云端后端/Vercel + 飞书多维表格真源）
> 代码基线：`2.16.0`（commit `9bd29be`）｜自动化测试：`402/402`
> 最近更新：2026-09-16

---

## 〇、测试方法与约定

**四层验证，互相独立，避免「只有一种证据」：**

| 层 | 手段 | 能证明什么 | 不能证明什么 |
|---|---|---|---|
| L1 单元/集成测试 | `npm test`（402 条） | 纯逻辑正确（账本、合并、队列、增量协议…） | 真实浏览器行为、真实飞书 |
| L2 浏览器实测 | 线上页面 + 真实飞书 | 端到端真的能用 | 极端边界（要大构造） |
| L3 数据核对 | 直接读 `/api/feishu/state` 与飞书表结构 | 两边数据真的一致 | 界面显示是否正确 |
| L4 静态不变量 | `test/*-invariants.test.js` 等 | 关键接线没有被改回去 | 运行期行为 |

**测试数据约定：**
- 新建的记录一律带 `ZZT` 前缀（sentinel），便于识别与清理。
- 库存变动尽量做成**净零**（+N 再 −N），避免影响真实库存。
- 账本流水是 append-only：**测试流水保留不删**（删了会留下 seq 缺口，
  会让账本自校验变成「数据不完整」）。测试流水用 `类型=功能验证` 标记，便于日后识别。
- 每轮结束跑一次 `replayAudit`，确认账本仍然 `complete-valid`。

**结果标记：** ✅ 通过 ｜ ❌ 不通过 ｜ ⚠️ 有保留（能跑但有隐患，进问题清单） ｜ ⬜ 未执行

---

## 一、阶段划分

| 阶段 | 范围 | 用例数 | 状态 |
|---|---|---|---|
| 阶段一 | 功能清单与测试矩阵（本文档） | — | ✅ 完成 |
| **阶段二** | **网页端核心业务流**：扫码定位、存取（领/退/补/拣）、工单执行与冲销、盘点、手工调整 | 18 | ⬜ 待执行 |
| **阶段三** | **CRUD**：8 张表的增/删/改/查 + 写回飞书 + 回读一致 | 24 | ⬜ 待执行 |
| **阶段四** | **双向同步与身份/删除闸门**：增量同步、冲突裁决、删除闸门、来历不明、批量对账 | 14 | ⬜ 待执行 |
| **阶段五** | **边界与异常**：离线队列、导入导出往返、原子导入、配额、PIN、标签与纸张 | 16 | ⬜ 待执行 |
| **阶段六** | **问题修复与回归**（按阶段二~五发现的问题分批修） | — | ⬜ 待执行 |

---

## 二、功能面清单（测试矩阵的输入）

### 2.1 页签（8 个）
`物料台账` · `人员台账` · `标签打印` · `编码生成` · `库存工单` · `NEC小工单` · `排版工具` · `扫码出入库`

### 2.2 扫码前缀（7 种）
| 前缀 | 含义 | 自动识别顺序 |
|---|---|---|
| `LOC:` | 库位 | 无前缀时：仓库工单 → NEC 工单 → 物品 → 手册 → 物料 |
| `CTN:` | 容器 | 同上 |
| `MAT:` | 物料 | |
| `WIP:` | 库存工单（可执行、会动库存） | |
| `NEC:` | NEC 小工单（纯记录，不动库存） | |
| `ITM:` | 物品（设备/工具/展品） | |
| `MAN:` | 手册 | |

> ⚠️ 关键不变量：无前缀且**同时**匹配仓库工单和 NEC 工单时必须**停下并要求显式指定**（P6 修复点）。

### 2.3 业务类型（`mes-core` 的出入库语义）
| 类型 | 名称 | 库存方向 |
|---|---|---|
| `LL` | 领料 | 减少 |
| `BH` | 补货 | 增加 |
| `JH` | 拣货 | 减少 |
| `TL` | 退料 | 增加 |

### 2.4 API 路由（10 条）
`ping` `state` `schema` `nextcode` `stock` `upsert` `delete` `incremental` `changes` `reconcile`

---

## 三、阶段二：网页端核心业务流

**前置**：线上页面打开、`fsOnline=true`、队列为 0、账本 `complete-valid`。

| ID | 用例 | 方法 | 预期 | 结果 |
|---|---|---|---|---|
| F-01 | 环境就绪 | 刷新页面，读 `APP_VERSION`/`fsOnline`/队列/`replayAudit` | 版本=2.16.0、已连接、队列0、账本 complete-valid | ⬜ |
| F-02 | 扫码定位物料（MAT） | 扫 `MAT:<物料码>` | 显示物料详情（名称/规格/库存/库位/容器），并记一条扫码历史 | ⬜ |
| F-03 | 扫码定位库位（LOC） | 扫 `LOC:<库位码>` | 显示库位类型与说明，列出该库位上的物料 | ⬜ |
| F-04 | 扫码定位容器（CTN） | 扫 `CTN:<容器码>` | 显示容器类型/规格/所在库位 | ⬜ |
| F-05 | 扫码定位物品/手册 | 扫 `ITM:` / `MAN:` | 各自显示详情 | ⬜ |
| F-06 | 扫码 NEC 小工单 | 扫 `NEC:<NEC单号>` | 显示 NEC 详情，**不出现执行按钮**（不动库存） | ⬜ |
| F-07 | 扫码仓库工单 | 扫 `WIP:<工单号>` | 显示工单详情 + 本次执行数量输入 + 执行按钮 | ⬜ |
| F-08 | 扫码歧义保护 | 构造同号（NEC 与仓库工单同号）后扫描裸码 | **停止操作**、给出 `WIP:`/`NEC:` 指定办法、**无执行入口** | ⬜ |
| F-09 | 未知码兜底 | 扫一个不存在的码 | 按物料查询并明确提示「未找到」 | ⬜ |
| F-10 | 领料出库（LL） | 对测试物料执行 1 件领料 | 库存 −1；新增流水（类型 LL、变动 −1）；飞书库存与流水同步 | ⬜ |
| F-11 | 退料入库（TL） | 对测试物料执行 1 件退料 | 库存 +1；新增流水（类型 TL、变动 +1） | ⬜ |
| F-12 | 补货入库（BH） | 补货 1 件 | 库存 +1 | ⬜ |
| F-13 | 拣货出库（JH） | 拣货 1 件 | 库存 −1 | ⬜ |
| F-14 | 工单全量执行 | 建一张 2 件的工单并执行全部 | 状态→已执行；执行数量/批次记录；库存按方向变动 | ⬜ |
| F-15 | 工单部分执行 | 计划 5 件，本次执行 2 件 | 状态→部分执行；剩余 3；已执行 2；**不重复扣减** | ⬜ |
| F-16 | 工单冲销 | 对冲销一张已执行工单 | 库存按原方向回退；写冲销记录与冲销流水；状态→已取消 | ⬜ |
| F-17 | 工单取消 | 取消一张未执行工单 | 状态→已取消；**不动库存** | ⬜ |
| F-18 | 盘点（盘盈/盘亏/账实相符） | 对测试物料分别录 实际>账面、<账面、=账面 | 差异写流水；账实相符时变动 0；库存被设为实盘值 | ⬜ |
| F-19 | 手工调整库存 | 改数量并填原因 | 库存变目标值；写「手工调整」流水；原因入库 | ⬜ |
| F-20 | 负库存拦截 | 让库存减到负数 | 按业务规则拦截或标记 `row-neg`（需确认预期） | ⬜ |

---

## 四、阶段三：CRUD（8 张表 + 飞书同步）

每张表测四件事：**增（新建→飞书出现）→ 改（改字段→飞书更新）→ 查（刷新后仍在）→ 删（删→飞书消失）**。
方法：网页端操作 + `GET /api/feishu/state` 核对 + 飞书表结构核对。

| ID | 表 | 增 | 改 | 查 | 删 |
|---|---|---|---|---|---|
| C-01~04 | 物料台账 materials | ⬜ | ⬜ | ⬜ | ⬜ |
| C-05~08 | 库位 locations | ⬜ | ⬜ | ⬜ | ⬜ |
| C-09~12 | 容器 containers | ⬜ | ⬜ | ⬜ | ⬜ |
| C-13~16 | 人员 members | ⬜ | ⬜ | ⬜ | ⬜ |
| C-17~20 | 物品 items | ⬜ | ⬜ | ⬜ | ⬜ |
| C-21~24 | 手册 manuals | ⬜ | ⬜ | ⬜ | ⬜ |
| C-25~28 | 工单记录 workorders | ⬜ | ⬜ | ⬜ | ⬜ |
| C-29~32 | 库存流水 transactions | ⬜（只增） | ⬜（不改） | ⬜ | ⬜ |

**CRUD 附加不变量：**

| ID | 用例 | 预期 | 结果 |
|---|---|---|---|
| C-33 | 物料 upsert **绝不能带绝对库存** | 推送物料后飞书「库存数量」不变（P4 闸门） | ⬜ |
| C-34 | 业务键缺失必须被拦 | 导入缺业务键列的文件被拒且**一个字都不改** | ⬜ |
| C-35 | 空业务键的行不得入库 | 导入含空编码行 → 跳过并告警，不产生幽灵记录 | ⬜ |
| C-36 | 推送幂等 | 同一 opId 重复提交只记一次账 | ⬜ |

---

## 五、阶段四：双向同步与身份/删除闸门

| ID | 用例 | 方法 | 预期 | 结果 |
|---|---|---|---|---|
| S-01 | 飞书改→网页端自动出现 | 在飞书改一条记录，等轮询 | ≤10 秒内页面显示新值（实测基线 4.38s） | ⬜ |
| S-02 | 网页端改→飞书收到 | 网页端改一条，核对飞书 | 飞书同步更新，且**不含**绝对库存 | ⬜ |
| S-03 | 删除闸门：分页不完整不判删 | 构造截断响应 | 一条都不删，转「待人工核删」 | ⬜ |
| S-04 | 删除闸门：阈值 | 构造大批缺失 | 超阈值转人工，不自动删 | ⬜ |
| S-05 | 删除闸门：连续两次完整对账 | 正常对账 | 只有连续两次完整才自动确认 | ⬜ |
| S-06 | 已确认删除 vs 本地新建 | 飞书删一条 + 本地新建一条 | 前者进待核删、**不自动删**；后者进待推送 | ⬜ |
| S-07 | 冲突裁决：保留本地 | 构造真双改，选保留本地 | qty 走 **delta + 库存直写**；其它字段以本地为准 | ⬜ |
| S-08 | 冲突裁决：采用飞书 | 同上选采用飞书 | 本地值跟随飞书，不写回 | ⬜ |
| S-09 | 冲突裁决预览 | 点「预览」 | 零写入，给出两个选择各自的结果 + 服务端 dryRun 反馈 | ⬜ |
| S-10 | 冲突回滚 | 裁决后回滚 | 数据**和 `__base` 基线**一起回退 | ⬜ |
| S-11 | 冲突池刷新存活 | 造冲突后刷新 | 冲突仍在且**面板自动弹出** | ⬜ |
| S-12 | 「来历不明」不变量 | 造一条飞书没确认过、队列里也没有的记录 | 被点出来并报警；无基线时不误报 | ⬜ |
| S-13 | 批量对账模式（写入闸门） | 造 >200 冲突 | 自动进入闸门；写入只入队不提交；「待推送」不被误清 | ⬜ |
| S-14 | 同编号多行巡检 | 构造同工单号两行（自动编号不同） | 报警并在面板列出 | ⬜ |

---

## 六、阶段五：边界与异常

| ID | 用例 | 方法 | 预期 | 结果 |
|---|---|---|---|---|
| E-01 | 离线写入落盘 | 断网后扫码/调整 | 立即进队列（实测 1ms），刷新不丢 | ⬜ |
| E-02 | 恢复后自动冲刷 | 恢复联网 | 队列自动提交并清空 | ⬜ |
| E-03 | 待人工处理不再无限重试 | 让一条连续失败到 needs_attention | 停止自动重试，角标显示「N 待处理」 | ⬜ |
| E-04 | 队列面板可重试/放弃 | 点角标 | 面板列出状态与原因；重试复原；放弃写删除凭据 | ⬜ |
| E-05 | 关页同步落盘 | 触发 pagehide | LS 立即写入最新状态（`__rev` 递增） | ⬜ |
| E-06 | LS/IDB 判新 | 构造 IDB 落后 | 以 LS 为准且**继承 IDB 的基线** | ⬜ |
| E-07 | 配额告警 | 触发 QuotaExceededError | 弹常驻告警条 + 处置指引 | ⬜ |
| E-08 | 日志封顶与去抖 | 大量写日志 | 上限 500 行、同一 tick 只落盘一次 | ⬜ |
| E-09 | Excel 导出/导入往返 | 导出→原样导入 | 各表条数与内容一致；PIN 列**无明文** | ⬜ |
| E-10 | 导入原子性 | 构造中途失败 | **整体回滚**，数据与导入前一致 | ⬜ |
| E-11 | 导入不改基线 | 导入后再同步 | 不产生假冲突 | ⬜ |
| E-12 | 模板列结构与导出一致 | 比对两文件表头 | 逐列相同 | ⬜ |
| E-13 | PIN 码不外泄 | 导出 Excel + 备份 JSON | Excel 无明文；JSON 默认不含 pin 键 | ⬜ |
| E-14 | 人员职务锁定三选项 | 看下拉 | 负责人/成员/本科生 | ⬜ |
| E-15 | 标签页签分离 | 看库位/模块区两个页签 | 交集为 0 | ⬜ |
| E-16 | 区卡打印守卫 | 标签纸模式下选模块区卡打印 | 被拦下并自动切 A4 | ⬜ |
| E-17 | 编码生成不撞号 | 生成库位/容器/物品/手册/工单 | 编号唯一且连续；跨设备不撞号 | ⬜ |
| E-18 | NEC 编码空间独立 | 建 NEC 单 | 带 `NEC-` 前缀，与仓库工单不可能同号 | ⬜ |
| E-19 | 数据身份一致性 | 每轮结束跑 `replayAudit` | `complete-valid`、不一致 0 | ⬜ |

---

## 七、问题清单（发现的缺陷与修复记录）

> 每条格式：现象 → 根因 → 修复 → 回归验证。按阶段修，修完重新跑受影响的用例。

### 阶段二执行中发现的问题（2026-09-16 第 1 轮）

| # | 严重度 | 现象 | 根因（已定位） | 状态 |
|---|---|---|---|---|
| **B1** | 🔴 高 | 每张工单都会产生最多 5 条**假冲突**（`items`/`execQty`/`execBatches`/`reverseInfo`/`cancelInfo`）；实测一次阶段二就累积 37 条 | `lib/three-way-merge.js` 的 `eq()` 最后一行是 `return a === b` —— 对数组/对象是**引用比较**。两个结构完全相同的数组永远不 `===`，于是 `planMerge` 判定「两边都改过」→ 冲突 | ✅ 已修 `2.17.0` |
| **B2** | 🔴 高 | 每一条本地新建的流水都会假冲突：`ts` 本地 `2026-09-16T13:32:06.724Z` vs 飞书 `2026-09-16T13:32:08.596Z`；`time` 本地 `2026/9/16 21:32:06` vs 飞书 `2026-09-16 13:32:08` | ① 客户端 `fsPushStock` **不上送自己的时间戳**，服务端 `writeStock` 用 `new Date()` 另盖一个 → 差 1–2 秒；② `time` 由同一个飞书列派生，但本地用 `toLocaleString()`、服务端用 `fmtLocal()`，**格式都不同** → 即使同一时刻也不相等 | ✅ 已修 `2.17.0` |
| **B3** | 🔴 高 | 两边的 qty 真的差 1（本机 10 / 飞书 10 / **账本算出应为 9**），账本 8 条链式不一致 | B1+B2 制造 37 条假冲突 → 超过 10% 阈值 → **自动打开批量对账闸门** → 之后所有写入只入队不提交 → 两边真的分叉。**放大器，不是独立 bug** | ✅ 随 B1/B2 消失（闸门已关、两边 qty 已重新一致） |
| **B4** | 🟡 中 | 想修一个「qty 与账本不一致」时发现：**没有任何正规接口能改 qty** | P4 把 upsert 的绝对 qty 关了（这是对的），但因此 qty 只能由服务端按 `期初 + Σ变动` 收敛。而**补一条流水改不了这个差**（qty 和 Σδ 同时加同一个数，差不变）。正确修法只有一条：**先把账本里那些「余量」列改对**，再触发任意一次库存写入让服务端收敛重写 qty —— 这条路径目前只能手工做，没有工具 | ⚠️ 已定位，需要决定要不要加一个「账本修数」工具 |
| N1 | 🟢 低 | `transactions.reason` 冲突：local `盘点差异` / remote `功能验证` | **我的测试传错了参数**：UI 传的是 `r.txn.reason`（"盘点差异"），我传了固定字符串。不是产品缺陷，修正测试即可 | ✅ 已确认非缺陷 |

> **B1 是最值得追的一条**：它不是边界情况 —— 所有工单的 `items` 都是数组，
> 所以只要用到工单，就必然产生假冲突。而假冲突会累积到触发写入闸门，把整个同步停掉。

---

## 八、执行记录

| 轮次 | 日期 | 范围 | 结果 | 备注 |
|---|---|---|---|---|
| 1 | 2026-09-16 | 阶段一（矩阵建立） | ✅ | 本文档建立；基线 2.16.0 / 402 测试 |
| 2 | 2026-09-16 | 阶段二 第1轮（扫码定位 + 存取四类 + 工单流） | ⚠️ 部分通过，发现 B1/B2/B3 | F-01~F-13 逐项验证（见下）；B1/B2 已定位到具体代码行 |
| 2b | 2026-09-16 | 阶段二 第1轮 明细 | — | F-01 环境 ✅ / F-02 扫物料 ✅ / F-03 扫库位 ✅ / F-04 扫容器 ✅ / F-05 扫物品手册 ✅ / F-06 扫NEC不动库存 ✅ / F-09 未知码 ✅ / **F-10 领料 ✅ / F-11 退料 ✅ / F-12 补货 ✅ / F-13 拣货 ✅ / F-14 全量执行 ✅ / F-15 部分执行 ✅ / F-16 冲销 ✅ / F-17 取消 ✅**（F-07/F-08 扫码维度的工单/歧义待重跑）/ F-18~F-20 因闸门开启未完整观察 |
| 3 | 2026-09-16 | B1/B2 修复（`2.17.0`） | ✅ 已修 + 408 测试 + 变异测试 | 新增 B1 四条、B2 两条断言；注入旧 `eq()` 后「结构相同的数组不得判为冲突」立刻失败 |
| 4 | — | **阶段二 重跑（待执行）** | ⬜ | B1/B2 上线后需要**在干净状态**上重跑阶段二，确认不再产生假冲突 |

**当前遗留（本轮结束时如实记录）：**
- 测试物料 `ZZT-FLOW-1` 与 7 张测试工单（`*209901019xx`）**仍在生产库里**，且该物料的流水链
  因早期（B1/B2 还在运行时）被 qty 回写打歪，仍有若干条「余量」不一致 → 需要用 B4 的办法再修一次。
- 队列里还有少量未冲刷完的测试写入（正在自动收敛）。
- 真实业务数据未受影响（物料 4 条、库位 188 条、人员 26 条等均未改动）。
- **下一步**：先清理测试数据（工单删除 + 流水余量修正），再重跑阶段二 → 阶段三。
