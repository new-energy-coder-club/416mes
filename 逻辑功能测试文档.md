# 416MES 逻辑功能测试文档

> 目的：逐项验证网页端业务功能 + 网页端↔飞书端数据同步 + CRUD，**记录每一步的真实结果**。
> 结论只有两种：**通过**（附证据）或 **不通过**（附现象，进「问题清单」分阶段修）。
> 环境：https://mes.newenergycoder.club （线上，云端后端/Vercel + 飞书多维表格真源）
> 代码基线：`2.16.0`（commit `9bd29be`）｜自动化测试：`402/402`
> 最近更新：2026-09-16

---

## 〇、测试方法与约定

**四层验证，互相独立，避免「只有一种证据」：**

| 层 | 手段 | 能证明什么 | 不能证明什么 |
|---|---|---|---|
| L1 单元/集成测试 | `npm test`（402 条） | 纯逻辑正确（账本、合并、队列、增量协议…） | 真实浏览器行为、真实飞书 |
| L2 浏览器实测 | 线上页面 + 真实飞书 | 端到端真的能用 | 极端边界（要大构造） |
| L3 数据核对 | 直接读 `/api/feishu/state` 与飞书表结构 | 两边数据真的一致 | 界面显示是否正确 |
| L4 静态不变量 | `test/*-invariants.test.js` 等 | 关键接线没有被改回去 | 运行期行为 |

**测试数据约定：**
- 新建的记录一律带 `ZZT` 前缀（sentinel），便于识别与清理。
- 库存变动尽量做成**净零**（+N 再 −N），避免影响真实库存。
- 账本流水是 append-only：**测试流水保留不删**（删了会留下 seq 缺口，
  会让账本自校验变成「数据不完整」）。测试流水用 `类型=功能验证` 标记，便于日后识别。
- 每轮结束跑一次 `replayAudit`，确认账本仍然 `complete-valid`。

**结果标记：** ✅ 通过 ｜ ❌ 不通过 ｜ ⚠️ 有保留（能跑但有隐患，进问题清单） ｜ ⬜ 未执行

---

## 一、阶段划分

| 阶段 | 范围 | 用例数 | 状态 |
|---|---|---|---|
| 阶段一 | 功能清单与测试矩阵（本文档） | — | ✅ 完成 |
| **阶段二** | **网页端核心业务流**：扫码定位、存取（领/退/补/拣）、工单执行与冲销、盘点、手工调整 | 20 | ✅ **全部通过** |
| **阶段三** | **CRUD**：8 张表的增/删/改/查 + 写回飞书 + 回读一致 | 56 断言 | ✅ **全部通过** |
| **阶段四** | **双向同步与身份/删除闸门**：增量同步、冲突裁决、删除闸门、来历不明、批量对账 | 14 | ✅ **全部通过** |
| **阶段五** | **边界与异常**：离线队列、导入导出往返、原子导入、配额、PIN、标签与纸张 | 19 | ✅ **全部通过** |
| **阶段六** | **问题修复与回归**（按阶段二~五发现的问题分批修） | — | ✅ **完成**（四层验证全绿，见「总验收结论」） |
| **阶段七** | **覆盖率补测**：把交互点枚举出来逐条对账后发现的 17 处未覆盖业务逻辑 | 待定 | ◐ **进行中**（#1 已通过，见第十一节） |

---

## 二、功能面清单（测试矩阵的输入）

### 2.1 页签（8 个）
`物料台账` · `人员台账` · `标签打印` · `编码生成` · `库存工单` · `NEC小工单` · `排版工具` · `扫码出入库`

### 2.2 扫码前缀（7 种）
| 前缀 | 含义 | 自动识别顺序 |
|---|---|---|
| `LOC:` | 库位 | 无前缀时：仓库工单 → NEC 工单 → 物品 → 手册 → 物料 |
| `CTN:` | 容器 | 同上 |
| `MAT:` | 物料 | |
| `WIP:` | 库存工单（可执行、会动库存） | |
| `NEC:` | NEC 小工单（纯记录，不动库存） | |
| `ITM:` | 物品（设备/工具/展品） | |
| `MAN:` | 手册 | |

> ⚠️ 关键不变量：无前缀且**同时**匹配仓库工单和 NEC 工单时必须**停下并要求显式指定**（P6 修复点）。

### 2.3 业务类型（`mes-core` 的出入库语义）
| 类型 | 名称 | 库存方向 |
|---|---|---|
| `LL` | 领料 | 减少 |
| `BH` | 补货 | 增加 |
| `JH` | 拣货 | 减少 |
| `TL` | 退料 | 增加 |

### 2.4 API 路由（10 条）
`ping` `state` `schema` `nextcode` `stock` `upsert` `delete` `incremental` `changes` `reconcile`

---

## 三、阶段二：网页端核心业务流

**前置**：线上页面打开、`fsOnline=true`、队列为 0、账本 `complete-valid`。

| ID | 用例 | 方法 | 预期 | 结果 |
|---|---|---|---|---|
| F-01 | 环境就绪 | 刷新页面，读 `APP_VERSION`/`fsOnline`/队列/`replayAudit` | 版本=2.16.0、已连接、队列0、账本 complete-valid | ✅ 2.16.0 / fsOnline=true / 队列0 / complete-valid |
| F-02 | 扫码定位物料（MAT） | 扫 `MAT:<物料码>` | 显示物料详情（名称/规格/库存/库位/容器），并记一条扫码历史 | ✅ 显示编码+名称+规格+成本+库存+定位来源+最近扫码 |
| F-03 | 扫码定位库位（LOC） | 扫 `LOC:<库位码>` | 显示库位类型与说明，列出该库位上的物料 | ✅ `B-01-01-01` → 类型/说明/存放 1 项/合计库存 |
| F-04 | 扫码定位容器（CTN） | 扫 `CTN:<容器码>` | 显示容器类型/规格/所在库位 | ✅ `A4SH-001` → 容器类型/存放 0 项 |
| F-05 | 扫码定位物品/手册 | 扫 `ITM:` / `MAN:` | 各自显示详情 | ✅ 两个前缀都正确命中 |
| F-06 | 扫码 NEC 小工单 | 扫 `NEC:<NEC单号>` | 显示 NEC 详情，**不出现执行按钮**（不动库存） | ✅ 显示详情、**无执行入口**、流水条数未变 |
| F-07 | 扫码仓库工单 | 扫 `WIP:<工单号>` | 显示工单详情 + 本次执行数量输入 + 执行按钮 | ✅ `LL20260912001` → 含执行入口 |
| F-08 | 扫码歧义保护 | 构造同号（NEC 与仓库工单同号）后扫描裸码 | **停止操作**、给出 `WIP:`/`NEC:` 指定办法、**无执行入口** | ✅ 提示「这个码有歧义，已停止操作」+ 两种指定办法 + **无执行入口**；显式 `WIP:` 仍可用 |
| F-09 | 未知码兜底 | 扫一个不存在的码 | 按物料查询并明确提示「未找到」 | ✅ 「未找到物料 … （台账中无此编码）」 |
| F-10 | 领料出库（LL） | 对测试物料执行 1 件领料 | 库存 −1；新增流水（类型 LL、变动 −1）；飞书库存与流水同步 | ✅ 10→9、流水 `#23 领料工单 d−1→9`、两边一致。L1：`库存扣减：领料工单按明细扣减并写流水` |
| F-11 | 退料入库（TL） | 对测试物料执行 1 件退料 | 库存 +1；新增流水（类型 TL、变动 +1） | ✅ 9→10、`#24 退料工单 d1→10`。L1：`退料(TL)视为入库、拣货(JH)视为出库` |
| F-12 | 补货入库（BH） | 补货 1 件 | 库存 +1 | ✅ 10→11、`#25 补货工单 d1→11`。L1：`库存增加：补货工单按明细增加库存` |
| F-13 | 拣货出库（JH） | 拣货 1 件 | 库存 −1 | ✅ 11→10、`#26 拣货工单 d−1→10` |
| F-14 | 工单全量执行 | 建一张 2 件的工单并执行全部 | 状态→已执行；执行数量/批次记录；库存按方向变动 | ✅ 10→8、状态「已执行」、执行数量与批次均已记 |
| F-15 | 工单部分执行 | 计划 5 件，本次执行 2 件 | 状态→部分执行；剩余 3；已执行 2；**不重复扣减** | ✅ 状态「部分执行」、计划5/已执行2/剩余3。L1：`部分执行` 系列 5 条 |
| F-16 | 工单冲销 | 对冲销一张已执行工单 | 库存按原方向回退；写冲销记录与冲销流水；状态→已取消 | ✅ 库存回退 +2、有冲销记录、状态「已取消」、写冲销流水。L1：`冲销工单【核心】出库工单冲销后库存回到执行前` |
| F-17 | 工单取消 | 取消一张未执行工单 | 状态→已取消；**不动库存** | ✅ 状态「已取消」、库存未变。L1：`取消工单` 系列 4 条 |
| F-18 | 盘点（盘盈/盘亏/账实相符） | 对测试物料分别录 实际>账面、<账面、=账面 | 差异写流水；账实相符时变动 0；库存被设为实盘值 | ✅ 盘盈 `#30 d+3→12`、盘亏 `#31 d−3→9`、账实相符 delta=0 仍留痕。L1：盘点系列 6 条 |
| F-19 | 手工调整库存 | 改数量并填原因 | 库存变目标值；写「手工调整」流水；原因入库 | ✅ `#33 手工调整 d+5→14`、原因字段入库。L1：`统一写入口` / `库存改成负数默认被拒绝` |
| F-20 | 负库存拦截 | 让库存减到负数 | 按业务规则拦截或标记 `row-neg` | ✅ **出库被拦截**（「库存不足（现 0，需 1）」）；手工调整成负数默认被拒（L1 覆盖）。渲染层负库存标红 `row-neg` |

---

## 四、阶段三：CRUD（8 张表 + 飞书同步）

每张表测四件事：**增（新建→飞书出现）→ 改（改字段→飞书更新）→ 查（刷新后仍在）→ 删（删→飞书消失）**。
方法：网页端操作 + `GET /api/feishu/state` 核对 + 飞书表结构核对。

**执行方式**：`node scripts/feishu-crud-check.mjs --site https://mes.newenergycoder.club`
（这个脚本就是为这件事写的：用 `ZZT<时间戳>` 哨兵记录跑真实往返，含异常路径必定清理）

**结果：8 张表 × 增/查/改/改后查/删/删后查/无残留 = 56 项断言全部通过**（退出码 0）。

| ID | 表 | 增 | 查 | 改 | 改后查 | 删 | 删后查 | 无残留 |
|---|---|---|---|---|---|---|---|---|
| C-01~04 | 物料台账 materials | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| C-05~08 | 库位 locations | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| C-09~12 | 容器 containers | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| C-13~16 | 人员 members | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| C-17~20 | 物品 items | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| C-21~24 | 手册 manuals | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| C-25~28 | 工单记录 workorders | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| C-29~32 | 库存流水 transactions | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |

顺带确认（脚本的只读核对项）：**飞书表结构缺列 0、缺单选选项 0**。

L1 层另有 `8 表 CRUD【核心】增删改查在每一张表上都能往返`（`test/feishu-api.test.js:986`）在 mock 上独立覆盖同一套动作。

**CRUD 附加不变量：**

| ID | 用例 | 预期 | 结果 |
|---|---|---|---|
| C-33 | 物料 upsert **绝不能带绝对库存** | 推送物料后飞书「库存数量」不变（P4 闸门） | ✅ 已确认：`upsert` 服务端对**已存在**物料的「库存数量」一律忽略并回报 `dropped`（L1：`upsert【P4 兜底】物料表的「库存数量」一律不写`）；新建时允许带期初库存 |
| C-34 | 业务键缺失必须被拦 | 导入缺业务键列的文件被拒且**一个字都不改** | ✅ 阶段五 E-10/C-34 合并验证：改名业务键列的文件被拒在**确认框之前**，物料/库位/流水一条没变（P5 实测） |
| C-35 | 空业务键的行不得入库 | 导入含空编码行 → 跳过并告警，不产生幽灵记录 | ✅ 导入对 9 张表逐张过滤空业务键并计入导入告警（P5 实现 + 8 条静态不变量覆盖） |
| C-36 | 推送幂等 | 同一 opId 重复提交只记一次账 | ✅ L1：`阶段0·幂等【核心】同一操作ID 并发提交 3 次，只记一次账`；本地端到端也验证过（P8 本地模式测试断言「操作ID 落库」） |

---

## 五、阶段四：双向同步与身份/删除闸门

| ID | 用例 | 方法 | 预期 | 结果 |
|---|---|---|---|---|
| S-01 | 飞书改→网页端自动出现 | 在飞书侧改一个字段（`GJ-SD-001` 的模块区），记下写入时刻，页面轮询可见 | ≤10 秒量级（轮询档默认 10s） | ✅ **精确实测 3.0 秒**（在页面内直接打时间戳：写入完成 → 本地可见 = 3003ms；写入本身 3157ms）。轮询档 10s，所以理论上界就是 10s，这次正好落在下次轮询前 3 秒。期间 0 冲突、账本 complete-valid。早先那次 16.1s 是含工具往返的**上界**，已作废。<br>L1：`增量合并` 系列 |
| S-02 | 网页端改→飞书收到 | 网页端改一条，核对飞书 | 飞书同步更新，且**不含**绝对库存 | ✅ 阶段二每个流程都逐条比对过两边（流水/库存/工单状态完全一致）；「不含绝对库存」由 `fsPush` 入队前剥除 + 服务端忽略更新型 qty 双重保证（L1：`upsert【P4 兜底】物料表的「库存数量」一律不写`） |
| S-03 | 删除闸门①分页不完整不判删 | 构造截断响应 | 一条都不删，转「待人工核删」 | ✅ L1：`增量·删除【闸门1】census 不完整时删除集必须为空`、`pullState【P3 端到端】截断的半截数据 + 关闭删除闸门 → 本地一条都不能少` |
| S-04 | 删除闸门②阈值 | 构造大批缺失 | 超阈值转人工 | ✅ L1：`删除【闸门2】删 3 条自动应用，删 200 条转人工`、`阈值是「超 5% 或超 50 条」`、`阈值必须恒 ≤ 50` |
| S-05 | 删除闸门③连续两次完整对账 | 正常对账 | 只有连续两次完整才自动确认 | ✅ L1：`删除【闸门3】主数据必须连续两次完整 census 都缺才自动确认`、`只认交集：两次缺不同的键不算连续确认`、`删除【闸门4】流水表不参与删除对账`、`删除【闸门5】已执行/已取消工单必须人工裁决` |
| S-06 | 已确认删除 vs 本地新建 | 飞书删一条 + 本地新建一条 | 前者进待核删、**不自动删**；后者进待推送 | ✅ L1：`合并【显式删除】allowDelete:true 时才把「上次同步有、这次没有」的删掉`、`合并【P3 默认安全】不显式允许时绝不删，且把它记进 pendingDelete`、`合并：首次同步（没有 syncedKeys）不删任何东西，全部记为待推送`；阶段四实测也验证过（喂「少一条」的远端快照 → 删 0 条、转待核删、基线键保住） |
| S-07 | 冲突裁决：保留本地 | 构造真双改，选保留本地 | qty 走 **delta + 库存直写**；其它字段以本地为准 | ✅ L1：`applyMerge【P4 核心】qty「保留本地」必须写成 远端 + (本地 − 基线)，并产出库存直写`；L2（P4 实测）：冲突(基线100/本地102/飞书103) 裁决后本地 105，队列里是 **delta=2** 而非绝对值 |
| S-08 | 冲突裁决：采用飞书 | 同上选采用飞书 | 本地值跟随飞书，不写回 | ✅ L1：`applyMerge【P4】qty「采用飞书」不产出库存直写`；L2 预览显示结果为 103 且无库存直写 |
| S-09 | 冲突裁决预览 | 点「预览」 | 零写入，给出两个选择各自的结果 + 服务端 dryRun 反馈 | ✅ L2（P4 实测）：预览前后**流水数/库存/队列三项全未变**；两个选择各算出 103 / 105，服务端回报「将记一条流水（变动 +2，余量 102，预分配 #22）」。L1：`upsert dryRun：只回报会做什么，不落任何数据` |
| S-10 | 冲突回滚 | 裁决后回滚 | 数据**和 `__base` 基线**一起回退 | ✅ L1：`applyMerge【P4-4 核心】回滚必须连 __base 一起回退`、`原本没有 __base 时回滚不能凭空留一个`、`applyMerge【核心】应用后可完全回滚（deep-equal）`；L2（P4 实测）：裁决后 105 → 回滚 100，期间人为把基线推进到 9999，回滚后基线**回到裁决前** |
| S-11 | 冲突池刷新存活 | 造冲突后刷新 | 冲突仍在且**面板自动弹出** | ✅ L2（P3 实测）：刷新后从 IDB 恢复 1 条、面板自动显示、两个裁决按钮在；清空后 store 记录也删掉 |
| S-12 | 「来历不明」不变量 | 造一条飞书没确认过、队列里也没有的记录 | 被点出来并报警；无基线时不误报 | ✅ L2（P5 实测）：正常 366 已确认 / 0 未决 / **0 来历不明**；插一条幽灵记录 → 被点出且面板报警并给推送入口；临时清空 `__syncedKeys` → `skip:true`「本机还没有与飞书对齐过，暂不判定」 |
| S-13 | 批量对账模式（写入闸门） | 造 >200 冲突 | 自动进入闸门；写入只入队不提交；「待推送」不被误清 | ✅ L2（P4 实测）：进入闸门后推一条 → 队列 0→1 且条目**仍在队列**（没丢）、qty 已剥；`pushPending` 返回 **0**（面板不会被误清）。L1：8 条静态不变量 + 变异测试 |
| S-14 | 同编号多行巡检 | 构造同工单号两行（自动编号不同） | 报警并在面板列出 | ✅ L1：`重复巡检【P7-3】同码但自动编号不同 → 飞书里确实有两行，必须报出来` 等 3 条；L2：线上实测拉取到 `autoNo`（首条值 "1"）、当前无同号多行 |

---

## 六、阶段五：边界与异常

| ID | 用例 | 方法 | 预期 | 结果 |
|---|---|---|---|---|
| E-01 | 离线写入落盘 | 断网后扫码/调整 | 立即进队列（实测 1ms），刷新不丢 | ✅ P5 实测：入队约 **1ms** 且不抛异常；L1 覆盖 `fsEnqueue 全不抛`；本轮多次观察到队列在页面重载后仍保留（刷新不丢） |
| E-02 | 恢复后自动冲刷 | 恢复联网 | 队列自动提交并清空 | ✅ 本轮实测：B1/B2 修复前的积压（16 条）在闸门关闭后**自动从 16→6→0** 收敛，无需人工干预 |
| E-03 | 待人工处理不再无限重试 | 让一条连续失败到 needs_attention | 停止自动重试，角标显示「N 待处理」 | ✅ P5 实测：连续失败后 `tries` 停在 5、状态转 `needs_attention`、**不再自动重试**；`fsFlushQueue` 对它 `held++` 跳过 |
| E-04 | 队列面板可重试/放弃 | 点角标 | 面板列出状态与原因；重试复原；放弃写删除凭据 | ✅ P5 实测：面板列出每条的状态与失败原因；「重试」把 `needs_attention` 复位为 `pending`；「放弃」写入删除凭据（deletionJournal） |
| E-05 | 关页同步落盘 | 触发 pagehide | LS 立即写入最新状态（`__rev` 递增） | ✅ P2 实测：触发 `pagehide` 后 `__rev` 25→26、最新值已落盘；`flushStateOnHide` 同时挂在 `visibilitychange` 上 |
| E-06 | LS/IDB 判新 | 构造 IDB 落后 | 以 LS 为准且**继承 IDB 的基线** | ✅ P2 实测 4 个分支（LS 新/IDB 新/都新/都旧）：LS 胜出时**继承 IDB 的 `__base`**（否则会凭空产生满屏假冲突）；`mes416_idb_behind_v1` 标记只在 IDB 落后时置位 |
| E-07 | 配额告警 | 触发 QuotaExceededError | 弹常驻告警条 + 处置指引 | ✅ P2 实测：触发 `QuotaExceededError` 后弹**常驻告警条**（不是一闪而过的 toast）并给出处置指引；`onLocalStorageFailure` 一处接线覆盖所有 save 路径 |
| E-08 | 日志封顶与去抖 | 大量写日志 | 上限 500 行、同一 tick 只落盘一次 | ✅ L4 静态不变量：`SCAN_LOG_MAX=500`（超出丢弃最旧）、`SCAN_LOG_VIEW=200`（只渲染最近 200 条）、`_logTimer` 去抖（同一 tick 只写一次） |
| E-09 | Excel 导出/导入往返 | 导出→原样导入 | 各表条数与内容一致；PIN 列**无明文** | ⚠️ **通过，但过程中发现 B7（已修）**。修后实测：导出 365 行（逐表与指纹一致）→ 原样导回 → 「导入成功：物料 4 行；恢复库存流水 19 条」；8 张表条数全等（4/188/122/26/2/2/2/19）、`JG-001`/`MB-004`/`B-01-01-01` 逐字段一致、流水 seq 1–19 完整、账本 `complete-valid`、**0 冲突**。PIN 列导出全是「未设置」无明文。<br>两处**无害的规范化**（如实记录）：`cat: ""` → `"QT"`（显示语义相同）、`device: ""` → `"excel"`（设备列在飞书没映射，导出无法区分「无设备」与「未知」）。两者都不参与余额链 |
| E-10 | 导入原子性 | 构造中途失败 | **整体回滚**，数据与导入前一致 | ✅ **通过（并发现 B8，已修）**。用**故障注入**构造真实中途异常（让解析在第 N 张表抛错）：<br>· 修复前：数据侥幸没变（异常发生在改动之前），但提示是「**回滚也失败了…建议立刻用备份 JSON 恢复**」—— 查日志抓到真因 `rollbackImport is not defined`（见 B8）<br>· 修复后：让异常发生在**第 6 张表**（前面几张已整表替换）→ 提示「导入失败，**已完整回滚**，本机数据与导入前一致」，日志「已完整回滚」，**8 张表逐字节一致**，账本 complete-valid<br>拒绝路径（业务键缺失/必需列缺失）仍实测「一个字都没改」 |
| E-11 | 导入不改基线 | 导入后再同步 | 不产生假冲突 | ✅ 本轮实测：Excel 往返（导出→导入）之后继续同步，**冲突数 0**、两边 6/8 表逐字节相同（另 2 表差异已逐字段确认无实质差异），说明基线没有被导入弄坏 |
| E-12 | 模板列结构与导出一致 | 比对两文件表头 | 逐列相同 | ✅ L1：`test/excel-schema.test.js` 4 条断言逐表逐列比对「模板 vs 导出」完全相同；本轮又加了 **B7 那条更强的断言** —— 导入认的业务键必须能在导出表头里找到 |
| E-13 | PIN 码不外泄 | 导出 Excel + 备份 JSON | Excel 无明文；JSON 默认不含 pin 键 | ✅ 本轮实测真实导出文件：人员表 PIN 列**全部是「未设置」**，无 4 位数字明文；P2 已实测备份 JSON 默认不含 `pin` 键（要带需显式确认） |
| E-14 | 人员职务锁定三选项 | 看下拉 | 负责人/成员/本科生 | ✅ L1/L2：`role` 字段为单选、三选项；本轮线上数据 26 人全部落在三选项内（如 `本科生`），无越界值 |
| E-15 | 标签页签分离 | 看库位/模块区两个页签 | 交集为 0 | ✅ 本轮实测：`recordsOf('loc')`=183、`recordsOf('zone')`=5，合计 188 = 库位总数；模块区不再混进库位页签（P6 修复点 `kind==='模块区'` 排除） |
| E-16 | 区卡打印守卫 | 标签纸模式下选模块区卡打印 | 被拦下并自动切 A4 | ✅ P6 实测：区卡在标签纸模式下被拦下、自动切到 A4，并给出说明（区卡尺寸与标签纸不匹配） |
| E-17 | 编码生成不撞号 | 生成库位/容器/物品/手册/工单 | 编号唯一且连续；跨设备不撞号 | ✅ **全部通过**（线上实测，测完已清理）：<br>· **故意用重叠区间**生成容器 `A4SH-001..003`（已存在）→ 「新增 0 个容器码（重复自动跳过）」<br>· 全新区间 `A4SH-900/901` → 新增 2，码唯一<br>· 工位 `W90-G01..G03` → 新增 3，码唯一<br>· **物料自动编码**：两行**同分类**的空白码 → 得到 `GZ-001` / `GZ-002`（互不相同，说明批内计数器是递增的）<br>· 全局唯一性：6 张表共 **327 个编码，0 重复**<br>机制：区间生成走 `addUnique`（按业务键去重，重复自动跳过）；单条生成走 `nextCode`（取同前缀最大号 +1）；批量自动编码用 `next` 缓存递增 |
| E-18 | NEC 编码空间独立 | 建 NEC 单 | 带 `NEC-` 前缀，与仓库工单不可能同号 | ✅ P6 实测：`NEC_CODE_PREFIX='NEC-'`，新建 NEC 单必带该前缀；本轮 F-08 又实测了「同号歧义保护」——即使人为造出同号，扫描裸码也会**停下并要求显式指定** `WIP:`/`NEC:` |
| E-19 | 数据身份一致性 | 每轮结束跑 `replayAudit` | `complete-valid`、不一致 0 | ✅ 全程持续验证：阶段六总回归时账本 `complete-valid`、不一致 0、覆盖度 complete、无缺口，且**每个物料的「库存数量」都等于账本口径（期初+Σ变动）**；身份三态：已确认 8 / 未决 0 / 来历不明 0 |

---

## 七、问题清单（发现的缺陷与修复记录）

> 每条格式：现象 → 根因 → 修复 → 回归验证。按阶段修，修完重新跑受影响的用例。

### 阶段二执行中发现的问题（2026-09-16 第 1 轮）

| # | 严重度 | 现象 | 根因（已定位） | 状态 |
|---|---|---|---|---|
| **B1** | 🔴 高 | 每张工单都会产生最多 5 条**假冲突**（`items`/`execQty`/`execBatches`/`reverseInfo`/`cancelInfo`）；实测一次阶段二就累积 37 条 | `lib/three-way-merge.js` 的 `eq()` 最后一行是 `return a === b` —— 对数组/对象是**引用比较**。两个结构完全相同的数组永远不 `===`，于是 `planMerge` 判定「两边都改过」→ 冲突 | ✅ 已修 `2.17.0` |
| **B2** | 🔴 高 | 每一条本地新建的流水都会假冲突：`ts` 本地 `2026-09-16T13:32:06.724Z` vs 飞书 `2026-09-16T13:32:08.596Z`；`time` 本地 `2026/9/16 21:32:06` vs 飞书 `2026-09-16 13:32:08` | ① 客户端 `fsPushStock` **不上送自己的时间戳**，服务端 `writeStock` 用 `new Date()` 另盖一个 → 差 1–2 秒；② `time` 由同一个飞书列派生，但本地用 `toLocaleString()`、服务端用 `fmtLocal()`，**格式都不同** → 即使同一时刻也不相等 | ✅ 已修 `2.17.0` |
| **B3** | 🔴 高 | 两边的 qty 真的差 1（本机 10 / 飞书 10 / **账本算出应为 9**），账本 8 条链式不一致 | B1+B2 制造 37 条假冲突 → 超过 10% 阈值 → **自动打开批量对账闸门** → 之后所有写入只入队不提交 → 两边真的分叉。**放大器，不是独立 bug** | ✅ 随 B1/B2 消失（闸门已关、两边 qty 已重新一致） |
| **B2b** | 🟡 中 | 阶段二重跑后仍残留 1 条 `workorders.execTime` 假冲突 | 与 B2 同一类：本地 `toLocaleString()` vs 飞书 `DT()`，同一时刻两种格式。B2 的修复只覆盖了 `transactions.ts/time`，漏了 `workorders.execTime` | ✅ 已修：把 `workorders.execTime` 也归入「派生字段，永不问人」 |
| **B8** | 🔴 高 | 导入的**原子回滚从来没生效过**：每次失败都报「回滚也失败了…建议立刻用备份 JSON 恢复」（误报），真出错时会留下半成品状态 | P5-5 把 `takeImportSnapshot` / `rollbackImport` 用 `const` 写在 **`try` 块内**，而调用点在 **`catch` 块**里 —— const 是**块级作用域，catch 里够不着**。实测日志：`⛔ 导入回滚失败：rollbackImport is not defined` | ✅ 已修 `2.20.0`：四个声明整体提到 `try` **之外**（仍在 `rd.onload` 函数体内），两个块都能看见；补静态断言「`const rollbackImport` 的下标必须小于 `try` 的下标」。**故障注入复验**：异常改到第 6 张表（此时前几张已替换）→ 提示改为「已完整回滚」、8 表逐字节一致。前者只是异常恰好发生在改数据之前，数据才侥幸完好 |
| **B7** | 🔴 高 | 应用**自己导出的文件导不回来** —— 导入被整体拒绝：「NEC工单」缺少业务键列「编码」 | 导出与模板写的是「工单号」，而 P5 新增的业务键预检只认「编码」/「编号」。文档明确承诺「导出什么样、导入就什么样」，而在 NEC 表上这条是**断的**：用户按提示「导出→改→导回」会被整份拒绝 | ✅ 已修 `2.19.0`：导入的业务键候选加「工单号」（三个写法都认）。并补**真正的不变量测试** —— KEYCOLS 里每张表认的业务键必须能在导出表头里找到。之前 excel-schema 只比了「模板 vs 导出」（两者都用「工单号」所以一致通过），没检查「导入认不认」，所以这个断层一直没被发现 |
| **B6** | 🔴 高 | 跑 8 表 CRUD 验收脚本时，它删掉了**两条不属于它的流水**（`#20`「P3验收·序号回写」、`#21`「P3验收·冲正」） | `scripts/feishu-crud-check.mjs` 的清理规则 `TEST_TXN_RE = /哨兵\|验收/` 是**子串匹配** —— 那两条流水的 reason 里含「验收」就被当哨兵删了。更危险的是「验收」在仓库场景是常见正常词（「设备验收后入库」「到货验收」），真有人这么写原因就会**静默丢数据** | ✅ 已修：改成锚定精确匹配 `/^\s*哨兵\s*$/`（脚本自己写的就是「哨兵」两字），并对 reason/operator 分别匹配；补静态断言防回归 |
| **B5** | 🟡 中 | 残留 1 条 `materials.qty` 冲突，但两边的 qty **已经完全相同**（都 4） | 冲突是**写入在飞过程中**被记录下来的（本地已 5、飞书还是 4），之后两边自己收敛到 4，但**冲突池里那条还留着**，面板会让人去裁决一个已经不存在差异的问题 | ⚠️ 已定位，未修（修法：渲染/裁决前丢弃 local==remote 的陈旧冲突） |
| **B4** | 🟡 中 | 想修一个「qty 与账本不一致」时发现：**没有任何正规接口能改 qty** | P4 把 upsert 的绝对 qty 关了（这是对的），但因此 qty 只能由服务端按 `期初 + Σ变动` 收敛。而**补一条流水改不了这个差**（qty 和 Σδ 同时加同一个数，差不变）。正确修法只有一条：**先把账本里那些「余量」列改对**，再触发任意一次库存写入让服务端收敛重写 qty —— 这条路径目前只能手工做，没有工具 | ⚠️ 已定位，需要决定要不要加一个「账本修数」工具 |
| N1 | 🟢 低 | `transactions.reason` 冲突：local `盘点差异` / remote `功能验证` | **我的测试传错了参数**：UI 传的是 `r.txn.reason`（"盘点差异"），我传了固定字符串。不是产品缺陷，修正测试即可 | ✅ 已确认非缺陷 |

> **B1 是最值得追的一条**：它不是边界情况 —— 所有工单的 `items` 都是数组，
> 所以只要用到工单，就必然产生假冲突。而假冲突会累积到触发写入闸门，把整个同步停掉。

---

## 八、执行记录

| 轮次 | 日期 | 范围 | 结果 | 备注 |
|---|---|---|---|---|
| 1 | 2026-09-16 | 阶段一（矩阵建立） | ✅ | 本文档建立；基线 2.16.0 / 402 测试 |
| 2 | 2026-09-16 | 阶段二 第1轮（扫码定位 + 存取四类 + 工单流） | ⚠️ 部分通过，发现 B1/B2/B3 | F-01~F-13 逐项验证（见下）；B1/B2 已定位到具体代码行 |
| 2b | 2026-09-16 | 阶段二 第1轮 明细 | — | F-01 环境 ✅ / F-02 扫物料 ✅ / F-03 扫库位 ✅ / F-04 扫容器 ✅ / F-05 扫物品手册 ✅ / F-06 扫NEC不动库存 ✅ / F-09 未知码 ✅ / **F-10 领料 ✅ / F-11 退料 ✅ / F-12 补货 ✅ / F-13 拣货 ✅ / F-14 全量执行 ✅ / F-15 部分执行 ✅ / F-16 冲销 ✅ / F-17 取消 ✅**（F-07/F-08 扫码维度的工单/歧义待重跑）/ F-18~F-20 因闸门开启未完整观察 |
| 3 | 2026-09-16 | B1/B2 修复（`2.17.0`） | ✅ 已修 + 408 测试 + 变异测试 | 新增 B1 四条、B2 两条断言；注入旧 `eq()` 后「结构相同的数组不得判为冲突」立刻失败 |
| 4 | 2026-09-16 | **阶段二 重跑（`2.18.0`）** | ✅ **通过** | 修前同样操作约 6 条假冲突 → 修后**执行全程 0 条**；两边流水/库存/工单状态完全一致；账本 complete-valid。拉取回来后残留 2 条，追根因得到 B2b（已修）与 B5（已修） |
| 5 | 2026-09-16 | 测试数据清理 | ✅ | 删掉 19 条测试流水（连续尾部 seq22-40，不留缺口）+ 8 张测试工单 + 2 条测试物料。生产回到测试前状态：**物料4/库位188/容器122/人员26/物品2/手册2/工单2/流水21**，账本 `complete-valid`、0 残留 |
| 6 | 2026-09-16 | **阶段二 全部 20 项** | ✅ **全部通过** | F-01~F-20 见上表。二维码扫码维度 7 项浏览器实测；业务逻辑 11 项 L1 有对应测试（测试名已附）+ 端到端已实测 |
| 7 | 2026-09-16 | 阶段三 CRUD | ✅ **56 项断言全通过** | 8 表 × 增/查/改/改后查/删/删后查/无残留；飞书缺列 0、缺选项 0；退出码 0 |
| 8 | 2026-09-16 | B6 修复与复验 | ✅ | 修掉验收脚本里「用『验收』做子串匹配」的误删；重跑 CRUD 仍 56/56 通过且**不再误删流水**；测试 410 → 411 |
| 9 | 2026-09-16 | **阶段四 全部 14 项** | ✅ **全部通过** | S-01 线上实测（飞书改→页面 ≤16.1s，0 冲突）；S-02 由阶段二逐流程比对 + P4 闸门覆盖；S-03~S-14 由 L1 专项测试（测试名已附）+ 各阶段 L2 实测共同覆盖 |
| 17 | 2026-09-16 | **E-10 中途异常回滚** | ✅ 通过（发现并修复 B8） | 故障注入：修复前报「回滚也失败」实为 `rollbackImport is not defined`；修复后异常改到第 6 张表仍逐字节一致、提示「已完整回滚」 |
| 18 | 2026-09-16 | **阶段六 总回归** | ✅ **全部通过** | 四层验证全绿，见下方「总验收结论」 |
| 19 | 2026-09-16 | 客户端侧只读核对 | ✅ 干净 | 队列 0 / 待人工 0 / 冲突 0 / 闸门关 / 账本 complete-valid / 已确认 8 / 未决 0 / 来历不明 ok / `__rev`=7 / 无 IDB-behind 标记 |

---

## 九、覆盖率审计（2026-09-16）—— 本文档**没有**覆盖全部业务逻辑

用户问「确定测试文档涵盖到全部业务逻辑了吗」。我不凭印象回答，而是**从代码里把交互点枚举出来逐条对账**：

`index.html` 里绑定了 `click/change/input` 的交互点共 **69 个**。逐条对账结果：

**已覆盖（52 个）**：编码生成 7 个、导入导出模板 3 个、审计回放与修数 3 个、盘点/执行/工单增项 3 个、
删除与新增（物料/人员/记录）6 个、云同步 1 个、队列与闸门 5 个、冲突裁决 4 个、
NEC 与标签页签若干、搜索过滤 4 个（间接）、其余为面板开关。

**未覆盖（17 处真实业务逻辑）** —— 这是本文档的真实缺口：

| # | 未覆盖的业务逻辑 | 交互点 | 风险 |
|---|---|---|---|
| 1 | **JSON 备份 / 恢复 / 合并** | `btnBackupJson` `btnMergeJson` `fileMergechange` | 🔴 数据安全核心功能，完全没测 |
| 2 | **NEC 工单的增删改查** | `btnNecCreate` `btnNecDel` `btnNecExport` | 🟡 阶段三只覆盖飞书 8 表，NEC 是本地单 |
| 3 | **工单列表操作**（删除/冲销/取消/加项/刷新） | `btnWipDelete` `btnWipReverse` `btnWipCancel` `btnWipAddItem` `btnWipRefresh` | 🟡 只测了执行与整体流程，没逐按钮测 |
| 4 | **定位线索**（找位置 / 定位审计） | `btnLocateFind` `btnLocateAudit` | 🟡 只在 F-02 见过「定位来源」文案 |
| 5 | **审计日志导出** | `btnAuditExport` | 🟢 |
| 6 | **健康面板 / 探测** | `btnHealthProbe` `btnHealthRefresh` `btnHealthHide` | 🟡 |
| 7 | **批量选择** | `btnSelAll` `btnSelNone` `matCheckAll` `memberCheckAll` `necCheckAll` | 🟡 批量删除的前置 |
| 8 | **标签打印的实际输出** | `btnPrint` `selPaper` `selInk` `numFontScale` `dsType` | 🟡 只测了页签分离与区卡守卫，没验打印结果 |
| 9 | **操作人切换** | `selOperator` | 🟡 影响流水「操作人」列 |
| 10 | **删除凭据面板** | `btnDelJournal` | 🟡 删除可追溯性的入口 |
| 11 | **手册 / 图纸查看与二维码** | `btnDsOpen` `btnDsCopy` `btnDsQr` `btnGalMore` `galAlbumchange` | 🟢 |
| 12 | **扫码相机** | `btnCam` `btnCamClose` | 🟢 需真实摄像头 |
| 13 | **InvenTree 连通测试** | `btnInvTest` | 🟢 |
| 14 | **闲鱼同步**（脚本侧） | `xianyu-sync.mjs` | 🟡 P6 有单测，但**没进本文档矩阵** |
| 15 | **InvenTree 同步**（脚本侧） | `inventree-sync.mjs` | 🔴 P6 有单测，但**从未在真机跑过** |
| 16 | **出厂示例数据 / 清空数据** | `btnSeed` `btnWipe` | ⚠️ **危险操作，刻意不在生产测** —— 应在独立环境做 |
| 17 | **工单的部分冲销** | （无独立按钮，走冲销流程） | 🟡 只测了整单冲销 |

**诚实结论**：本文档覆盖的是**核心业务链**（存取四类 / 工单全量·部分执行 / 冲销 / 取消 / 盘点 / 手工调整 /
8 表 CRUD / 双向同步与四条删除闸门 / 导入导出真实往返 / 编码生成），这部分的证据是充分的。
但**不能说涵盖全部业务逻辑** —— 上表 17 处没进矩阵，其中 #1（JSON 备份恢复）和 #15（InvenTree 真机同步）风险最高。
阶段七就是补这些。

---

## 十一、阶段七：覆盖率补测（进行中）

### #1 JSON 备份 / 恢复 / 合并 —— ✅ 全部通过（2026-09-16，`2.21.0`）

这是覆盖率审计里风险最高的一项（数据安全核心，此前完全没测）。实测方式：真下载备份文件在本地解析 + 真上传合并。

**① 备份文件结构（两种变体都测）**

| 验的是什么 | 结果 |
|---|---|
| 剔除内部簿记字段 | ✅ `state` 里 **`__` 开头的键 0 个**（`__base`/`__syncedKeys`/`__watermarks` 等全部剔除，体积 43.8KB 而不是翻倍） |
| 顶层结构 | ✅ `app=416MES`、`version=2`、`deviceId`、`exportedAt`、`pinsIncluded`、`state` |
| 表齐全 | ✅ 物料4/库位188/容器122/人员26/物品2/手册2/工单2/NEC0/流水19/操作人1 |
| **不含 PIN 时删的是「键」不是写空串** | ✅ 26 人**有 `pin` 键的 0 条、空串 0 条** —— 这条是关键：空串会被 `Object.assign` 理解成「明确要求清空 PIN」，恢复+推回飞书就把全员 PIN 抹了 |
| 含 PIN 时 | ✅ `pinsIncluded=true`、26/26 人都有 `pin` 键、`MB-004` 的 PIN 原样保留 |
| 文件名标注 | ✅ 含 PIN 的文件名带「含PIN」，不带的没有 |

**② 合并往返**

| 验的是什么 | 结果 |
|---|---|
| **无 PIN 备份合并后，本机 PIN 不被抹掉** | ✅ 先把 `MB-004` 的 PIN 设成 `4321`，再合并**不含 PIN** 的备份 → PIN **仍是 `4321`**（这是「删键而不是写空串」那个设计的核心价值） |
| 幂等 | ✅ 合并同一份备份：新增物料 0、库位 0、容器 0、物品 0、手册 0、人员 0、工单 0、NEC 0、流水 0 条 |
| 流水号取最大 | ✅ `txnSeq` 保持 19，未被备份里的值重置 |
| 流水并集去重 | ✅ seq 无重复 |
| **不擅自写飞书** | ✅ 合并后**显式询问**「是否把合并后的数据一并推送到飞书？」；拒绝后队列仍为 0（一条都没写） |
| 数据完好 | ✅ 计数不变 4/188/122/26/2/2/2/19，账本 `complete-valid`、0 不一致、0 冲突 |

> 这一项**没有发现 bug** —— 设计（尤其是「PIN 默认不进备份 + 删键而非空串」）经得起检验。

### #15 InvenTree 真机同步 —— ⬜ 需你提供环境

`inventree-sync.mjs` 的翻页/超时/数量校验/库位路由在 P6 有 7 条单测 + 用本地 HTTP 桩验过，
但**从未对真实 InvenTree 服务端跑过**。要补这一项，我需要你给一个可访问的地址与 API Key
（或者你确认这台机器能连到哪个 InvenTree）。**在此之前这项只能标记为「未验证」。**

### 批 A（面板 / 选择 / 导出类 6 项）—— ✅ 全部通过（2026-09-16，`2.21.0`）

| # | 用例 | 实测结果 |
|---|---|---|
| **#4** | 定位线索（`btnLocateAudit` / `btnLocateFind`） | ✅ 面板弹出并出报告：「物料总数 4，随机抽查 4 件（**seed 20260915，同 seed 可复现**），本次查询耗时 **1.1ms**，精确率 **75%**」。75% 是数据本身的必然结果（`JG-001` 台账没登记库位/容器/模块区 → 只能给「历史线索」级），不是缺陷 |
| **#5** | 审计日志导出（`btnAuditExport`） | ✅ `count=19` 与实际 `txns` 条数一致；每条都含 `seq/matCode/delta/balance`；**不含任何内部簿记字段**。<br>📝 **观察项（非缺陷）**：导出按 **seq 升序**（1→19），而应用内 state 与全站约定是降序。对「给人核对的审计凭证」来说升序更好读，所以判定为合理设计，仅记录 |
| **#6** | 健康面板（`btnHealthRefresh` / `btnHealthHide` / `btnHealthProbe`） | ✅ 刷新后内容完整：「同步健康：首屏 5.80s 偏慢 · 探测 1.20s 偏慢 · 键对账 2.46s；覆盖度：账本完整且一致（流水 19 条，比对 19 条）；三方合并可用；待裁决冲突 0 条；待人工删除 0 条」。隐藏后 `display:none` ✅<br>📝 面板如实标出「首屏 5.80s / 探测 1.20s **偏慢**」——这是它该做的事（自我诊断） |
| **#7** | 批量选择（`btnSelAll` / `btnSelNone`） | ✅ 在库位页签上：可见 **183** 条 → 全选后 **183** → 全不选后 **0**。这是批量删除/批量打印的前置 |
| **#9** | 操作人切换（`selOperator`） | ✅ 下拉从操作人 + 人员名单生成；切换后 `state.operator` 立即生效（后续流水会用新操作人）。测完已还原为「管理员」 |
| **#10** | 删除凭据面板（`btnDelJournal`） | ✅ 无凭据时如实提示「还没有任何删除凭据。」（不编造空面板） |

### 批 B-1（#14 闲鱼同步，脚本侧）—— ✅ 通过（2026-09-16，`2.21.0`）

用样例文件在本地跑真实合并（不依赖网络），覆盖 JSON / CSV（含引号内逗号）两条输入路径：

| 验的是什么 | 结果 |
|---|---|
| **受保护字段不被外部覆盖** | ✅ 分类 `cat` 保持 `JG`（外部给的 `SHOULD_NOT_OVERRIDE` 被忽略）、规格保持「本地规格」、库位保持 `B-01-01-01`、安全库存保持 `5` |
| 数量与成本「始终采用外部值」 | ✅ `qty 1→7`、`cost 9→19.99`（售价 1999 ÷ `priceDivisor` 100） |
| 闲鱼编号 / 图片链接 | ✅ `xy OLD→8801`、`img` 空→有值 |
| 新建记录 | ✅ `XY-MAT-2` 分类默认 `QT`、`stock=0` → `qty=0`（文档写的「stock=0 表示售罄」） |
| **幂等** | ✅ 同一份再跑一次：新建 0 · 更新 0 · **无变化 2** |
| CSV 路径 | ✅ 逗号分隔 + 带引号的标题（`"带,逗号的标题"`）正确解析成 1 个字段 |

📝 **行为提醒（按设计，但值得你知道）**：受保护的只有**分类/库位/容器/模块区/安全库存/规格**；
而**名称 / 闲鱼编号 / 图片链接**的规则是「外部值非空就采用」——
也就是说**你手动改过的商品名称会被闲鱼侧覆盖**。实测：本地「本地名」被换成「闲鱼测试件A」。
这与脚本文档一致，不是 bug；如果你希望名称也受保护，说一声我改。

### 批 B-2（#2 NEC CRUD / #17 部分冲销）—— ✅ 通过（2026-09-16，`2.22.0`）

> 上一轮这三项因**本机网络中断**（`curl` 返回 `000`、浏览器 `Page.navigate` 连续超时）未执行，
> 已明确记录为阻塞而**没有伪造结果**。本轮网络恢复后立即补测。

**#2 NEC 工单 CRUD**

| 验的是什么 | 结果 |
|---|---|
| 建单（`btnNecCreate`） | ✅ 码为 `NEC-RW20260916001` —— **带 `NEC-` 前缀**，与仓库工单编码空间分离 |
| 与仓库工单撞号？ | ✅ **不撞**（`state.workorders` 里没有同号）——这正是当初加前缀要防的事：两边都是 `<类型><日期><3位>`，同一天同类型会生成完全相同的号 |
| 扫码 | ✅ `NEC:` 能命中该单，且**不出现执行入口**（不会误扣库存） |
| 删除（`btnNecDel`） | ✅ 勾选后删除，条数回到建单前、无残留，且 **`sel.nec` 被清空**（下标整体失效，不清会打错标签） |

**#17 工单部分冲销**

| 验的是什么 | 结果 |
|---|---|
| 部分执行 | ✅ 计划 3 件、执行 1 件 → 状态「部分执行」、已执行 1、剩余 2、库存 2→1 |
| **冲销只回退已执行的数量** | ✅ 库存 1→**2**（回退 **1** 件，而不是回退计划的 3 件）、写入 `d1` 冲销流水、有冲销记录、状态→已取消 |
| 重复冲销 | ✅ 被拒：「工单 LL20990115902 已取消，无需冲销」 |
| 账本 | ✅ `complete-valid` |

**#3 工单列表按钮** —— ✅ 通过

| 验的是什么 | 结果 |
|---|---|
| 详情页按钮按状态显示 | ✅ **未执行**的工单只显示「取消」「删除」，**不显示「执行」「冲销」**（没执行过就无从冲销） |
| `btnWipCancel`（未执行单） | ✅ 状态→已取消、写 `cancelInfo`、**库存一条没动** |
| `btnWipDelete`（已取消/无库存变动） | ✅ 允许删除并从列表移除；源码守卫「**已执行且未取消**时拒绝并提示请先冲销」与 #17 的流程互为印证 |
| `btnWipRefresh` | ✅ 点击无异常、工单数不变（只重绘不改数据） |

**#11 手册 / 图纸查看** —— ✅ 通过（只读部分）
- ✅ 扫 `MAN:SC-001` 命中并显示完整详情（手册码/名称/版本/库位及其层级）
- ✅ 看板四个入口 `btnDsOpen` / `btnDsCopy` / `btnDsQr` / `btnGalMore` 均在
- ⬜ 二维码图像本身与相册翻页未做视觉核验（属 UI 层，留待 UI 阶段）

### 🔧 本批顺带修掉 B5 的一个缺口（`2.22.0`）

补测时删掉一条测试物料后，**关于它 `qty` 的冲突一直挂在面板上** —— 让人去裁决一个**已经不存在的记录**。
B5 原本只在「记录还在且当前值与飞书一致」时丢弃，没有覆盖「记录已被删掉」的情况。
已补：`findRec` 取不到记录时同样丢弃（并记日志）。测试 419 → **420**。

### #8 标签打印输出 —— ⚠️ **静态核对通过**（未实测出纸）

本机没有打印机，**真实出纸效果无法验证**，我不拿静态结论冒充实测。已核对的静态事实：

| 核对项 | 结论 |
|---|---|
| 两种纸型 | ✅ 标签纸 `@page{size:60mm 40mm;margin:0}`；A4 整版 `@page{size:A4 portrait;margin:7mm}`，打印时动态改写 |
| **区卡只能在 A4 下打印** | ✅ CSS 层硬守卫：`body:not(.a4paper) #printSheet .zonecard{display:none}`（不是靠 JS 判断，改错模式也印不出来） |
| 选到区卡自动切 A4 | ✅ 有对应逻辑（注释写明「选到 zone 时自动切 A4」） |
| **纸型与 body class 是绑定的** | ✅ 注释明确：两者只恢复一个会得到**空白打印** —— 这是个真实踩过的坑，已在代码里写明 |
| 库位与模块区尺寸混打 | ✅ 已在源头上避免：库位页签**排除模块区**（两套尺寸 60×40 vs A4 很容易打错） |

**仍缺你一句话**：验收标准是什么？（例如「A4 区卡必须精确 210×297mm」「条码必须能被扫码枪扫出」）
给了标准我就能用浏览器量尺寸、验条码内容；**真实打印机出纸效果我验不了**，那需要你那边印一张。

### #16 出厂数据 / 清空数据 —— ⚠️ **静态核对通过**（危险操作，未在生产执行）

这两颗按钮会清掉数据，**我拒绝在生产库上实跑**。已核对的静态事实：

| 核对项 | 结论 |
|---|---|
| `btnSeed` 重置为出厂种子 | ✅ 有 `confirm` 二次确认，提示语明确要求「建议先导出备份」 |
| `btnWipe` 清空本机数据 | ✅ 有 `confirm`，且提示语写得很诚实：「**不可恢复！**（飞书侧不会被删除，但**下次同步会重新拉回**）」 |
| **是否只动本地、不删飞书** | ✅ 两者都只调 `fsResetLocalData(...)`，**没有任何飞书删除调用** —— 这是关键安全属性；而且提示语如实告知「清空后会被同步拉回」，不让用户误以为能借此清空飞书 |
| 清空范围 | ✅ 显式列出 8 张表 + `serials`/`necSerials`/`scanHistory`/`txnSeq`/`operators`/`scanLog`，并保留 `operator='管理员'` |

**要实测的话**：给我一个独立环境（或你确认可以在某个非生产 URL 上试），我再做「清空后确实为空 + 同步能拉回」的实测。
在此之前这一项**只能标静态核对**。

### #12 扫码相机 —— ◐ **降级路径通过**，真实摄像头未验

| 验的是什么 | 结果 |
|---|---|
| 本环境摄像头数 | ✅ `enumerateDevices()` 返回 **0 个**（确认确实是无摄像头环境，不是权限问题） |
| 无摄像头时点击 | ✅ 明确提示「**无法打开摄像头：Requested device not found**」——带底层原因、不静默、不卡死 |
| 失败后页面可用性 | ✅ 页面仍可用，没有把界面卡住 |
| ⬜ | 真实摄像头出码 → 需要设备 |

### #13 InvenTree 连通测试 —— ◐ **失败路径通过**，成功路径需服务器

结果写在 `#invStatus` 元素里（不是 alert —— 我第一次找错了地方，是**测试方法的问题**）：

| 验的是什么 | 结果 |
|---|---|
| 未配置/不可达时 | ✅ 显示「❌ Failed to fetch」，如实反映失败，**不假装成功** |
| 失败后按钮状态 | ✅ 按钮文本复位为「测试连接」、未卡在禁用态 |
| ⬜ | 「✅ 服务器 版本」的成功路径 → **需地址 + Key** |

### #15 InvenTree 真机同步 —— ⛔ 明确阻塞

`inventree-sync.mjs` 的翻页/超时/数量校验/库位路由在 P6 有 7 条单测 + 本地 HTTP 桩验过，
但**从未对真实 InvenTree 服务端跑过**。补测这一项必须有：
1. 一个可访问的 InvenTree 地址
2. 对应的 API Token（或确认该实例允许匿名只读）

**在没有这两样之前，我不会用桩测的结果冒充真机验证** —— 这项保持「未验证」。

---

## 阶段七 收束（2026-09-16）

| 类别 | 项 |
|---|---|
| ✅ **实测通过（14 项）** | #1 备份/恢复/合并 · #2 NEC CRUD · #3 工单列表按钮 · #4 定位线索 · #5 审计导出 · #6 健康面板 · #7 批量选择 · #9 操作人切换 · #10 删除凭据 · #11 手册/图纸（只读）· #12 相机降级路径 · #13 InvenTree 失败路径 · #14 闲鱼同步 · #17 部分冲销 |
| ⚠️ **静态核对通过（2 项）** | #8 标签打印（纸型/CSS 守卫/绑定关系已核对；**真实出纸验不了**）· #16 出厂数据/清空（二次确认 + **只动本地不删飞书**已核对；**危险操作拒绝在生产跑**） |
| ⛔ **明确阻塞（1 项）** | #15 InvenTree 真机同步 —— **需地址 + API Token** |
| 部分未验的支路 | #8 真实出纸（需打印机 + 你的验收标准）· #12 真实摄像头 · #13 成功路径 |

**结论**：阶段七能做的都做完了。剩余的不是「没做」，而是**缺外部条件**（InvenTree 实例、打印机、摄像头、独立环境）。
本阶段共发现并修复 **1 个缺口**（B5：冲突指向的记录不存在时未丢弃 → `2.22.0`），
其余 16 项**没有发现缺陷** —— 这套业务逻辑经得起逐项检验。

| # | 未覆盖 | 计划 |
|---|---|---|
| #2 | NEC 工单 CRUD | 线上建/改/删一张测试 NEC 单再清理 |
| #3 | 工单列表各按钮（删除/冲销/取消/加项/刷新） | 线上逐按钮测 + 清理 |
| #8 | 标签打印实际输出 | 需确认打印预览可断言的部分（尺寸/条码内容） |
| #11 | 手册/图纸查看与二维码 | 只读，容易 |
| #12 | 扫码相机 | 需真实摄像头，可能只能验降级路径 |
| #13 | InvenTree 连通测试（`btnInvTest`） | 需可访问的 InvenTree |
| #14 | 闲鱼同步（脚本侧） | 只读脚本，用样例文件测解析与合并 |
| #15 | InvenTree 真机同步 | **需你提供地址 + API Key** |
| #16 | 出厂数据 / 清空 | **建议不在生产测**（会清空数据） |
| #17 | 工单部分冲销 | 线上测 + 清理 |

---

## 十、总验收结论（2026-09-16，基线 `2.21.0`，**范围限定为「核心业务链」**）

**四层验证全部通过：**

| 层 | 手段 | 结果 |
|---|---|---|
| L1 自动化 | `npm test` | **413 / 413 通过**（0 失败 0 取消 0 跳过） |
| L2 浏览器实测 | 线上页面 + 真实飞书 | 阶段二扫码 7 项 + 业务流 11 项、阶段四同步/闸门、阶段五导入导出与编码生成 —— 全部通过 |
| L3 数据核对 | `scripts/feishu-crud-check.mjs` 跑线上 | **8 张表 × 增/查/改/改后查/删/删后查/无残留 = 56 项断言全过**；飞书缺列 0、缺单选选项 0 |
| L4 不变量 | 账本自校验 + 逐物料口径核对 | 账本 `complete-valid`、不一致 0、覆盖度 complete、无缺口；**每个物料的「库存数量」都等于账本口径（期初 + Σ变动）** |

**客户端侧（L2 补充，只读）**：队列 0、待人工 0、冲突 0、写入闸门关、账本 `complete-valid`、
已确认键 8、未决 0、来历不明 ok、`__rev`=7、无 IDB-behind 标记。

**「本地 vs 飞书」逐行核对**：8 张表中 6 张逐字节相同；`materials` 与 `locations` 经逐字段对齐后确认**无实质差异** ——
`materials` 仅 2 行的 `img` 字段（本地 `""`，飞书没这个键；那是我做 Excel 往返时补上的、不对应任何飞书列），
`locations` **字段差异计数为 0**（188/188 行、所有字段相同，只是数组顺序不同）。

**生产数据状态**：物料 4 / 库位 188 / 容器 122 / 人员 26 / 物品 2 / 手册 2 / 工单 2 / 流水 19（seq 1–19），
账本 `complete-valid`、无缺口、**0 测试残留**。

**本目标（完整逻辑功能验证）发现并修复的问题：**

| # | 严重度 | 问题 | 修复版本 |
|---|---|---|---|
| B1 | 🔴 | `eq()` 用 `===` 比较数组/对象 → **每张工单必然产生假冲突** | 2.17.0 |
| B2 | 🔴 | 流水 `ts`/`time` 两边格式必然不同 → **每条本地新建流水假冲突** | 2.17.0 |
| B2b | 🟡 | `workorders.execTime` 同类遗漏（B2 修复时漏了它） | 2.18.0 |
| B5 | 🟡 | 已自行收敛的陈旧冲突仍挂在面板上让人裁决 | 2.18.0 |
| B6 | 🔴 | 验收脚本用「验收」做**子串**认哨兵 → 误删真实流水 | 2.19.0 |
| B7 | 🔴 | **应用自己导出的文件导不回来**（NEC 表业务键与导出表头不一致） | 2.19.0 |
| B8 | 🔴 | **导入的原子回滚从未生效**（声明在 `try` 内、调用在 `catch` 内） | 2.20.0 |

七条全部：定位到具体代码位置 → 修复 → 补断言（含变异测试确认断言有效）→ 线上复发验证。

**B4 已按用户批准实现并验证**（`2.21.0`）：新增「🔧 账本修数」按钮 + `CORE.ledgerRepairPlan()` 纯函数。
线上实测：生产账本已自洽 → 点按钮提示「账本与库存数量已经自洽，无需修数」且**零写入**；
人为注入不一致（余量 +999、qty +42）→ 正确识别出 `#19 1014→15` 与 `JG-001 57→15`。
顺带修掉一条脆断言（P3-1 原来写死调用点个数，改成「每个调用点必须有第 7 个实参」）。测试 413 → **419**。

> ⚠️ **本结论的范围限定**：以上四层验证覆盖的是**核心业务链**。
> 第九节的覆盖率审计列了 **17 处未覆盖**的业务逻辑（其中 JSON 备份恢复、InvenTree 真机同步风险最高）——
> 那些**不在本结论的通过范围内**，属于阶段七的工作。
| 11 | 2026-09-16 | **阶段五 E-09 Excel 真实往返** | ⚠️→✅ | 首次往返被**整体拒绝**→ 发现 B7（已修）→ 重跑成功，逐表逐字段一致、0 冲突 |
| 12 | 2026-09-16 | 阶段五 其余用例 | ✅ 复核登记 | E-01~E-08/E-11~E-16/E-18/E-19 由各阶段已做的 L1+L2 证据覆盖（见用例表）；**E-10 仅部分验证**（见上）、**E-17 未测**（下轮补） |
| 14 | 2026-09-16 | **E-17 编码生成** | ✅ 全部通过 | 重叠区间 0 新增、新区间唯一、工位唯一、同分类两行自动编码得到不同码、全局 327 编码 0 重复；测试数据已清理（生产回到 4/188/122/26/2/2/2/19） |
| 15 | 2026-09-16 | **同步延迟精确化** | ✅ **3.0 秒** | 在页面内直接打时间戳（写入完成→本地可见 3003ms，写入本身 3157ms），轮询档 10s。上一条 16.1s 的「上界」结论作废 |

**⚠️ 本轮发生的一次真实数据误删（已如实记录）**：第 7 条那次运行把流水 `#20`/`#21` 删掉了
（即 B6）。这两条是**我自己在 P3 阶段做序号回写验证时写入的测试流水**（净零 +1/−1，
原《待定.md》里记的就是它们；该文件已按用户要求删除，其未完成事项已并入《待办.md》），所以**业务数据没有损失**：JG-001 的库存仍是 15，
账本仍 `complete-valid`（删的是连续尾部，不留缺口）。
但机制本身是错的 —— 它靠「原因里有没有『验收』两个字」认哨兵，换成任何一句正常原因都可能中招。
已修 + 加断言，并重跑确认不再发生。

**当前遗留（阶段二结束时）：**
- ✅ 测试数据已全部清理，生产数据与账本都回到干净状态（见执行记录第 5 条）。
- ⬜ **B4 未决**：`qty` 与账本不一致时没有正规接口能改 qty（P4 关掉绝对 qty 是对的，
  但因此只能「先把账本余量列改对，再触发一次库存写入让服务端收敛重写」）。要不要加工具，等你定。
- **下一步**：阶段三 CRUD（8 张表 × 增删改查 + 写回飞书 + 回读一致，共 24 项）。

---

## 十二、用户视角整改阶段 0：工单重复与详情白屏闭环（2026-09-17，`2.25.0`）

### 12.1 用户实际发现的问题

- 本地出现 6 条同号 `LL20260917004`，飞书按业务键折叠为 1 行，形成「本地 9 / 飞书 4 / 本地多 5」；
- 删除工单或详情指向已不存在工单后，列表与详情同时隐藏，页面整片空白，只能 F5；
- 对账面板能报数量差，却不能识别「本地同业务键重复行」，也没有安全收敛动作。

### 12.2 修复内容

| 编号 | 修复 | 安全规则 |
|---|---|---|
| B9 | `showWipDetail` 先查存在性再进入详情；删除/同步替换后 `backToWipList()+ctxClear()` | 不存在工单不得留下隐藏列表与幽灵当前对象 |
| B10 | 取号在 `await` 后重读并同步占号；生成按钮加重入闸门；`createOrder` 按工单号查重 | 连点不能产生同号；本地校验失败只回收尚未被后续使用的号 |
| 阶段0收敛 | `workorderDuplicateGroups` 逐行扫描本地原数组；`collapseIdenticalWorkorderDuplicates` 只收敛同号同内容；写 `__localDupJournal`；支持哈希保护撤销 | 只改本地数组，**绝不调用 `fsPushDelete`**；内容不同禁止自动合并；重复未收敛前阻断详情/扫码执行/普通删除 |

### 12.3 自动化与变异验证

- `npm test`：**438 / 438**；
- 去掉按钮提前 `return` → B10 断言失败；
- 去掉 `createOrder` 同号闸门 → B10 断言失败；
- 在本地收敛路径注入 `fsPushDelete` → 安全不变量断言失败；
- 纯函数覆盖：3 条同号同内容 → 1 条；明细/状态/批次不同 → 保留全部并阻断；明细顺序不同但语义相同 → 可收敛；重复执行幂等；回退可恢复；后续编辑后拒绝回退；空编号只报警。

### 12.4 ego-browser 界面验证（本地工作区页面 + 公网飞书兜底）

| 用户路径 | 结果 |
|---|---|
| 构造 3 条完全相同同号工单 | 对齐面板显示 3 行明细与「内容完全相同」；工单列表隔离重复组并提示「已隔离同号重复 3 行」 |
| 点击「保留第一行，删除其余」 | 本地 3→1；写入 1 条回退凭据；面板保持可见；未调用飞书删除 |
| 点击「撤销上次本地工单去重」 | 1→3 精确恢复；随后测试数据清理；账本保持 `complete-valid` |
| 构造同号但数量不同 | 面板显示差异字段 `items`，不出现自动处理按钮；详情被阻断，列表保持可见 |
| 扫描同号冲突工单 | 显示「同号工单冲突 · 已停止执行」，无 `btnExec`，不会扣库存 |
| 打开不存在工单 | 提示已不存在并回列表；列表可见、详情隐藏、CTX 清空、无白屏 |
| 删除正常未执行工单 | 删除后立即回列表、CTX 清空、无白屏（测试数据随后恢复） |
| 无效物料连续点击生成 6 次 | 只触发 1 次校验提示；按钮最终恢复；计数器从 5 回到 5；无新工单、无队列、无重复 |

### 12.5 线上核对

- 8 表 CRUD：**56 项断言全通过**，缺列 0、缺单选选项 0、生产无脚本残留；
- 飞书侧当前同号工单：0 组；
- 账本：`complete-valid`，不一致 0，覆盖度无缺口；
- 当前线上计数会随用户真实操作变化，阶段验收以「无测试残留 + 账本自洽 + 无同号」为准，不再把固定条数当业务不变量。

### 12.6 阶段结论

✅ 阶段 0 通过，可进入阶段 1「记录级同步异常展示与人工收场」。

> 注意：用户截图所在浏览器与测试用 ego profile 的本地状态不同。新版入口上线后，用户原浏览器若仍保留 6 条重复，会直接在「数据对齐」面板看到安全收敛按钮；系统不会自动删除内容不同的记录。

---

## 十三、用户视角整改阶段 1：记录级同步异常展示与人工收场（2026-09-17，`2.26.0`）

### 13.1 修复内容

| 编号 | 问题 | 修复 |
|---|---|---|
| B11 🔴 | 面板只报「本地多 N / 飞书多 N」，服务端早已返回的 `localOnly/remoteOnly` 具体键**被前端丢弃**（grep 0 引用）——用户看得出不一致，却永远不知道是哪几条，也没有任何收场动作 | 逐表展开具体键；每个 localOnly 键给「推回飞书 / 删除本地」；remoteOnly 只读展示（网页不提供删除飞书动作） |
| B11b 🔴 | `_censusPending` 每轮自动对账**无条件清空**（`_censusPending={}`）——自动档一轮只扫一张表，上一张表的人工待办在下一轮凭空消失；「确认删除」按钮约 90 秒后自己不见 | 改为按表合并（新 `lib/census-status.js`，7 条单测）；持久化到 `state.__censusStatus`；不完整/失败保留旧 pending；刷新后恢复 |
| B11c 🔴 | `_lastReport` 只在内存——**刷新页面就丢**，一致性表归零重来 | 持久化到 `state.__lastReport`，启动时恢复 |
| B11d 🟡 | `pendingCount` 可能把**库存流水**算进「待推送」，但 `pushPending` 对非 PUSHABLE 直接 `continue` → 出现一个**永远点不掉的「全部推到飞书」** | 待推送计数只算 `PUSHABLE`；流水差异单独显示「只增审计账本，不参与普通批量推送/自动删除」并引导到回放/修数/导出 |
| B11e 🟡 | `censusTable` 用「非空业务键数」比对 `total`——飞书表里**只要有空业务键行**，`complete` 恒为 false，**该表永远无法做删除对账** | 完整性改按「实际收到的行数」；空键单独返回 `blankKeys` 并在面板报「空业务键 N 行（数据质量问题）」 |
| B11f 🟡 | 对账不完整时只写进另一个页签的日志，面板无解释；REASON 表缺 `full-pull-missing` | 新增「键对账状态」区：每表显示完整性（✅完整 X/Y / ⚠️分页不完整 X/Y）、空键数、待人工数、上次核对时间 |
| 新增 | 已执行/已取消工单直接删本地会破坏审计链 | `isProtectedLocalDelete`：已执行/已取消工单**只允许推回，不允许直接删本地**；流水一律禁删 |
| 新增 | outbox 只能按整项删除，批量项删一条会把整项（含同批其它记录）丢掉 | `removeRecord(table,key)`：同步/异步两个实现，只从批量 upsert 中摘除该键，摘空才删项（2 条单测） |
| 新增 | 删除本地无凭据、状态残留 | `deleteOneLocalRecord`：先写删除凭据（写失败零变更）→ 删单键 → 同步清理 `__syncedKeys`/`_pendingPush`/`_censusPending`/`__censusMissing`/outbox → 保存；**绝不调用 `fsPushDelete`** |

### 13.2 过程中发现并修复的一个自伤

我在把 `_lastReport` 改成 `state.__lastReport` 时，把初始化写在了 `load()` **之前**——而那时 `state` 还是 `null`，
整个主脚本在启动中途停止，**页面完全空白**。`node --check`/静态检查全绿，**只有 ego-browser 打开真实页面才暴露**。
已修复（`restoreSyncUiState()` 在 `load()` 后恢复），并加了防复发断言：脚本加载期不得读取 `state.*`。

> 这正是「每阶段必须做真实界面验证」的价值：静态全绿 ≠ 页面能启动。

### 13.3 自动化验证

- `npm test`：**452 / 452**（新增 12 条：census-status 7、outbox removeRecord 2、对账面板 7 组断言）
- 变异测试：去掉报告持久化 → 断言失败；注入全局清空 `_censusPending={}` → 断言失败
- `censusTable` 空键行为：2 行数据含 1 空键 → `complete=true, blankKeys=1`（旧逻辑会误报分页不完整）

### 13.4 ego-browser 界面验证（本地页面）

| 用户路径 | 结果 |
|---|---|
| 注入「仅本地 ZZT-LOCAL-ONLY / 仅飞书 REMOTE-X」 | 表格逐键显示；localOnly 键带「推回飞书」「删除本地」；remoteOnly 只读且明示「网页不提供删除飞书动作」 |
| 库存流水差异（仅本地 999） | 显示键但**零操作按钮**，明示「只增审计账本，不参与普通批量推送/自动删除」并引导回放/修数/导出 |
| 点击「推回飞书」 | 恰好推送 1 条该记录 + 恰好 1 次 `runReconcile`（stub 计数验证） |
| 注入待人工项后刷新页面 | `FAKE-P1/P2` **刷新后仍在**（`_censusPending` 恢复，B11b 核心验证） |
| 注入假 pending 后等自动对账轮到该表 | 该表被完整扫描且无真实缺失 → 待办被清（**正确行为**：完整对账无缺失即清） |
| 启动顺序（`state=null` 读取） | 修复后页面正常启动，账本 `complete-valid`、队列 0 |
| 「键对账状态」区 | 每表显示「✅ 完整 189/189（时间）」等明细 |

### 13.5 阶段结论

✅ 阶段 1 通过。同步异常从「只报数字」升级为「报记录 + 可安全收场」，且待人工项不再被轮询清空、刷新不丢。
进入阶段 2「全站状态坞」。

---

## 十四、用户视角整改阶段 2：全站状态坞（2026-09-17，`2.27.0`）

### 14.1 修复内容

| 问题 | 修复 |
|---|---|
| 四块全站面板（对齐/队列/冲突/健康）+ 操作日志全部塞在 `#tab-ledger` | 迁入 `</main>` 前的 `#statusDock`（默认折叠 `<details>`），**任何页签都可见** |
| 顶栏队列角标 `#fsQueueChip` 在非台账页点击**无任何反应**（面板在隐藏的 section 里） | `fsOpenQueuePanel` 现在 `dock.open = true`，从任何页签点击都展开 |
| `log()` 全站在用，`#scanLog` 却只在扫码页——台账页操作被静默拒绝时**解释文字在另一个页签** | 日志搬进状态坞，当前页即可见 |
| 异常不可发现 | 新增 `renderStatusDockSum()` 摘要行：队列 / 冲突 / 待人工 / 表不一致 / 重复工单 一行汇总；**出现异常自动展开**（即使之前被折叠） |
| 摘要不随页签刷新 | 切页签回调、`renderAlignBox`、`renderConflictPanel`、`renderHealthPanel`、`fsStatusHint` 均触发摘要刷新 |

**迁移安全性**：id 全部保留（`queueBox`/`cloudGapBox`/`conflictBox`/`healthBox`/`scanLog`/`scanLogLast`，有测试断言）；所有更新函数都按 `getElementById` 访问，位置改变零逻辑风险；`locateAuditBox` 是台账业务功能，**保留在台账页**。

### 14.2 自动化验证

- `npm test`：**456 / 456**（新增 4 条状态坞不变量）
- 变异测试：去掉角标展开坞 → 断言失败；去掉异常自动展开 → 断言失败
- 结构断言：`#statusDock` 必须包含全部 6 个 id；`#tab-ledger` 与 `#tab-scan` 不得再残留这些 id

### 14.3 ego-browser 界面验证（本地页面）

| 用户路径 | 结果 |
|---|---|
| 冷启动 | 页面正常启动、账本 `complete-valid`；状态坞存在且**默认折叠** |
| 切到扫码页 → 状态坞可见？ | ✅ `dockVisible=true`（原来队列面板藏在台账 section 里看不到） |
| 在**扫码页**点顶栏队列角标 | ✅ 状态坞自动展开 + 队列面板 `display:block`（**修复了「点了没反应」**） |
| 折回后切到**工单页**再点角标 | ✅ 同样展开 |
| 在工单页 `log()` 写日志 | ✅ 当前页展开状态坞即可见，最新一行实时显示（**修复了「日志在另一个页签」**） |
| 注入 1 条冲突 | 摘要显示「⚔️ 冲突 1」且坞**自动展开** |
| 注入 1 条待人工 | 摘要显示「🗑️ 待人工 1」且坞自动展开 |
| 清空后 | 摘要回到「同步/健康/日志」中性文案 |
| DOM 唯一性 | 四块面板 id 在全文档各只有 1 个（无重复定义） |

### 14.4 线上核对

- 8 表 CRUD：56 项断言全过、缺列 0、生产无残留
- 版本 `2.27.0` 已部署（见下）

### 14.5 阶段结论

✅ 阶段 2 通过。全站状态有了全站的家：任何页签可见、角标任何页可用、日志当前页可见、异常自动展开。
进入阶段 3「库存流水独立页与全量筛选」。

---

## 十五、阶段2 修订 + 阶段3 起步（2026-09-17，`2.28.0`）

### 15.1 用户反馈驳回了第一版状态坞

反馈原话：「当前前端是将状态坞放到每一个页面的下方了，我觉得还是不好，最好直接单开一个页面，和其他页面同级，还有的就是这个状态坞其中的 ui 也有问题」。

**反馈是对的**。我第一版把四块面板做成「每页底部的折叠坞」，等于把同一条噪音复制到每个作业页；
本来要解决的是「状态没有家」，结果变成「状态到处都是家」。

| 问题 | 修订 |
|---|---|
| 每页底部都挂一条折叠坞 | 改成**与其它页签同级的独立页** `#tab-sync`「同步与状态」，作业页彻底干净 |
| 发现性怎么办（独立页会被忽略） | 状态条上加**全站唯一入口** `#sbDock`：平时朴素，异常时**变红并显示异常摘要**，点击直达 |
| 一致时四块面板全隐藏 → 空白页 | 新增 `#syncAllClear`「✅ 一切正常」正面反馈 |
| 进同步页看到上次的旧内容 | `renderSyncTab()` 进入时重画四块面板 + 日志 |
| **对账明细里的长业务键被裁成残缺键**（截图可见 `ZZT9174214-M…`） | 明细**移出限宽表格**（表格 `max-width:520px`，colspan 单元格会裁掉长键），改为表格下方整宽展开；`overflow-wrap:anywhere` 允许换行；窄屏按钮占整行 |

### 15.2 我自己造成的两个线上事故（截图暴露的）

**事故一：`ZZT9174214-*` 残留记录。**
我跑线上 CRUD 核查脚本会建哨兵记录、验完删除。但用户浏览器只要在那个窗口同步过一次，
就会留下「只在本地」的残留（飞书已删，本地还在）。截图里 5 张表各 1 条。
→ 收场：阶段1 已交付的**逐键「删除本地」**正好能清（这次修订让它的键不再被裁切，可读可复制）。

**事故二（更严重）：流水序号高水位被污染。**
脚本用 `seq: 900001` 建哨兵流水。`state.txnSeq` 是**只增**的高水位（防号码重用破坏审计链），
被顶到 900001 后**没有任何回退路径** → 账本覆盖度永久报「缺 #24~#900000」，
**90 万条不存在的流水**，真正的缺口反而被埋在假缺口里看不见。

两处修复：
1. **根因**：脚本改成 `realMaxSeq + 1`（运行时读真实最大 seq），并加不变量断言禁止四位以上常量 seq。
2. **收场**：新增「孤悬大号流水」检测与清理。

关键认识：一开始我把检测写成「高水位 − 最大 seq > 1000」，**在真实形态下会漏报** ——
残留流水还在本地时，最大 seq 就等于高水位，差值为 0，看起来「正常」，但缺口照样存在，
因为缺口是那条流水**自己**造成的（孤零零悬在连续段之上）。
改成「找连续段之后的大跳跃」，并且真正的收场是**删掉那条残留流水**、高水位随之回落。

清理动作的安全设计（流水是只增审计账本，删它极重）：
- 先写删除凭据，**写失败零变更**
- 只删「对账认定为**仅本地**」的；飞书里也存在的一律不动（可能是真实数据）
- 没有对账凭据时**拒绝执行**（连确认框都不弹）——本地静态环境实测验证了这条
- 逐条从 outbox 摘除，避免残留 upsert 又把它们推回去
- **绝不调用 `fsPushDelete`**（不碰飞书）
- 完成后立即重新核对 + 回放校验，如实报状态

### 15.3 阶段3：库存流水独立页（同批交付）

| 问题 | 修复 |
|---|---|
| `renderTxns` 硬编码 `slice(0, 50)` —— **第 51 条起网页端完全不可达** | 分页（每页 100 + 「加载更多」），并显示「还有 N 条」 |
| 流水没有任何筛选，「找某天某物料的流水」不可能 | 关键词（物料码/类型/操作人/关联单/原因）+ 日期范围筛选 |
| 流水寄生在物料台账页底部的折叠块里 | 独立页 `#tab-txn`，回放校验/账本修数/导出审计日志随之归位 |
| 旧深链 `#ledger?view=transactions` 会静默失效 | 自动跳转到 `#txn`；`#txn?code=` 支持预填筛选（只读，不触发任何数据动作） |

### 15.4 自动化验证

- `npm test`：**465 / 465**（本轮新增 6 条：同步页结构、状态条入口、明细排版、孤悬检测、清理安全性、脚本 seq 不变量）
- 变异测试全部被抓住：去掉角标切页 / 去掉异常变红 / 明细塞回表格 / 去掉面板刷新 / 检测容差改错 / 凭据顺序颠倒 / 只删仅本地的闸门去掉 / 高水位不回落或回落错值 / 脚本改回 `seq:900001`（两种写法）
- **过程中发现两个我自己的假绿**，都已修强：
  ① 断言只搜 `realMaxSeq + 1`，命中了注释和日志文本 → 改为盯住**赋值那一行**且只允许一处；
  ② `--test-name-pattern` 写错时 node 报「零测试通过」，和「变异没被抓住」输出一模一样 → 加基线对照（先确认基线为 0 再判定）。

### 15.5 ego-browser 界面验证（本地页面）

| 用户路径 | 结果 |
|---|---|
| 冷启动 → 台账页 | 页面正常启动；作业页**不再有任何状态面板**（`statusDock` 已彻底移除） |
| 状态条入口 | 存在、可见、异常时显示「⚠ 同步与状态 · 🗑️ 待人工 2 · ⚠️ 2 张表不一致」 |
| 点入口 | 切到 `#sync`，四块面板/全清面板可见 |
| 在工单页点顶栏队列角标 | 自动切到同步页 + 队列面板展开（原来点了没反应） |
| 在工单页写日志 → 同步页 | 日志可见，最新一行实时显示 |
| 超长业务键（60+ 字符） | **完整渲染、零裁切**（`scrollWidth == clientWidth`）；明细在表格外（`insideTable: 0`）；页面零横向溢出 |
| 库存流水差异行 | 显示键但**零操作按钮**（只增账本语义） |
| 孤悬流水检测 | 认出 `contiguousMax: 23 / outlier: 900001 / jump: 899977`，健康面板显示警告 + 清理按钮 |
| 无对账凭据时点清理 | **零变更拒绝**，连确认框都不弹，提示先「重新核对」 |

### 15.6 阶段结论

✅ 阶段 2 修订通过（独立页 + UI 修复），阶段 3 主体交付。
待生产验证：孤悬流水清理的完整链路（本地静态环境无 `/api/feishu/reconcile`，拿不到 `localOnly`）。

---

## 十六、阶段 4：资源档案页 + 两处生产污染事故（2026-09-17，`2.29.0`）

### 16.1 阶段 4 交付

用户视角的真实缺口：**库位/容器/物品/手册这四张表原本只有「生成」没有「维护」** ——
容器建好类型/规格就写死，库位想改说明只能删了重建（而删记录会作废标签编码，代价很大）；
容器创建时 `loc` 恒为 `''`，网页端从不写「当前库位码」，而容器放错位置正是最常见要改的事。
（label 页的「记录列表」是只读文本，没有任何编辑控件。）

新增 `#tab-res`「资源档案」四段（库位/容器/物品/手册），每段都有：
全量列表 + 筛选 / 新增 / **编辑** / 作废删除（带影响范围与同步状态）/ 详情 / 打印跳转；
原 `gen` 页整体并入本页「批量建档」折叠区，避免同一对象两个入口；
旧深链 `#gen` 自动跳 `#res`。

### 16.2 我自己造成的两起生产污染（必须记录）

#### 事故一：「本地验证」根本不是隔离环境

在本地起 `python -m http.server` 做界面验证时，页面探不到同源 API（静态服务器对 `/api/*` 返回 501），
于是 `fsPullState` 走到**跨境兜底**，直接改用 `https://mes.newenergycoder.club`。

**后果**：我在「本地」做的 CRUD 验证，新建/修改的记录**真的写进了生产飞书**：
- 新建残留 6 条：`B-99-99-99`、`B-001`（库位）、`A4SH-011`（容器）、`WP-004`/`WP-005`（物品）、`SC-004`（手册）
- **修改了 2 条真实记录**：`A4SH-001` 的规格被改成「32×25×34cm（验证改规格）」、
  `A4SH-001`/`A4SH-002` 的「当前库位码」被我写成 `B-01-02-01`（原本都是空）

已全部清理并核对：`189/122/3/3`，无重复、无残留（`A4SH-001` 规格已还原为「A4 四抽」）。

**方法论修正**：本地构建只是「被测代码」，**数据始终是生产数据**。因此：
- 只做只读核对时可以用本地构建；
- 任何写操作必须**先建自己的测试记录、只改自己建的那条、最后删掉**，绝不碰真实记录。
- 已加护栏：跨境写入时明确记一行日志「同源后端不可用，已改用线上后端，这里的写入会直接改线上数据」，
  不再静默发生。

#### 事故二：新增容器会覆盖真实的 A4SH-001

`nextCode` 的前缀正则是 `^([A-Z]+)-(\d+)$`，而 `A4SH` 里含数字 `4` → 匹配失败 → `max` 恒为 0
→ **永远返回 `A4SH-001`**，也就是已存在的那个码。飞书按业务键 upsert，重复键不是「多一条」而是
**整行覆盖**（`SC4-*` 同理）。

这正是阶段 0 那类问题的翻版：**编号生成器产出撞键 → 飞书 upsert 把它压成一条**。
修复两层：
1. 正则改为 `^([A-Z0-9]+)-(\d+)$` 并整体比对前缀；
2. `resSave` 新增**兜底**：生成的码若已存在就换号重试，宁可换号也绝不覆盖真实记录。

（`gen` 页的批量生成原本就用 `addUnique` 跳过已存在的码，所以只有我新写的单条新增有这个问题。）

### 16.3 另外两个修掉的真问题

| 问题 | 修复 |
|---|---|
| 编辑时 `await` 推送 → 编辑器卡在网络往返上（离线要等超时），期间列表不刷新，用户看到「点了保存没反应」，而本地其实早存好了 | 先 `save()` + 关编辑器 + 重渲染，再 fire-and-forget 推送（全站既有约定）。实测关编辑器从「>800ms 且不关」变成 94ms |
| `__syncedKeys[table]` / `_pendingPush[table]` 是**数组**不是 Set，写成 `.has()`/`.delete()` 会静默失效（守卫恒为假=死代码），记录永远留在「已确认」名单里，对账反复报警 | 按既有写法改为数组 `filter`/`map(String).includes()` |

### 16.4 「删除凭据写失败就零变更」原本是假的

`fsJournalDeletion` 旧实现把异常吞掉并 `return 0`，于是调用方无论写
`if (j.ok === false)` 还是 `try { await ... } catch` 都**永远不触发** ——
安全保证实际是「删除照做，只是没留痕」。

修复：如实返回 `{ok, count, error}`；三处承诺零变更的调用点
（`deleteOneLocalRecord` / `resDelete` / 孤悬流水清理）全部改为检查返回值；
「人工确认删除」的日志也不再无条件声称「已写入删除凭据」。

### 16.5 新增能力：显式清空字段（clearFields）

服务端默认「空值不写」（这是对的：字段缺失的推送不该静默抹掉飞书的值），
但「只进不退」也不行 —— 容器从货位拿走之后「当前库位码」永远清不掉，只能删记录重建。
现在 `upsert` 接受 `clearFields`（本地字段名数组），只清空显式申报的列；
前端在「原本有值、现在被清空」时自动申报。

### 16.6 自动化验证

- `npm test`：**477 / 477**（本轮新增 13 条）
- 变异全部被抓住：`await` 推送 / 去掉 `#gen` 别名 / 凭据顺序颠倒 / `.has()` 当 Set /
  `.delete()` 当 Set / 编号正则退回 `[A-Z]+` / 撞键兜底改成 `if (false)` / 去掉显式清空
- 修掉一条不稳定测试：「四段预读必须并行发出」的时间兜底（4×120ms）在整套并行跑时假阳性，
  而「同时在飞的请求数」已完整覆盖该需求，删掉时间断言
- 过程中还发现两个我自己的**假绿**：断言只搜 `some(...)` 时把条件改成 `if (false)` 照样通过；
  `--test-name-pattern` 写错时 node 报「零测试通过」，与「变异未被抓住」输出一模一样
  （现在先跑基线对照再判定）

### 16.7 ego-browser 界面验证

| 用户路径 | 结果 |
|---|---|
| 进入资源档案页 | 四段类型条带计数（库位 189 / 容器 122 / 物品 3 / 手册 3）；表格 190 行 |
| 列头 | 库位码 / 类型 / 说明 / 同步状态 / 操作（详情·编辑·打印·作废删除） |
| 编辑容器「当前库位码」 | 保存后 94ms 内关编辑器、行内立即显示新值，**刷新后仍在**（本地持久化） |
| 四种类型的 新增→编辑 | 全部成功，字段变更生效（库位 desc、容器 loc、物品 name、手册 ver） |
| 筛选 | 关键词过滤正确，汇总显示「共 N 条 · 筛选出 M 条」 |
| 删除（取消） | **零变更**；确认框含「同步状态 / 影响范围 / 物理载体报废规则」 |
| 删除（确认） | 记录从本地与列表移除，日志留痕 |
| 详情 | 显示全字段 + 同步状态 + 被谁引用 |
| 打印跳转 | 切到标签页、类型正确、该条已预选、预览含该编码 |
| gen 并入 | `#tab-gen` 不复存在；批量建档表单仍在（`btnGenShelf`/`btnSeed` 可用） |
| 编号防撞 | `nextCode(containers,'A4SH')` 返回 `A4SH-011`（不再是 `A4SH-001`）；新增得到不冲突的码 |

### 16.8 阶段结论

✅ 阶段 4 通过（含两起自造事故的清理与根因修复）。
教训：**「本地跑一跑」不等于安全环境** —— 这个应用是会同步的生产客户端，验证必须按生产对待。

### 16.9 补充：幽灵流水收场（`2.29.1`）

清理本机残留时发现一个**阶段 1 没闭环的缺口**：流水被排除在普通本地删除之外（这个保护是对的，
账本凭证不能随手删），但阶段 1 把流水的收场指向了「回放校验 / 修数 / 导出」——
这三样**都删不掉**一条幽灵流水。结果是：只在本地、飞书里已经没有的流水会永远挂在
「本地多 N」上，既清不掉又让账本对不上，用户只能看着。

**来因（我的核查脚本造成的典型）**：线上 8 表 CRUD 核查在飞书建了一条哨兵流水，
本机同步过它，脚本随后把飞书那条删了 → 本机这条成为幽灵。
关键：它**号码是连续的**（23 之后是 24），所以第 15 节做的「孤悬大号流水」检测**抓不到它**，
只能靠对账的 `localOnly` 认定。

新增「幽灵流水」收场，沿用同一套安全设计：
只认对账确认仅本地的 → 先写凭据（写失败零变更）→ 只删本机 → **绝不调用 `fsPushDelete`**
→ outbox 逐条摘除 → 高水位回落到真实最大 +1 → 完成后重新核对 + 回放校验。

生产实测：识别幽灵 `#24`（type:测试 / operator:哨兵）→ 清理 → `transactions` 回到 23 条、
高水位 #24、账本 `complete-valid`、无缺口。

### 16.10 阶段 4 最终线上核对（`2.29.1`）

| 项 | 结果 |
|---|---|
| 8 表条数（本地/飞书） | 物料 5/5 · 库位 189/189 · 容器 122/122 · 人员 26/26 · 物品 3/3 · 手册 3/3 · 工单 5/5 · 流水 23/23 |
| 有差异的表 | 无 |
| 账本 | `complete-valid`，缺口 0，不一致 0 |
| 离线队列 / 冲突 / 待人工 | 0 / 0 / 0 |
| 高水位污染 / 幽灵流水 | 无 / 无 |
| 8 表 CRUD 核查 | 56/56 断言通过，生产库无残留 |
| 资源档案页 | 四段计数正确（189/122/3/3），列表 189 行，「本页记录均已同步到飞书」 |
| `A4SH-001` / `A4SH-002` | 规格已还原「A4 四抽」，被误写的「当前库位码」已清空 |

### 16.11 阶段结论

✅ 阶段 4 通过。两起自造的生产污染已完全清理并核对。
最重要的教训：**这个应用是会同步的生产客户端，「本地跑一跑」不是隔离环境** ——
本地静态服务器探不到同源 API 时会兜底连线上后端，测试记录会真的写进生产库。
验证写操作必须：先建自己的测试记录 → 只改自己建的 → 最后删掉，绝不碰真实记录。

---

## 十七、阶段 5：工单完整生命周期（2026-09-17，`2.30.0`）

### 17.1 阶段 5 的大部分已在阶段 0 落地

B9/B10 修复时已经做了，本轮逐条核对确认：

| 方案要求 | 落地情况 |
|---|---|
| 建单入口固定在库存工单页 | ✅ 已归位（`gen` 页只留主数据建档） |
| 列表行提供「详情」 | ✅ `data-detail` 行按钮 |
| 详情顶部提供「返回列表」 | ✅ `#btnWipBack` + `#btnWipClose` |
| 删除入口在用户能看见的位置并写明 | ✅ 危险区独立成块，文案写明「飞书同号记录也会一起删除，不可恢复」 |
| 已执行工单不可直接删除，必须先冲销 | ✅ `p.anyExecuted && !isCancelled(w)` → 提示先冲销 |
| 部分执行工单冲销只回退已执行数量 | ✅ 预览与执行都按 `it.executed` 过滤（实测：计划 8 / 已执行 5 → 只回退 5） |
| 删除/取消/冲销后返回正确列表状态 | ✅ 都调用 `backToWipList()` + `ctxClear()` |
| 当前对象条不指向幽灵工单 | ✅ 工单不存在时 `backToWipList()+ctxClear()` |
| 异常不出现白屏 | ✅ 「先确认存在再切详情视图」的顺序铁律 + 防复发断言 |

### 17.2 本轮补齐的唯一缺口：未执行工单可修改计划

`CORE.updateOrderPlan(state, order, patch, opts)` + 详情页内联编辑器。

**为什么只有未执行能动**（不是保守，是正确性）：
- 冲销的依据是 `it.executed`，而「已执行」是相对计划算出来的。
  事后改计划会让「计划 / 已执行 / 剩余」三者对不上，流水金额也无法解释；
- 部分执行的工单若把计划改到低于已执行数量，进度会出现 >100% 或负数剩余。

**工单号不提供编辑控件**：它是流水 `ref`、二维码机读串与飞书主键，
改了会让已有流水指向一个不存在的单号。

**逐项留痕**：改动进 `order.planEdits`，由 `orderHistory` 呈现为 `plan-edit` 事件。
只记「项数变了」不够 —— 对账时得能看到「哪个物料从 5 改成了 2」。

**复用建单行组件**（`wipItemRow()`）：保证「未建档 / 数量非法」的校验与建单完全一致，
不会出现两套规则漂移。

### 17.3 自动化验证

- `npm test`：**496 / 496**（新增 8 条核心行为 + 6 条静态不变量）
- 核心行为覆盖：改数量/删行/加行/改日期、已执行拒绝且零变更、已取消拒绝、
  非法输入（空明细/0/负数/未建档/无内容）全部零变更、同物料重复行合并、
  留痕与历史、**改计划不产生任何流水也不动库存**、部分冲销只回退已执行
- 变异验证（用对目标后全部被抓住）：放行已执行改计划 / 绕过核心直接赋值 /
  按钮去掉未执行守卫 / 删除块去掉 `ctxClear` / 删除块去掉回列表

> 过程中两次变异「没被抓住」其实是我**变异目标选错了**（改的是别处的同名代码、
> 或用的 `--test-name-pattern` 没匹配到目标用例）。重新定位后两条都被抓住 ——
> 记在这里，是因为「假绿」和「真漏洞」在输出上长得一模一样。

### 17.4 ego-browser 全生命周期实测（隔离环境）

**这次验证真的隔离了**：启动后立刻断开全部写通路
（`fsOnline=false; cloudOnline=false; fsWriteBase='http://127.0.0.1:9/dead'`），
所以所有写入只落本机离线队列。**事后核对生产库：8 表条数与内容零改动** ——
这正是第 16 节那次污染事故之后要建立的做法。

| 步骤 | 结果 |
|---|---|
| 新建工单 | 列表出现，KPIs 计划 5 / 已执行 0 / 剩余 5 |
| 打开详情 | 返回列表 ✓ 编辑计划 ✓ 删除 ✓ |
| **编辑计划** 5→8 + 改日期 | 明细与日期生效，`planEdits` 留痕，执行历史出现 `plan-edit`，编辑器自动关闭，详情保持打开 |
| 编辑器内容 | 无「工单号」输入框；日期预填；明细行复用建单组件 |
| 部分执行 3 件 | 状态→部分执行，剩余 5，批次 1，库存 20→17 |
| 执行后入口变化 | **编辑计划消失**（已执行）、取消消失、冲销出现 —— 全部符合预期 |
| 继续执行 2 件 | 默认值自动填剩余 5；执行后已执行 5、剩余 3、批次 2、库存 15 |
| 冲销预览 | 只列已执行 5，显示「加回 +5 / 当前库存 15 / 冲销后 20」 |
| 确认冲销 | 库存 15→20（**只回退已执行的 5，不是计划的 8**），计划仍为 8，状态已取消 |
| 删除工单 | 记录移除、详情关闭、列表可见、「当前对象」清空 |
| **重新打开已删工单的详情** | 自愈回列表 + 明确提示「已不存在…已回到工单列表」，**无白屏**，列表仍可用 |
| 全程账本 | `complete-valid`，无缺口，无流水不一致 |

### 17.5 线上核对（`2.30.0`）

- 8 表 CRUD：**56/56 断言通过，生产库无残留**
- 工单页：5 张工单（全部未执行）正常渲染；打开详情有「返回列表 / 编辑计划 / 删除」
- 编辑器在真实数据上正常打开：明细行数正确、日期预填、「工单号」无编辑控件、取消可关闭
- 账本 `complete-valid`，缺口 0，不一致 0，离线队列 0
- 隔离验证后生产库条数与内容零改动

### 17.6 阶段结论

✅ 阶段 5 通过。工单生命周期闭环：建单 → 改计划 → 部分执行 → 继续执行 →
冲销（只回退已执行）→ 取消 → 删除 → 详情自愈，任一环节都不白屏、不脱账。

---

## 十八、阶段 6（网页端部分）+ 一处线上账本不一致（2026-09-17，`2.31.0`）

### 18.1 标签页回归纯打印

方案要求「`label` 回归纯打印」。原状：危险区里藏着「删除勾选记录」，
能删库位/容器/模块区/物料/物品/手册/工单 —— 它其实是**第二个业务删除入口**
（资源档案是另一个），藏在折叠区里既不容易发现，删错还会作废标签编码。

- 移除 `#btnDelRec` 及其处理逻辑（含模块区分支），换成「去资源档案」指引链接
- 确认「模块区」没有因此失去管理能力：资源档案的「库位」段渲染 `state.locations`
  （含 `kind=模块区`），编辑/删除/打印一应俱全 —— 实测 6 个模块区均可在资源档案管理
- label 页现在只有：类型切换 / 筛选 / 全选 / 全不选 / 打印勾选标签

### 18.2 打印守卫（无需打印机即可验收的网页端部分）

| 场景 | 实测结果 |
|---|---|
| 未勾选直接点打印 | 拦下并提示「请先勾选要打印的标签」，**不会打出一张白纸** |
| 模块区卡在 60×40 标签纸模式下打印 | 拦下 + 自动切 A4 + 文字解释（不打守卫会出白纸，用户只会以为「打印机又坏了」） |
| 守卫位置 | 必须在 `window.print()` **之前**（已加断言） |
| 条码值与对象来源 | 模块区卡渲染 `LOC:M-01` 机读串 + 二维码（6 张卡 6 个 QR），与扫码图例声明的 7 类前缀（MAT/LOC/CTN/WIP/NEC/ITM/MAN）一致 |

### 18.3 摄像头无设备降级

把 `getUserMedia` 打成 `NotFoundError` reject 后实测：浮层保持打开并**常驻显示**
「无法打开摄像头：Requested device not found。请检查浏览器权限后重试。」
（不是弹一个 alert 就没下文 —— 关掉浮层用户就再也看不到失败原因了）。

### 18.4 仍需用户输入才能验收的部分

这些**不是没做**，而是依赖我拿不到的外部条件，不能由网页端单方面宣称通过：

| 项 | 缺什么 |
|---|---|
| 标签真实出纸 | 现场打印机 + 你的验收标准（尺寸/边距/字号） |
| 真实摄像头扫码 | 带摄像头的设备（降级路径已验） |
| InvenTree 真机同步 | 实例地址 + API Token |
| 闲鱼真实取数 | 取数命令/接口配置（不能拿 mock 结果当真机结果） |
| `btnSeed`/`btnWipe` 破坏性实测 | 一个与生产隔离的环境（在生产上点「清空全部数据」会真的清空） |

### 18.5 ⚠️ 线上账本发现一处不一致（需要你决定，我没有自动改）

本轮线上核对时发现 `replayAudit` 报 `mismatch`：

```
物料 GJ-SD-001：账本口径（期初 3 + Σ变动 72）= 75，实际库存记录 = 225，差 +150
```

**账本链**（飞书真实流水，共 24 条）：

| # | 时间 | 类型 | delta | 余量 |
|---|---|---|---|---|
| 1 | 09-12 13:05 | 手工调整 | +2 | 5 |
| 2 | 09-12 13:10 | 手工调整 | +2 | 7 |
| 3 | 09-13 20:00 | 盘点 | −1 | 6 |
| 4 | 09-13 20:03 | （乱码） | −81 | **−75** |
| 5 | 09-16 21:15 | 系统补号 | 0 | −75 |
| 24 | 09-17 11:47 | 手工调整 | **+150** | **225** |

关键矛盾：第 4 条把余量打到 **−75**，第 24 条 (+150) 的余量却是 **225** ——
按链式算应该是 75。也就是说，在第 5 条与第 24 条之间，**有过一次把库存从 −75 改成 75
的动作，但它没有留下流水**（或者那条流水被删掉了）；随后第 24 条又加了 150。

我的判断与边界：
- 第 4 条那个 **−81 / −75 负库存**很可能是我早期用 `GJ-SD-001` 做工单执行测试时
  过度执行留下的损伤（该物料当时是负库存，这在早期记录里出现过）；第 24 条那次
  「手工调整 +150」更像是一次纠正尝试；
- 但**我无法证明**第 24 条是谁写的，也**不能替你决定哪个数才是真实库存**：
  225 可能是对的（实物盘出来 225，只是中间那次修正没落流水），
  75 也可能是对的（那一列被误写成 225）。
- 我的核查脚本不可能写这个数：它只建 `ZZT…-MAT` 哨兵物料，
  且服务端对物料表的「库存数量」一律不写（有专门的兜底与断言）。

**建议的处理方式**（两个工具都在「同步与状态」→ 健康面板）：
1. 先点「回放校验」看完整证据（会逐条列出哪条流水与账面对不上）；
2. 按实物盘点结果决定方向：
   - 若实物是 **225** → 账本缺一条「修正」流水，用「账本修数」把缺失的那次修正补成流水；
   - 若实物是 **75** → 直接点「账本修数」，服务端会按账本口径把库存数量改回 75。
3. 两种都会留下凭据，可追溯。

> 这正是「账本不变量」的意义：库存数量与「期初 + Σ变动」必须相等，
> 一旦被外部改动（导入、手工纠正、早期 bug）就会立刻报出来，而不是静默漂移。

另外：本轮跑线上 8 表 CRUD 核查后，浏览器本地会留下 `ZZT…` 哨兵记录（飞书已清理，
本地是「只在本地」的残影）。在「同步与状态」点「重新核对」，再逐键「删除本地」即可清掉。

### 18.6 自动化验证

- `npm test`：**500 / 500**（阶段 6 新增 4 条静态不变量）
- 变异：去掉模块区打印守卫、去掉未勾选守卫，均被断言抓住
- 线上：`2.31.0` 已部署；label 页无删除入口、未勾选守卫生效、6 个模块区在资源档案可编辑可删除

### 18.7 阶段结论

✅ 阶段 6 的**网页端部分**通过（纯打印、打印守卫、条码/来源、摄像头降级）。
⚠️ 依赖外部条件的 5 项（真实出纸、真实摄像头、InvenTree、闲鱼、破坏性实测）需要你提供条件后才能验收。
⚠️ 线上 `GJ-SD-001` 账本差 +150，已定位到具体流水，**等你按实物决定方向后用「账本修数」收场**。
