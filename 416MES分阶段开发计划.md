# 416MES 分阶段开发计划（按需求书 v1.1）

## 1. 计划原则

- 以 `416MES开发需求书.md` 为准绳，结合当前代码现状推进；
- 分阶段交付，每阶段都必须可运行、可验证、可回收；
- 每阶段完成后先做功能验证测试，再做回归检查，最后进行验收回收；
- 前一阶段验收通过后，才进入下一阶段；
- 先修复影响库存正确性的核心问题，再补齐扫码闭环，再推进云同步和外部集成；
- 文档中描述但当前未实现的能力，先在计划中标记，不直接默认已完成。

## 2. 当前阶段结论

当前代码已经覆盖需求书中的大部分单机能力，核心可用模块包括：

- 物料台账；
- 人员台账；
- 编码生成；
- 标签打印；
- 扫码查询与工单执行；
- 盘点；
- 库存流水；
- Excel / JSON 导入导出；
- NEC 工单；
- 排版工具；
- 飞书 CLI 同步；
- InvenTree CLI 单向镜像。

但按需求书完整要求，当前仍缺以下关键能力：

1. 模块区字段挂接与 `LOC:M-0x` 物料清单；
2. 30S 定位兜底（未命中时回查最近扫码/流水位置）；
3. 扫码前缀补全（`NEC:`、`ITM:`、`MAN:`）；
4. 工单管理页、部分执行、取消/冲销、重复物料合并校验；
5. 真源模式 `feishu-server.mjs`、直写飞书、离线队列冲刷；
6. jsQR 摄像头扫码；
7. 闲鱼同步；
8. InvenTree 扫码即写；
9. 自动化测试与验收基线。

---

# 3. 分阶段规划

## Phase 0：基线与开发环境收口

### 目标

先把当前工作区整理成可开发、可回归、可安全提交的基线。

### 范围

- 建立 Git 基线提交；
- 补 `.gitignore`；
- 移除或隔离敏感凭据文档；
- 增加基础开发脚本；
- 为后续测试准备统一入口。

### 具体任务

1. 初始化当前仓库的首次提交；
2. 新增 `.gitignore`，至少覆盖：
   - `node_modules/`
   - `feishu-backend.config.json`
   - `inventree-sync.config.json`
   - `nec-sync.config.json`
   - `xianyu-sync.config.json`
   - `inventree-data/`
   - 各类导出的备份文件与临时文件
3. 检查 `INVENTREE-INTEGRATION.md` 中的敏感信息，改为占位说明或迁移到本地私有文档；
4. 补 `npm run dev` / `npm run serve` 之类的基础启动脚本；
5. 确定后续测试组织方式，优先抽离纯逻辑到独立模块。

### 验证测试

- `git status` 干净；
- 本地静态服务可启动；
- `node --check` 通过现有脚本；
- 首页、主页面、NEC 工单页可正常打开；
- 敏感文件不会被误提交。

### 回收标准

- 有可回滚的基线提交；
- 没有敏感凭据进入版本库；
- 后续开发可以在统一命令下运行。

---

## Phase 1：核心库存逻辑修复与可测试化

### 目标

先修复“库存工单 + 扫码执行”里会导致库存错误的核心问题，并把核心规则抽成可测试模块。

### 范围

- 工单执行逻辑修复；
- 盘点逻辑修复；
- 库存流水规则统一；
- 重复物料合并；
- 执行前校验前置；
- 单元测试建立。

### 具体任务

1. 将 `index.html` 中的核心库存逻辑抽出为独立 JS 模块；
2. 工单创建时校验物料存在性；
3. 同一工单中同一物料自动合并或禁止重复；
4. 执行前按物料汇总后校验库存；
5. 保留计划数量，执行数量单独记录；
6. 盘点时严格校验输入，不允许 `NaN` 静默转 0；
7. 库存变化必须统一走流水写入函数；
8. 建立单元测试覆盖：
   - 工单创建；
   - 库存扣减；
   - 库存增加；
   - 库存不足拦截；
   - 已执行工单防重复；
   - 盘点差异；
   - 回放校验；
   - 重复物料合并。

### 验证测试

- 手工录入一组领料工单并执行；
- 验证同一物料多行时不会扣成负库存；
- 验证已执行工单不能再次执行；
- 验证盘点差异写流水；
- 单元测试全部通过；
- 既有页面功能无回归。

### 回收标准

- 库存不会再因为重复物料而出错；
- 关键库存规则有自动化测试；
- 该阶段验收通过后，才进入扫码闭环扩展。

---

## Phase 2：扫码出入库闭环补全

### 目标

把当前“扫码查询 + 扫工单执行”升级为真正完整的扫码闭环。

### 范围

- 扫码前缀补全；
- 模块区扫码；
- 物品 / 手册 / NEC 工单扫码；
- 未命中兜底逻辑；
- 扫码页交互优化。

### 具体任务

1. 支持 `NEC:`、`ITM:`、`MAN:` 扫码；
2. 增加扫码后的业务视图：
   - `ITM:` 查物品；
   - `MAN:` 查手册；
   - `NEC:` 查 NEC 工单；
3. `LOC:M-0x` 支持列出模块区物料；
4. `MAT:` 未命中时，自动回查：
   - 最近一次扫码记录；
   - 最近一次库存流水；
   - 最近一次相关工单；
5. 在扫码结果中展示最近位置与时间；
6. 增加扫码日志检索能力；
7. 优化扫码页显示，使查询结果更适合现场快速判断。

### 验证测试

- 扫 `MAT:`、`LOC:`、`CTN:`、`WIP:` 均正常；
- 扫 `NEC:`、`ITM:`、`MAN:` 可打开对应记录；
- 扫模块区码能列出该区物料；
- 对不存在物料能给出最近一次位置线索；
- 回归既有工单执行流程。

### 回收标准

- 需求书 3.4 的扫码闭环规则完整落地；
- 30S 定位的查询路径已经打通；
- 扫码闭环验收通过后，才进入工单管理增强。

---

## Phase 3：工单管理增强

### 目标

把“库存工单”从当前的扫码执行入口，升级为完整的管理模块。

### 范围

- 工单列表页；
- 工单详情页；
- 状态流转；
- 部分执行；
- 取消 / 冲销；
- 执行历史；
- 关联流水查看。

### 具体任务

1. 新增独立的“库存工单”管理页；
2. 工单状态模型扩展：
   - 待执行；
   - 部分执行；
   - 已执行；
   - 已取消；
3. 支持工单搜索、筛选、查看详情；
4. 支持部分执行；
5. 支持取消未执行工单；
6. 支持执行后冲销；
7. 工单详情展示计划数量、实际数量、剩余数量；
8. 工单详情关联库存流水；
9. 增加执行批次记录；
10. 增加执行历史追溯。

### 验证测试

- 创建一个多物料工单并部分执行；
- 再次执行剩余部分；
- 取消未执行工单；
- 冲销已执行工单；
- 验证库存与流水链一致；
- 验证历史记录完整可查。

### 回收标准

- 工单不再只是“创建 + 扫码执行”两段式；
- 具备完整的业务管理闭环；
- 该阶段验收通过后，才进入模块区与 30S 定位收口。

---

## Phase 4：模块区与 30S 精确定位收口

### 目标

完成需求书提出的“任何零件 30S 内定位”能力。

### 范围

- 物料模块区字段；
- 模块区下拉绑定；
- 模块区物料清单；
- 30S 定位兜底；
- 定位结果呈现优化。

### 具体任务

1. 给物料台账增加 `zone` / `module` 字段；
2. 台账中支持模块区下拉候选；
3. 扫 `LOC:M-0x` 时列出该区物料；
4. `MAT:` 未命中时回查最近记录；
5. 在查询结果中展示：
   - 库位；
   - 容器；
   - 模块区；
   - 最近扫码时间；
   - 最近流水时间；
6. 优化台账搜索和扫码结果展示，使现场可快速定位。

### 验证测试

- 随机抽 10 个零件，验证 30S 内可定位；
- 验证无台账记录时仍能给出最近位置；
- 验证模块区与物料关联正确；
- 验证扫码页和台账页的一致性。

### 回收标准

- 需求书中的最终目标 6 可验收；
- 任何零件都能快速定位到库位 / 容器 / 模块区；
- 该阶段验收通过后，才进入云同步与外部集成。

---

## Phase 5：飞书真源模式与离线队列

### 目标

把当前“只读拉取 + CLI 推送”升级为需求书中定义的真源模式。

### 范围

- `feishu-server.mjs`；
- 启动拉取；
- 直写飞书；
- 离线队列；
- 恢复后冲刷；
- 与本地合并逻辑统一。

### 具体任务

1. 新增 `feishu-server.mjs`；
2. 启动时从飞书 8 表拉取并合并；
3. 工单执行、盘点、手工调整直写飞书；
4. 断网进入离线队列；
5. 网络恢复后冲刷队列；
6. 与现有 `mergePackage()` 保持同一套合并语义；
7. 增加失败重试和错误日志；
8. 补配置说明。

### 验证测试

- 启动后能拉取云端数据；
- 工单执行能直写飞书；
- 断网后操作进入队列；
- 恢复后队列成功冲刷；
- 本地与云端数据一致。

### 回收标准

- 真源模式可运行；
- 离线队列可恢复；
- 该阶段验收通过后，才进入摄像头扫码和外部系统深化。

---

## Phase 6：移动端摄像头扫码与现场体验

### 目标

补齐需求书中明确要求的手机摄像头扫码能力。

### 范围

- jsQR 集成；
- 摄像头调用；
- 移动端扫码 UI；
- 现场作业反馈。

### 具体任务

1. 引入 jsQR；
2. 支持 `getUserMedia` 摄像头扫码；
3. 扫码结果自动填入扫码输入框；
4. 手机端优化扫码页布局；
5. 增加扫码成功 / 失败提示；
6. 支持常见二维码内容格式。

### 验证测试

- 手机摄像头可识别 `MAT:` / `LOC:` / `CTN:` / `WIP:`；
- 识别后能正确跳转或执行；
- 无摄像头环境下回退到手输；
- 不影响现有扫码枪输入。

### 回收标准

- 移动端扫码可用；
- 现场操作效率明显提升；
- 该阶段验收通过后，才进入外部系统联动深化。

---

## Phase 7：外部系统深化

### 目标

完成需求书中规划但当前未落地的外部集成。

### 范围

- 闲鱼同步；
- InvenTree 实时写；
- 历史数据导入流程完善；
- 与云同步逻辑统一。

### 具体任务

1. 新增 `xianyu-sync.py` 或等价实现；
2. 建立 `.venv-xianyu/` 隔离环境；
3. 完成闲鱼字段映射与合并规则；
4. InvenTree 支持扫码即写；
5. 统一台账同步入口；
6. 历史数据导入流程补齐文档和校验。

### 验证测试

- 闲鱼数据可正确合并进台账；
- InvenTree 能实时写入库存变化；
- 历史数据导入可重复执行且幂等；
- 不影响现有本地数据。

### 回收标准

- 外部集成不再是“文档里有、代码里没有”；
- 该阶段验收通过后，才进入最终收尾。

---

## Phase 8：最终验收与文档收口

### 目标

按需求书验收标准做一次完整回归，并完成文档收口。

### 范围

- 全链路回归测试；
- 文档同步更新；
- 发布检查；
- 遗留问题清单整理。

### 具体任务

1. 按需求书验收标准逐项回归；
2. 更新 README / 需求书 / 集成文档；
3. 清理过期描述；
4. 输出最终验收报告；
5. 标记未完成项为 Roadmap，而不是混入已实现说明。

### 验证测试

- 9 项验收标准逐项通过；
- 本地、真源、公网模式至少各跑通一次；
- 文档与代码一致；
- 所有阶段回收记录齐全。

### 回收标准

- 项目达到需求书最终版要求；
- 可进入正式维护和持续迭代阶段。

---

# 4. 当前最优先启动的阶段

结合当前代码状态和需求书要求，建议立即从 **Phase 0** 开始，随后进入 **Phase 1**。

原因是：

- 当前还没有 Git 基线；
- 还没有 `.gitignore`；
- 核心库存逻辑存在重复物料导致负库存的风险；
- 需求书验收标准 2、9 目前还无法完整通过。

---

# 5. 交付节奏建议

建议每个阶段都按同一节奏执行：

1. 先实现；
2. 再补测试；
3. 再做功能验证；
4. 再做回归检查；
5. 最后回收验收。

每一阶段都必须留下：

- 变更说明；
- 测试结果；
- 验收结论；
- 是否允许进入下一阶段。

---

如果你认可这个分阶段计划，下一步我可以直接开始把它落成 Phase 0 的具体任务清单和执行顺序。