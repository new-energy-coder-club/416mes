# 416MES 分阶段开发计划（按需求书 v1.2 · 迁移包校准版）

## 1. 计划原则

- 以 `416MES开发需求书.md` v1.2 为准绳，结合**迁移包 2026-09-14 版**与主目录现状推进；
- 分阶段交付，每阶段都必须可运行、可验证、可回收；
- 每阶段完成后先做功能验证测试，再做回归检查，最后进行验收回收；
- 前一阶段验收通过后，才进入下一阶段；
- 先解决版本对齐与密钥安全，再修复库存正确性，再补扫码闭环，最后推进 Roadmap。

## 2. 当前阶段结论（迁移包校准后）

迁移包 `416MES-迁移包/` 是 2026-09-14 从 `D:\Project_env\416MES` 打包的**新版本**，主目录 `/srv/416mes` 是**旧版本**。两版核心差异：

| 能力 | 主目录 | 迁移包 |
| --- | --- | --- |
| index.html 行数 | 1788 | 2016 |
| 模块区字段（zone） | ❌ | ✅ 含列、下拉、Excel 导入导出 |
| `LOC:M-0x` 列物料 | ❌ | ✅ |
| jsQR 摄像头扫码 | ❌ | ✅（jsqr.min.js + getUserMedia） |
| 飞书真源直写 | ❌ | ✅（feishu-server.mjs + fsPushStock） |
| 离线队列冲刷 | ❌ | ✅（fsQueue） |
| InvenTree 扫码即写 | ❌ | ✅（invPush） |
| `NEC:` 扫码前缀 | ❌ | ⚠️ 正则已含，但无 `scanNec()` 专属分支 |
| `ITM:` / `MAN:` 扫码 | ❌ | ❌ |
| 30S 定位兜底 | ❌ | ❌ |
| 工单管理增强 | ❌ | ❌（重复物料 bug 仍在） |
| 闲鱼同步 | ❌ | ❌ |

迁移包额外包含：

- 三个含密钥的 `*.config.json`；
- `.cloudflared/` 隧道凭证与证书；
- `inventree-data/` 完整 SQLite 数据库；
- `启动.bat` / `启动隧道.bat`。

主目录额外包含：

- `typesetting/`（排版工具 + 字体）；
- `join.html` / `flyer-a4.html` / `recruit-*` 招募物料；
- `api/feishu-sync.js`（Vercel Serverless 只读接口）；
- `INVENTREE-INTEGRATION.md` / `README.md` 等文档。

---

# 3. 分阶段规划

## Phase 0：版本对齐与基线收口

### 目标

把主目录和迁移包对齐成同一份可开发基线，排除密钥与数据库，建立 Git 基线。

### 范围

- 版本差异合并；
- `.gitignore` 建立；
- 敏感文件隔离；
- 开发脚本统一；
- 首次 Git 提交。

### 具体任务

1. 以迁移包 `index.html`（2016 行）为基准，确认与主目录版本的完整差异清单；
2. 合并迁移包新增能力到主目录 `index.html`：
   - 模块区 `zone` 字段与 `zoneList`；
   - `LOC:M-0x` 模块区物料清单；
   - jsQR 摄像头扫码；
   - `fsBoot()` / `fsPushStock()` / `fsQueue` 飞书真源；
   - `invPush()` InvenTree 实时同步；
   - `NEC:` 扫码前缀正则；
   - 顶部 `nav.tabs a.tab-link` 样式；
3. 把迁移包的 `feishu-server.mjs`、`jsqr.min.js` 纳入主目录；
4. 保留主目录独有资产：`typesetting/`、`join.html`、`flyer-a4.html`、`api/feishu-sync.js`；
5. 新增 `.gitignore`，排除：
   - `*.config.json`（feishu-backend / inventree-sync / nec-sync / xianyu-sync）
   - `.cloudflared/`
   - `inventree-data/`
   - `node_modules/`
   - 备份与导出文件
6. 将迁移包中含密钥的文件移出版本库（保留在本地或加密存档）；
7. 更新 `README.md` 与 `INVENTREE-INTEGRATION.md`，移除明文密码与 Token；
8. 建立首次 Git 提交，标记为基线。

### 验证测试

- `git status` 干净，密钥文件未被跟踪；
- `node feishu-server.mjs` 可启动；
- 页面 header 显示「🟢 飞书真源已连接」；
- 模块区下拉、摄像头扫码按钮、离线队列入口均可见；
- `npm run dev`（或等价命令）可用。

### 回收标准

- 主目录与迁移包代码对齐；
- 密钥与数据库不进入版本库；
- 有可回滚的 Git 基线；
- 验收通过后进入 Phase 1。

---

## Phase 1：核心库存逻辑修复与可测试化

### 目标

修复"库存工单 + 扫码执行"里会导致库存错误的核心问题，并建立自动化测试。

### 范围

- 工单执行逻辑修复；
- 盘点逻辑修复；
- 重复物料合并；
- 执行前校验前置；
- 单元测试建立。

### 具体任务

1. 将核心库存逻辑抽出为独立 JS 模块（如 `mes-core.js`）；
2. 工单创建时校验物料存在性；
3. 同一工单中同一物料自动合并或禁止重复；
4. 执行前按物料汇总后校验库存（修复负库存 bug）；
5. 保留计划数量，执行数量单独记录；
6. 盘点时严格校验输入，不允许 `NaN` 静默转 0；
7. 库存变化统一走流水写入函数；
8. 建立单元测试覆盖：
   - 工单创建；
   - 库存扣减 / 增加；
   - 库存不足拦截；
   - 已执行工单防重复；
   - 盘点差异；
   - 回放校验；
   - 重复物料合并。

### 验证测试

- 手工录入一组领料工单并执行；
- 验证同一物料多行时不会扣成负库存；
- 验证已执行工单不能再次执行；
- 验证盘点差异写流水；
- 单元测试全部通过；
- 既有页面功能无回归。

### 回收标准

- 库存不会再因为重复物料而出错；
- 关键库存规则有自动化测试；
- 验收通过后进入 Phase 2。

---

## Phase 2：扫码出入库闭环补全

### 目标

把当前"扫码查询 + 扫工单执行"升级为真正完整的扫码闭环。

### 范围

- `NEC:` 扫码专属分支；
- `ITM:` / `MAN:` 扫码支持；
- 未命中兜底逻辑；
- 扫码页交互优化。

### 具体任务

1. 为 `NEC:` 增加 `scanNec()` 分支，打开 NEC 工单详情；
2. 支持 `ITM:` 扫码，显示物品名称/规格/库位；
3. 支持 `MAN:` 扫码，显示手册名称/版本/库位；
4. `MAT:` 未命中时自动回查：
   - 最近一次扫码记录；
   - 最近一次库存流水；
   - 最近一次相关工单；
5. 在扫码结果中展示最近位置与时间；
6. 优化扫码页显示，使查询结果更适合现场快速判断。

### 验证测试

- 扫 `MAT:`、`LOC:`、`CTN:`、`WIP:`、`NEC:`、`ITM:`、`MAN:` 均正常；
- 对不存在物料能给出最近一次位置线索；
- 摄像头扫码与手输结果一致；
- 回归既有工单执行流程。

### 回收标准

- 需求书 3.4 的扫码闭环规则完整落地；
- 验收通过后进入 Phase 3。

---

## Phase 3：工单管理增强

### 目标

把"库存工单"从扫码执行入口升级为完整的管理模块。

### 范围

- 工单列表页；
- 工单详情页；
- 状态流转；
- 部分执行；
- 取消 / 冲销；
- 执行历史；
- 关联流水查看。

### 具体任务

1. 新增独立的"库存工单"管理页；
2. 工单状态模型扩展：待执行 / 部分执行 / 已执行 / 已取消；
3. 支持工单搜索、筛选、查看详情；
4. 支持部分执行；
5. 支持取消未执行工单；
6. 支持执行后冲销；
7. 工单详情展示计划数量、实际数量、剩余数量；
8. 工单详情关联库存流水；
9. 增加执行批次记录；
10. 增加执行历史追溯。

### 验证测试

- 创建一个多物料工单并部分执行；
- 再次执行剩余部分；
- 取消未执行工单；
- 冲销已执行工单；
- 验证库存与流水链一致；
- 验证历史记录完整可查。

### 回收标准

- 工单具备完整的业务管理闭环；
- 验收通过后进入 Phase 4。

---

## Phase 4：30S 精确定位收口

### 目标

完成需求书提出的"任何零件 30S 内定位"最终目标。

### 范围

- 未命中兜底；
- 定位结果呈现优化；
- 现场抽查验证。

### 具体任务

1. `MAT:` 未命中时回查最近记录（已在 Phase 2 部分实现，本阶段收口）；
2. 在查询结果中展示：库位、容器、模块区、最近扫码时间、最近流水时间；
3. 优化台账搜索和扫码结果展示；
4. 现场抽查验证。

### 验证测试

- 随机抽 10 个零件，验证 30S 内可定位；
- 验证无台账记录时仍能给出最近位置；
- 验证模块区与物料关联正确；
- 验证扫码页和台账页的一致性。

### 回收标准

- 需求书验收标准 9 通过；
- 验收通过后进入 Phase 5。

---

## Phase 5：闲鱼同步（Roadmap 落地）

### 目标

补齐需求书 4.3 中规划但当前未实现的闲鱼数据同步。

### 范围

- 闲鱼同步脚本；
- 隔离环境；
- 合并规则；
- 凭据管理。

### 具体任务

1. 新增 `xianyu-sync.py`（或等价 Node 实现）；
2. 建立 `.venv-xianyu/` 隔离环境；
3. 实现字段映射：outer_id/product_id/标题/stock/售价/首图；
4. 实现合并规则：不覆盖本地已维护字段，空值不覆盖旧值；
5. 凭据存 `xianyu-sync.config.json` 并加入 `.gitignore`；
6. 与现有 Excel 导入流程打通。

### 验证测试

- 闲鱼数据可正确合并进台账；
- 本地已维护字段不被覆盖；
- 0 库存正确表示售罄；
- 幂等重复执行不产生重复记录。

### 回收标准

- 需求书 4.3 落地；
- 验收通过后进入 Phase 6。

---

## Phase 6：最终验收与文档收口

### 目标

按需求书验收标准做一次完整回归，并完成文档收口。

### 范围

- 全链路回归测试；
- 文档同步更新；
- 发布检查；
- 遗留问题清单整理。

### 具体任务

1. 按需求书 8 项验收标准逐项回归；
2. 更新 README / 需求书 / 集成文档；
3. 清理过期描述；
4. 输出最终验收报告；
5. 标记 Roadmap 剩余项。

### 验证测试

- 9 项验收标准逐项通过；
- 本地、真源、公网模式各跑通一次；
- 文档与代码一致；
- 所有阶段回收记录齐全。

### 回收标准

- 项目达到需求书最终版要求；
- 可进入正式维护和持续迭代阶段。

---

# 4. 当前最优先启动的阶段

结合迁移包校准结果，建议立即从 **Phase 0：版本对齐与基线收口** 开始。

原因：

- 主目录与迁移包并存，直接开发会从旧版本往回做；
- 迁移包内含密钥与数据库，必须先隔离；
- 当前没有 Git 基线；
- 需求书验收标准 2、9 仍需 Phase 1 / Phase 4 才能完整通过。

---

# 5. 交付节奏建议

每个阶段按同一节奏执行：

1. 先实现；
2. 再补测试；
3. 再做功能验证；
4. 再做回归检查；
5. 最后回收验收。

每一阶段都必须留下：

- 变更说明；
- 测试结果；
- 验收结论；
- 是否允许进入下一阶段。

---

如果你认可这个校准后的计划，下一步我可以直接开始执行 **Phase 0：版本对齐与基线收口**，先输出两版 index.html 的完整差异清单，再合并代码并建立 Git 基线。