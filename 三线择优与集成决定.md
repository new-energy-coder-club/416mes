# 三线择优结论（集成前）

## 结论

拟采用 **GPT-6路线 `559237c`** 作为本地集成基线。理由是它目前唯一提供覆盖S0–S5及S5.1的完整页面/API/持久化/隔离测试候选，且主会话有固定提交上的独立复验。不是因为它与主会话同模型，也不是因为其测试数更高。

该决定只针对**默认新ITM正式写入关闭的代码基线**，不是批准生产发布，更不是宣称真实多设备库存已启用。共享协调/身份提供者与真实飞书表、硬件门禁依然必须满足。

## 三路线实际比较

| 维度 | GPT-6 | GLM5.3 | Kimi K3 |
|---|---|---|---|
| 分支 | feature/unique-item-gpt6 | feature/unique-item-glm53 | feature/unique-item-kimik3 |
| 候选 | 559237c；前锚点cfc6b44 | 45a1fda及其后未提交修复 | 55d9297＋未提交领域文件 |
| 完成范围 | 领域、schema、IDB、双向同步、独立查询、三码/条码、操作协议、实际仓储、复核UI | 领域、schema、S2同步/持久化及部分真实网页合并接线 | 领域模块及23测试；其余未完成 |
| 独立证据 | cfc6b44：577Node+1Chromium+30探针；559237c：36独立探针通过；最后全量集成回归待执行 | 主会话全套工作树566通过、后续定向缺陷修复；真实合并入口5通过；S3/S4/S5未交付 | 领域23通过、独立5通过；完整日志534通过，未交完整路线 |
| 安全/恢复 | 默认拒新写、保留未知屏障、固定载荷、日志组验证、故障修复 | 多个已发现缺陷已改进，操作证明和关联完整性仍在收尾 | 多次工具参数错误，虽领域修复通过，不能评价未实现链路 |
| 集成资格 | 有条件合格：仅已实现范围、默认禁写 | 不完整，不作为整条产品基线 | 不完整，不作为整条产品基线 |

GLM供应商后期出现570秒请求超时，已要求短报告冻结，不通过无限重试拖住整体集成。Kimi反复生成无效权限升级参数且增量schema文稿存在字段错误，已回收保留，不将其失败误归为主机无权限。

## 不混合三套实现

三线各自数据和API模块组织不同，不自动merge全部分支。选择GPT完整基线后，GLM/Kimi有价值的测试/风险发现保留到独立验收，不直接拼接造成多套真源和注册表。

## 集成前和集成后动作

1. 保留main原提交及三条本地分支；记录工作区原有未跟踪计划/证据文档。
2. GPT候选必须保持clean；本地集成前核查与主目录同名需求文档冲突，不覆盖最新规划。
3. 本地合入后安装锁定依赖，运行Node全套、Chromium及独立矩阵；失败则修复并重验，不push。
4. 明确用户可用范围：查询、扫码、本机草稿与隔离验证；真实正式出入库当前默认不可写。
5. 形成最终本地集成验收报告，列已通过、未实现和外部待验证，不把NOT_RUN计PASS。

## 不可省略的限制

- 尚无真实Vercel持久协调适配器和生产身份提供者，handler默认401/503；提供协议和组合入口不等于生产已完成。
- 飞书第九表Table ID/schema/ACL未核验，不进行生产写测试。
- 真机相机/飞书内置浏览器/实物标签未验证。
- LOC v1只支持核实启用，停用/重新启用需版本化扩展，不能凭无序历史日志猜。
- 跨设备导入命令仅可只读复核，不自动重发。
- 正式push会触发main的Vercel生产工作流，需单独授权；本次不push。
