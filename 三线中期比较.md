# 三线开发阶段比较（中期，非最终择优）

## 比较口径

共同基线55d9297、相同需求和测试门槛。测试数不同不能直接判断优劣；阶段覆盖、真实页面接线、安全边界和独立复测优先。主会话曾对Kimi作者补丁作机械应用，已记录，不是完全无辅助产出；其他两线同样收到独立缺陷反馈，不共享代码。

## 当前观察

| 维度 | GLM 5.3 | Kimi K3 | GPT-6 |
|---|---|---|---|
| 当前提交 | d1d63ca，S2修复 | 基线55d9297，领域未提交 | 227b962，S4；S5收尾未提交 |
| 唯一实体纯逻辑 | 已实现，多项缺陷经反馈修复 | 已实现，23领域测试主验通过 | 已实现，含受控注册/启用后续扩展 |
| 飞书schema与读写保护 | 已接入，继续日志proof核验 | 未接入 | 已接入，真实Repository＋fakeHTTP往返通过 |
| IDB/同步 | S2接线与proof仍在调整 | 未实现 | S2已提交，主会话17定向测试＋缺陷复验通过 |
| 页面查询/三码/条码 | 尚待完整阶段交付 | 未实现 | 独立查询、作业表格、真实ZXingfixture、Chromium整页已有证据 |
| 操作接口与恢复 | S4未交付 | 未实现 | S4已提交，幂等/恢复/默认拒写验证通过 |
| 端到端 | 未执行完整路线 | 未执行 | 分层E2E已执行，真实生产门禁仍未通过 |
| 执行稳定性 | 长阶段调试但持续有文件进展 | 多次可选权限参数误用和等待停顿；同模型接管后仍需主会话督促 | 持续阶段提交，收到缺陷反馈后有可复验修复 |
| 完整路线资格 | 暂不具备 | 已回收为不完整候选，不参与本次集成选择 | cfc6b44固定复验通过；S5.1限定补齐进行中 |

## 当前不得下的结论

- 不因GPT先完成就自动选择GPT；必须等待固定候选和完整独立检查。
- 不因GLM测试数更多就认定覆盖更全面；其中旧测试和细分断言计数不同。
- Kimi工具调用格式问题不等于宿主文件权限故障，不能以此申请无边界权限。
- 共享协调仅协议fixture、真实身份未配置，不能宣传生产多设备正式作业已完成。
- 用户建飞书第九表只是结构准备事实，tableId/列/ACL未经核验，不得用生产表测试。

## 后续判定流程

1. 候选交付冻结HEAD和LINE_REPORT，完成其声明范围测试。
2. 主会话在固定commit独立跑npm、browser和对抗矩阵；未测项标NOT_RUN/EXTERNAL_GATE。
3. 未完成路线继续开发；若长期不能提供完整实现，如实列为不具备集成资格，而非将其缺失功能算低分后假称完整。
4. 选合格路线的整体基线；局部借鉴须额外审查和回归。
5. 主工作区本地集成后重新验收，不push、不触发生产CI/CD。
