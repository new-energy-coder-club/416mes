# 需求书校准记录（2026-09-25）

> 背景：`416MES开发需求书.md` 与 `dev-docs/` 均在本项目 `.gitignore` 中（本地工作文档约定），
> 故校准本身不入库。此文件放在仓库根（可跟踪），作为**校准摘要与依据**，便于入库后追溯。

## 一、本次校准的 6 处

| # | 位置 | 原文 | 校准为 | 依据 |
|---|---|---|---|---|
| 1 | §4.2 InvenTree 同步 | 承诺 CLI 镜像 + 实时写两类能力 | **已移出本期范围（代码不存在，不做）** | `inventree-sync.mjs` 文件不存在；`invPush` / `btnInvTest` / `mes416_inventree_cfg` 在当前代码中完全不存在（grep 全仓为 0）。用户原话：「InvenTree 这个技术在这个项目中删了，不需要」 |
| 2 | §4.2 架构图第二列 | `InvenTree / inventree-sync.mjs → :8001 + 实时写` | `Vercel 云函数 / /api/feishu/* → mes.newenergycoder.club` | 与 `vercel.json` 及 `api/feishu/*` 实际部署形态一致 |
| 3 | §3.10 与 §8#5 Excel sheet 数 | 「导出 9 sheet」 | **8 sheet** | `index.html:5420` 注释：「『物料台账』sheet 已移除（G3 归档流水账本）」。实测 `sheet(` 调用 9 次，其中 `sheet('物料台账')` 仅用于**导入兼容**，导出侧确为 8 个 |
| 4 | §3.7 扫码日志条数 | 「界面仅显示最近 200 条」 | 「内存保留最近 500 条，界面显示由 `SCAN_LOG_VIEW` 控制」 | `index.html:7629` `SCAN_LOG_MAX = 500`；显示条数是另一个常量 |
| 5 | §3.1 手工改数 | 「直接改库存必填调整原因，取消则还原」 | **界面不再提供直接改数入口**；修数走「盘点」或「工单冲销」 | G3 物料数量账封存（`index.html:5181`、`test/mat-archive.test.js`）。`applyManualAdjust` 保留为库函数供历史回放，页面层不暴露 |
| 6 | §9 自动化测试数 | 「108 个用例」 | **1071 个用例**（另有 6 个 browser 文件） | `npm test` 实测（本次校准时为 1071；随迭代会继续增长） |
| 7 | §3.10 标签类型表 | 表格列 7 类标签 | **8 类**：表格补第 8 行「模块区卡（zone，A4）」 | BUG-4（TASK-15，来源 `dev-docs/全量实测报告-v3.12.0.md`）。DSH 实测标签页确认 8 个类型按钮：库位码 / 容器码 / 物料码 / 物品标签 / 手册标签 / 工单码 / NEC 任务 / 模块区卡（`.zonecard`） |

## 二、同步修正的小项

- §1.3 范围表：「飞书 / InvenTree 集成」→「飞书集成（真源直写 + 离线队列）」
- §4.4 历史数据导入：删除 `node inventree-sync.mjs push` 示例命令
- §5.2 存储：InvenTree 配置键标注「已随 InvenTree 移除」
- §6 数据安全：密钥清单去掉 `inventree`

## 三、未改动的「实现超出文档」项（记案，不阻断）

| 项 | 说明 |
|---|---|
| §3.10 标签类型 | 需求书表格列 7 类，实现含第 8 类「模块区大卡」（`.zonecard`，A4） |
| §3.3 工单号唯一性 | 需求书承诺「按类型+日期+3 位流水自动递增」；**离线/多设备下会撞号**（TASK-07 的 M3，已在 v3.9.0 补可见提示，但未禁离线建单——用户拍板不挡现场作业） |
| §9 多设备并发与真事务 | 需求书标「⬜ 未实现」；实际为**范围外**（需求书 §1.3 自己列为范围外，且飞书平台无事务/条件写，真事务不可能） |
| §4.3 闲鱼 | 标「✅ 已完成」，实际是「脚本已完成、页面层被测试禁止调用」（G3 封存） |

## 四、防回流保障

InvenTree 的移除有回归测试锁死，不会再悄悄回来：
`test/align-recovery.test.js:712` `InvenTree 集成必须已被彻底移除，且不得回流`
—— 断言 `inventree-sync.mjs` / `INVENTREE-INTEGRATION.md` / `inventree-sync.config.json` 不得存在，
且 `index.html` 不得再出现 InvenTree 字样。
