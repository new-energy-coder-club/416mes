# 三线开发任务登记

共同代码提交：55d9297。三线计划见《三线独立开发与验收计划.md》，需求见《库位、容器、物品逻辑落地.md》。所有分支仅本地，禁止push及生产部署。

| 路线 | 模型/供应商内部标识 | 分支 | 工作区 | 长期会话ID |
|---|---|---|---|---|
| GLM | aicocde / glm-5.3 | feature/unique-item-glm53 | /srv/416mes/.dev-lines/glm53 | sb-bab8443d-4296-4912-8850-2d1f74d44cc9 |
| Kimi | aicocde / kimi-k3 | feature/unique-item-kimik3 | /srv/416mes/.dev-lines/kimik3 | sb-8c8643c9-c479-4a98-895d-5b594a85f0b3（接管） |
| GPT | aicocde / gpt-6-astra | feature/unique-item-gpt6 | /srv/416mes/.dev-lines/gpt6 | sb-f20361df-0fa2-4dab-959b-78700ce51e63 |

状态：GLM已交S1、GPT已交S2，主会话已进行独立定向复测；未选择胜出路线。具体证据见《三线阶段审查与缺陷台账.md》。

Kimi旧会话 `sb-f954158e-5cf2-4bba-930c-b41fcefd99fc` 因重复传可选升级参数产生校验错误而长期停顿，主会话已取消并清空其待办输入；同模型新会话接管同worktree现有文件，不开第四路线、不清理既有代码。首先验证不带升级字段的普通工具调用；真实沙箱拒绝依然遵循审批，禁止通过新会话绕过拒绝。

主会话负责：查看DEV_PROGRESS.md/LINE_REPORT.md、阶段commit及测试证据；独立复测；发回缺陷；最终择优本地集成。不会以自报完成代替验收。

基线复验：主工作区 npm test 后台任务 bash-24 返回 completed，exit code 0；工具输出记录 tests=511、pass=511、fail=0、cancelled=0、skipped=0，耗时约155秒。先前bash-22也返回0，但临时日志未保留。主会话已收集bash-24完整工具输出；各路线须自行重新跑并保存线路日志。

主会话新增《三线统一独立验收矩阵.md》，规定统一夹具、一票否决项和功能/持久化/同步/服务故障场景；已向三条路线发送只读参照通知，不允许互相查看候选代码。

本地 .git/info/exclude 已加入 /.dev-lines/，仅隐藏主工作区中worktree目录，不改变仓库受跟踪忽略规则。各worktree拥有自己的分支与index，共享Git对象库；不得跨目录修改。
