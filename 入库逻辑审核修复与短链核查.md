# ITM 入库逻辑审核与修复交付（v2.40.0，已上线）

## 背景

按你的要求分发了两个研究子代理：①ITM 入库逻辑实测审核；②物品二维码短链方案核查。另主会话直接在生产环境实测复现。本文件是①的修复交付；②的结论和待你确认事项见文末。

## 审核实测出的真实问题（逐场景跑出来的，不是猜的）

| 场景 | 旧行为 | 修复后 |
|---|---|---|
| 旧物品实物不在档案旧库位（WP-001 档案写 W01-G01，实物在别处） | verifyLegacy 提交后 LEGACY_LOCATION_CONFLICT **永久卡死**，退役也拒收 unknown，无任何出口 | 领域层新增显式覆盖开关 `confirmLegacyLocOverride`；待处理区 REJECTED 卡出现「现场确认后以实物为准重发」按钮（新 opId 重发，before 里保留旧位置供审计）；unknown 物品允许退役 |
| 容器旧库位与目标不一致 | activateContainer 同款死锁 | 同款覆盖开关 |
| 扫到未建档物品码 | 只显示英文 `items: WP-999`，无任何引导 | 中文提示「该物品码未建档：WP-999」+「以该码建档」按钮，点击自动展开建档区并把 WP-999 带入编码输入框 |
| 建档只能生成随机码 | 扫到的实物码没法用 | 建档区新增可选「编码」输入框，扫到的码直接沿用 |
| 领域错误码全英文 | REJECTED 卡显示 LEGACY_LOCATION_CONFLICT 等 | 统一中文映射（如「实物位置与档案旧库位不一致」「数据刚被更新过，请刷新后重试」），括号保留英文码便于排障 |
| 「不能重复入库或尚待核实」混为一句 | 分不清已在库还是待核实 | 拆成「物品已在库，不能重复入库」「物品是旧档案，尚待核实」 |

不变的安全边界：不带覆盖开关的旧位置冲突**依然拒绝**（防误扫）；覆盖必须现场确认；所有操作都进物品操作表留痕。

## 测试证据

- 审核子代理留下的 20 场景边界脚本转为正式回归 `test/itm-legacy-edge.test.js`（旧死锁断言更新为修复后行为）；
- 新增 UI 测试：未建档引导带入编码、覆盖重发新 opId、旧物品核实引导；
- 全量回归 **635/635 通过**，Chromium 整页通过；
- 生产 2.40.0 实测：扫 `ITM:WP-999` → 中文未建档提示 → 点引导 → 建档区展开且编码已带入 WP-999。

## 短链方案核查结论（任务②，子代理已完成）

《物品二维码短链方案.md》12 条现状断言全部属实、算法实测成立（30 万往返零碰撞），整体可行。关键发现：

1. **最大缺口**：当前新物品注册码是 `WP-` + 随机 UUID（不是数字序号），方案的结构化短码算法 `WP-(\d+)` 不命中 → 以后所有新物品只能在线查表，离线解码落空。要实施短链，建议先把物品取号改成顺序号（WP-004 往后）。
2. 二维码密度实际 42 字符恰好卡满 QR V3-M，能印在 60×40 标签上。
3. 现状扫短链 URL 会被当物料误查 → 四个扫码入口都要先接短链解析（该改法已核实可行）。
4. 落地页、/i/ 前缀、扫码统计三个待确认点，子代理建议：/i/ 可用；落在查询页物品详情卡；v1 不做统计。

## 需要你拍板后我才实施短链的决策点

1. **物品取号改顺序号**（WP-004、WP-005…）？这是短链离线解码的前提；
2. 飞书新建「物品短链」表（字段清单在方案 §6.1）——你建还是我用 API 建；
3. 已打印的 Code128 物品条形码继续并存读取（推荐）还是全部换二维码重印。
