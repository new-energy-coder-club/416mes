# 物品码二维码化与短链接方案（调研 + 设计）

> 状态：调研完成，待评审后实施
> 需求来源：物品码不再用条形码（Code128），改为二维码；二维码内容是一串**短链接**，
> 网页端生成短链字符 → 复制到乐写等打印软件 → 打印二维码 → 物品扫码一律扫这个码。
> 参考能力：草料二维码的「链接压缩（活码短链）」、飞书多维表的「多行重定位（行级链接）」。

---

## 1. 需求拆解（对原话的逐句落实）

| 需求原话 | 落地含义 |
| --- | --- |
| 物品码不使用条形码，改为二维码 | 实物标签从 Code128 改为 QR；扫码端已有 jsQR，无需新解码库 |
| 二维码针对物品码做单独需求 | 只有 ITM 一类换内容格式；LOC/CTN/MAT/WIP/NEC/MAN 保持「前缀:编码」机读串不变 |
| link 首先回调网页端/飞书多维表 | 扫码先命中我们自己的后端 `https://mes.newenergycoder.club/i/xxxxxxxx`；映射关系**记录在飞书多维表**里 |
| 然后压缩转跳到对应行 | 后端 302 转跳到「对应行」：默认是网页端的物品详情（查询页定位到该物品），可选转飞书记录行 |
| 最后八位是 link 定位位置 | 短链尾部固定 8 位字符作为定位码 |
| 中间转跳经过飞书/草料压缩/后端记录 | 转跳由后端完成，映射持久化在飞书表（相当于草料活码的"后台记录"角色） |
| 最后对应到物品码 | 8 位短码 ↔ WP-xxx 物品码一一对应 |
| 网页端生成短链接字符，复制到乐写打印 | 标签打印页新增「物品短链」面板：生成 + 复制 + 批量导出 |
| 物品码扫描都扫这个二维码 | 全部扫码入口（扫码工作台 / 物品作业 / 查询页 / 手输）都识别短链二维码 |

---

## 2. 项目现状盘点（与方案相关的代码事实）

| 事实 | 位置 |
| --- | --- |
| 物品码格式 `WP-001`，主档在飞书「物品」表 `tblpq4NZYc0H1Npk` | `lib/feishu-api.js:54` |
| 标签打印页：选类型→勾选→预览 60×40mm→导出 PDF；`itm` 类型=物品标签 | `index.html:1324` `#tab-label`、`TYPES.itm`（1817 行）、`labelHtml()`（3790 行） |
| 目前所有标签二维码内容 = `前缀+编码`（物品是 `ITM:WP-001`），内嵌 qrcode-generator 离线生成 | `index.html:3800`、`qrSvg()`（2698 行） |
| 扫码统一入口 `handleScan(raw)` 支持 7 种前缀 + 无前缀自动识别 | `index.html:5665` |
| 物品作业三码状态机 `ItemScan.parse()`：识别 `LOC|CTN|ITM` 前缀，ITM 步骤接受裸 `WP-` 码 | `lib/item-scan.js:11` |
| 相机浮层每帧「先 jsQR 解二维码，再 ZXing 解一维码」，识别后弹确认卡 | `lib/scan-camera.js:18-33`、`lib/item-ui.js:12-18` |
| 唯一物品查询页有公开方法 `itemPage.queryScan(text)`（填入并查询）——深链定位直接复用 | `lib/item-ui.js:103、132` |
| 页签深链已存在：`index.html#tab名`（`history.replaceState`） | `index.html:2705` |
| 云端后端 = Vercel Serverless `api/feishu/*.js`；本地 `feishu-server.mjs` 是薄适配层，**require 同一份 handler** | `feishu-server.mjs` 头注释 |
| 生产域名 `https://mes.newenergycoder.club`（Vercel hkg1）；file:// 离线时前端自动指向该域名 | `index.html:3696、5974` |
| 飞书写入全部走 `lib/feishu-api.js` 共用层（TABLE_DEFS 字段映射、业务键去重、batchCreate/batchUpdate） | `lib/feishu-api.js` |
| 已有「物品操作」第 9 表的接入先例：TABLES 里按环境变量/内置 ID 追加 | `lib/feishu-api.js:59-62` |

**结论：所有需要的积木都在**——二维码生成、二维码解码、域名、后端框架、飞书写入层、查询页定位方法。本方案是纯增量，不改任何现有数据语义。

## 3. 外部能力调研结论

### 3.1 草料二维码（参考对象）

- **批量生成标签**：把网址/编号等数据直接编码进二维码，扫码结果就是数据本身，"常配合自有系统的 App 或专用扫码设备，识别编号后进入系统里的业务流程"。内容生成后不可改。
- **批量生成活码**：二维码图案不变、后台内容随时可改，支持 API 对接自动生码，短链域名是草料的（qr61.cn 等）。
- 对我们的启示：**"码里只放短链接、真目标在后台"正是草料活码的核心模式**。但我们已有域名 + Vercel + 飞书后端，自建短链零额外成本、无第三方依赖、离线可解码（见 §5），草料账号/付费/API 配额都不需要。草料可作为**备选 B**（见 §10 对比）。

### 3.2 飞书多维表行级链接（多行重定位）

- **已实测确认**（浏览器只读打开本 Base）：表/视图级链接格式为
  `https://trinacli.feishu.cn/base/NpWBb0RXYayqfosYqnScWdlDnMb?table=tblpq4NZYc0H1Npk&view=vewrqBHzUt`
  （打开时飞书自动补写了 `table=` 和 `view=` 参数）。
- **行级定位**（高置信度，实施第一步复核）：在上面的 URL 追加 `&record={record_id}`（rec 开头）。
  服务端 `listRecordsEx()` 返回里本就带 `record_id`（`lib/feishu-api.js:471`），生成时顺手用真实 ID 验证一次即可定稿。
- 注意：飞书行链接打开需要登录 + Base 权限，**只适合管理员**；一线扫码的默认目标必须是网页端（无需登录）。

### 3.3 乐写等打印软件

通用流程：新建标签 → 插入二维码元素 → 内容框粘贴字符串 → 软件自己生成二维码 → 打印。
所以网页端**只需要产出并复制一串字符**（短链接），不需要替乐写生成二维码图片。
批量场景：多数标签软件支持 Excel/CSV 导入逐行生码，面板提供「复制全部 / 导出 CSV」即可衔接。

---

## 4. 推荐方案总览

```
打印时（管理员电脑）
  标签打印页「物品短链」面板
    → 本地算出 8 位短码（离线可用）→ 拼出短链接
    → 在线时 POST /api/feishu/item-link 登记到飞书「物品短链」表（幂等）
    → 复制字符串 → 粘贴进乐写 → 打印二维码

扫码时（一线手机 / 扫码枪 / 微信或飞书内置浏览器）
  二维码内容： https://mes.newenergycoder.club/i/K7X2M9Q4   ← 最后 8 位=定位码
        │
        ├─ A. 在本系统相机/扫码枪里扫：前端本地解码 8 位短码 → WP-001
        │     （离线可用，不走网络，直接当成 ITM:WP-001 走现有流程）
        │
        └─ B. 用微信/系统相机等任意 App 扫：打开短链接
              → Vercel 端点 GET /i/{8位}（= 回调网页端）
              → 302 转跳到网页端物品行：
                  https://mes.newenergycoder.club/index.html#item/WP-001
              → 网页端启动时识别 #item/ 深链 → 自动进「库位/容器/物品查询」页
                并调用 itemPage.queryScan() 定位到该物品（= 对应行）
              （管理员可选 &to=feishu 变体 → 302 到飞书记录行链接）
```

链路完全满足需求句式：**回调网页端（`/i/` 端点）→ 压缩转跳（302）→ 对应行（查询页物品详情 / 飞书记录行）→ 八位定位 → 物品码 WP-xxx**。

### 为什么 8 位短码要"结构化"而不是纯随机

| 方案 | 在线转跳 | 离线扫码 | 重复生成 | 说明 |
| --- | --- | --- | --- | --- |
| 纯随机 8 位 + 查表 | ✅ | ❌ 必须联网查飞书 | ❌ 每次不一样，重印换码 | 仓库离线场景（本系统 IndexedDB 离线优先）不可接受 |
| **结构化：序号+校验 8 位（推荐）** | ✅ | ✅ 本地解码校验 | ✅ 同一物品永远是同一短码，重印不换码 | 转跳端点即使飞书宕机也能 302（本地解码），飞书表只承担"记录/审计"角色 |

---

## 5. 八位短码设计（`lib/item-link.js`，浏览器/Node 共用 UMD）

- **字符集**：Crockford Base32（`0-9 A-H J-K M-N P-T V-Z`，去掉易混淆的 I/L/O/U），打印安全、口头报读安全、大小写不敏感解码。
- **结构**：8 位 = 5 位数据 + 3 位校验。
  - 数据 25 bit：物品序号 serial（`WP-001` → 1），容量 3355 万，够用到 `WP-999999`；
    为不让短码看起来连号：`scrambled = (serial * 8121 + 104729) mod 2^25`（乘奇数保证可逆）。
  - 校验 15 bit：`FNV1a32('416mes:' + serial) >>> 17`，纯算术、同步、无 crypto 依赖。
  - `encode/decode` 双向；校验不过 = 不是我们的物品码，转去走原有扫码分支。
- **容量/碰撞**：结构化编码天然零碰撞（同一序号唯一编码）；校验位防"扫糊一位误中别的物品"（误判率 1/32768）。
- **非 WP-数字格式的物品码**（历史遗留如 `I-A`）：退化为随机 8 位 + 飞书表查表解码；生成面板对此类码标注「仅在线可扫」。
- 示例（算法定稿后由测试锁死期望值）：
  `WP-001 → K7X2M9Q4 → https://mes.newenergycoder.club/i/K7X2M9Q4`

**二维码密度核算**（决定打印质量）：
短链全长 41 字符（字节模式，容错 M）→ QR 版本 3（29×29 模块）。
60×40 标签上二维码区 22mm → 每模块约 0.66mm ≈ 汉印 N41（203dpi）5 个打印点，余量充足。
对比直接放飞书记录长链（110+ 字符 → 版本 6+，模块 0.46mm）——这正是草料/需求强调"压缩"的原因。

---

## 6. 后端改动（Vercel + 本地同源）

### 6.1 新增飞书第 10 张表「物品短链」

| 列 | 类型 | 说明 |
| --- | --- | --- |
| 短码 | 文本（业务键） | 8 位短码 |
| 物品码 | 文本 | WP-001 |
| 物品名称 | 文本 | 冗余给人看 |
| 短链接 | 文本 | 完整 URL，方便在飞书里直接复制 |
| 创建时间 | 创建时间 | |
| 备注 | 文本 | 可选 |

- 接入方式照抄「物品操作」先例：`TABLES.itemLinks = process.env.FEISHU_ITEM_LINKS_TABLE || 'tbl…'`。
- 建表两种方式：① 用开放 API `POST /bitable/v1/apps/{app}/tables` 写个一次性脚本；② 手动在 Base 里建（给字段清单）。建完把 tbl  ID 写进环境变量/代码。

### 6.2 新增端点

| 端点 | 方法 | 行为 |
| --- | --- | --- |
| `/i/{8位短码}` | GET | **转跳端点**。先本地解码：成功→302 到 `index.html#item/WP-xxx`（不依赖飞书，飞书宕机也能跳）；解码失败→查「物品短链」表（随机码兜底）→302 到表内目标；都没有→404 页提示联系管理员。支持 `?to=feishu` 改跳飞书记录行。 |
| `/api/feishu/item-link` | POST | body `{items:[{code,name}]}` → 逐条算短码、按业务键 upsert 进「物品短链」表（幂等）→ 返回 `[{code, short, link, recorded:true}]`。 |
| `/api/feishu/item-link?code=` | GET | 解析接口：扫码端本地解码失败时的在线兜底；也可批量拉全表做本地缓存。 |

### 6.3 路由接线

- `vercel.json` 加 rewrite：`{"source": "/i/(.*)", "destination": "/api/item-link/$1"}`，
  新增动态函数 `api/item-link/[code].js`（现有 regions 配置不动）。
- `feishu-server.mjs` 的 ROUTES 增加：`/api/feishu/item-link` + `/i/:code` 前缀路由 → require 同一份 handler（保持"本地与云端同一段代码"的铁律）。
- handler 复用 `lib/feishu-api.js` 的 tenantToken/listRecordsEx/batchCreate，不新写飞书访问代码。

---

## 7. 前端改动

### 7.1 标签打印页：新增「物品短链二维码」面板（`#tab-label` 内，仅在类型=物品标签时显示）

位置：`.label-layout` 之后、`danger-zone` 之前（`index.html:1351-1368` 之间插入）。

```
┌─ 物品二维码短链（复制到乐写等软件打印 QR） ─────────────┐
│ 已选 3 件物品   [生成短链并登记飞书]  [复制全部] [导出CSV] │
│ ┌────────┬────────┬──────────┬─────────────────────┐ │
│ │ 物品码  │ 名称    │ 8位短码   │ 短链接               │ │
│ │ WP-001 │ 电烙铁  │ K7X2M9Q4 │ mes.newenergycoder… │ │
│ │        │        │          │ [复制] [预览二维码]    │ │
│ └────────┴────────┴──────────┴─────────────────────┘ │
│ 状态：✓ 已登记飞书 / ⚠ 离线本地生成（联网后自动登记）      │
└───────────────────────────────────────────────────────┘
```

- 数据源直接复用本页选择集 `sel.itm`（`recordsOf('itm')` = state.items）。
- 短码**本地计算**（`ItemLink.fromItemCode`），断网也能生成、复制；
  在线时顺手 POST `/api/feishu/item-link` 登记，回显登记状态。
- 「预览二维码」用现有 `qrSvg()` 当场渲染短链二维码，打印前肉眼核对。
- 「复制全部」输出每行一条短链接（乐写批量导入/逐条粘贴都友好）；「导出 CSV」衔接 Excel 导入型打印软件。

### 7.2 物品标签自身的 PDF 二维码换内容

`labelHtml('itm', r)`（`index.html:3790`）与 PDF 导出处（约 3978/4044 行）的二维码内容，
物品类型从 `ITM:WP-001` 换成短链接（本地算，无需联网）——
这样**网页端 PDF 打印和乐写打印出的是同一个码**，不会出现两套并行标签。
人读区仍印 `WP-001`，可在二维码下加一行 8 位短码小字。

### 7.3 扫码识别（4 个入口，共用一个解析函数）

`lib/item-link.js` 暴露 `parseScanText(raw) → {code:'WP-001'} | null`：
识别 `任意主机 + /i/8位` 的 URL（生产域名、localhost、局域网 IP 都能用），本地解码+校验。

| 入口 | 位置 | 改法 |
| --- | --- | --- |
| 扫码工作台 | `index.html:5665` `handleScan()` | 前缀匹配前先试 `parseScanText`，命中即按 `ITM:` 分支走 `scanItem()` |
| 物品作业三码填入 | `lib/item-scan.js:11` `parse()` | 同上，命中返回 `{type:'ITM', code}`，后续类型/状态校验全部复用 |
| 相机确认卡文案 | `lib/item-ui.js:12-18` `describeHit/describeWorkHit` | 命中短链时显示「物品 WP-001 · 电烙铁（物品二维码）」，类型校验照常 |
| 查询页手输/扫码枪 | `lib/item-ui.js:39` `search()` | 手输框粘贴整条短链也能查（解析后按物品码精确查） |

相机解码层 `lib/scan-camera.js` **零改动**（jsQR 本来就解二维码）。

### 7.4 网页端「对应行」深链

启动时解析 `location.hash`：
- 现有 `#tab名` 逻辑不动；
- 新增 `#item/WP-001` → `goTab('items')` → `mountItemPage()` 后 `itemPage.queryScan('ITM:WP-001')`
  → 用户扫码（微信/相机 App）落地直接看到该物品详情卡，即"对应行"。

---

## 8. 兼容与迁移

| 旧物 | 处置 |
| --- | --- |
| 已贴出的 Code128 条形码标签 | **继续可读**（ZXing 一维码链路保留），与新二维码并存，逐步换贴 |
| 网页端已打印的 `ITM:WP-001` 二维码 | 继续可读（前缀分支不动） |
| 手输习惯 | `ITM:WP-001`、裸 `WP-001`（作业页 ITM 步骤）照旧可用 |
| 库存/操作/同步协议 | 零改动；短链只是"新的码面内容"，落到系统里还是同一个物品码 |

---

## 9. 测试计划（沿用仓库现有测试模式）

1. `test/item-link.test.js`（node:test）：
   - encode/decode 往返（serial 0/1/999/上限 2^25-1）；
   - 期望值锁定（防算法被无意改动导致已印标签失效）；
   - 篡改一位字符→校验拒绝；非法字符→拒绝；大小写混合→可解；
   - `parseScanText`：生产 URL / localhost / 局域网 IP / 非本系统 URL 不误判。
2. 端点测试：照 `test/feishu-api.test.js` 的 `FEISHU_HOST` mock 起假服务，
   验 `/api/feishu/item-link` 幂等（POST 两次不产生重复行）、`/i/` 转跳 302 目标正确、未知码 404。
3. DOM 测试：照 `test/item-ui.test.js` 的 linkedom 模式验面板渲染、复制内容格式；
   `item-scan` 测试加「扫短链填入 ITM 步骤」用例。
4. 现场验收（需打印机，网页端验不了出纸）：
   乐写粘贴短链 → 打印一张 → ① 手机系统相机扫 → 应打开物品详情页；② MES 相机扫 → 确认卡显示「物品 WP-001…」→ 确定填入；③ 断网手机扫码枪扫 → 应照常识别（离线解码）。

## 10. 备选 B：直接用草料活码（不推荐，留档对比）

| | 自建短链（本方案） | 草料活码 |
| --- | --- | --- |
| 成本 | 0（现有 Vercel+域名） | API/活码量需付费版 |
| 依赖 | 无新增 | 账号、配额、qr61.cn 可用性、数据出仓 |
| 离线扫码 | ✅ 本地解码 | ❌ 必须联网 |
| 扫码统计看板 | 无（v2 可加计数列） | ✅ 现成 |
| 落地工作量 | 本文 §6-§7 | 需研究其 API + 账号配置，码内容仍要逐条对接 |

结论：草料的**模式**值得抄（短链+后台记录），**服务**不必买。若将来想要扫码统计大屏，再评估在转跳端点加飞书写计数（非阻塞、失败不碍转跳）。

---

## 11. 实施顺序（建议）

1. 飞书 Base 新建「物品短链」表（§6.1 字段清单）→ 拿到 tbl  ID；
   顺带用真实 record_id 复核飞书行级链接格式（§3.2）。
2. `lib/item-link.js` + 单测（算法先锁死，之后永不许改）。
3. 端点三件套：`api/feishu/item-link.js`、`api/item-link/[code].js`、`vercel.json` rewrite + `feishu-server.mjs` 路由。
4. 标签打印页「物品短链」面板。
5. 扫码 4 入口接 `parseScanText` + `#item/` 深链。
6. itm 标签二维码换短链内容（网页 PDF 与乐写一致）。
7. 部署 → 用 WP-001~003 全链路实测（生成→乐写打印→扫码落地）→ 文档/README/版本号。

## 12. 待你确认的 3 件事

1. **短链域名路径**：`/i/` 这个前缀可以吗？（也可 `/q/`、`/s/`；越短码密度越低）
2. **扫码默认落地页**：一线人员用微信/相机扫出来，落在「库位/容器/物品查询」物品详情卡，对吗？还是想落在别的页面（如专门的物品公开页）？
3. **要不要扫码统计**（谁在什么时候扫了哪件物品）：v1 不做（转跳零写入、最快最稳）；要的话 v2 在转跳端点加飞书计数列，或评估草料。
