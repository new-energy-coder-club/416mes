# 416MES 分阶段开发验收报告

> 基线：`416MES开发需求书.md` v1.2 ｜ `416MES分阶段开发计划.md`（迁移包校准版）
> 结论日期：2026-09-15 ｜ 最终提交：见文末「提交记录」

## 一、总体结论

按分阶段计划完成 Phase 0 – Phase 6，需求书 8 项验收标准中 **7 项通过**，1 项（飞书真机连通）**代码路径已验证、真机连通待用户在装有 lark-cli 的机器上确认**。

| 指标 | 结果 |
|---|---|
| 自动化测试 | **108 个用例全部通过**（`npm test`） |
| 阶段验收清单 | 各阶段累计 **172 项检查全部通过** |
| 未捕获异常 | 浏览器全流程实测 **0 个** |
| 版本库密钥 | **0 个**（配置文件全部 gitignore） |
| 需求书验收标准 | 8 项中 **7 ✅ / 1 ⏸** |

## 二、逐阶段结果

| 阶段 | 目标 | 状态 | 关键产出 | 验收 |
|---|---|---|---|---|
| **Phase 0** | 版本对齐与基线收口 | ✅ | 主目录与迁移包对齐（index.html 2016 行）、密钥隔离、统一开发命令、Git 基线 | 34 项 |
| **Phase 1** | 核心库存逻辑修复与可测试化 | ✅ | `mes-core.js` 纯逻辑模块；修复重复物料负库存、盘点 NaN 静默转 0；库存写入口唯一化 | 34 项 |
| **Phase 2** | 扫码出入库闭环补全 | ✅ | 7 种扫码前缀 + 无前缀自动识别；`MAT:` 未命中回查最近扫码/流水/工单；结构化扫码历史 | 35 项 |
| **Phase 3** | 工单管理增强 | ✅ | 四态状态模型、部分执行、取消/冲销、工单管理页与详情页、执行历史、关联流水 | 52 项 |
| **Phase 4** | 30S 精确定位收口 | ✅ | 库位解析到架-层-位；定位质量分级；定位抽查工具（CLI + 网页） | 35 项 |
| **Phase 5** | 闲鱼数据同步 | ✅ | 字段映射 + 合并规则 + CSV/JSON 导入 + 取数命令适配 | 21 项 |
| **Phase 6** | 最终验收与文档收口 | ✅ | Excel 流水往返修复；需求书状态更新；本报告 | 见下节 |

## 三、需求书 8 项验收标准

| # | 验收项 | 结果 | 依据 |
|---|---|---|---|
| 1 | 离线启动 | ✅ | `file://` 直接打开，种子数据自动载入，7 个页签均可用 |
| 2 | 扫码闭环 | ✅ | 7 种前缀实测通过；重复物料多行（3+4 超库存 5）被拦截为「库存不足（现 5，需 7）」，库存不变化、不写流水 |
| 3 | 审计 | ✅ | 回放校验链式余量一致；导出审计 JSON 含 seq / 时间 / 设备 ID |
| 4 | 打印 | ✅ | 七类标签、60×40 与 A4 版面、二维码机读串（本阶段未改动，回归通过） |
| 5 | Excel 双向 | ✅ | **本阶段修复**：导出的 9 个 sheet 原样导入后，物料与流水逐字段一致（含流水号/余量/设备），`txnSeq` 与回放校验同步保持；台账-only 导入按差异补写审计流水 |
| 6 | 飞书闭环 | ⏸ | 8 个集成用例（含故障路径）全绿；push 幂等、字段映射、错误处理均已验证。**真机连通需在有 lark-cli 的机器上执行 `status` / `pull`**；且工单表缺 3 列，启用前须补齐 |
| 7 | 盘点 | ✅ | 盘亏/盘盈/账实相符均写流水；空值与非数字被拒绝且库存不变 |
| 8 | NEC 工单 | ✅ | 状态流转、`NEC:` 扫码详情、关联物料库存联动（本阶段未改动，回归通过） |
| 9 | 30 S 定位（最终目标） | ✅ | 位置解析到架-层-位/工位格/开放区块；容器解析到所在库位；已领出零件回查最近带位置的扫码；抽查 10 件 100% 可定位，查询耗时 < 10 ms |

## 四、验收过程中发现并修复的真实缺陷

按发现顺序，全部在对应阶段修复并补了回归测试：

| # | 缺陷 | 后果 | 阶段 |
|---|---|---|---|
| 1 | `feishu-server.mjs` 缺配置时 `process.exit(1)` | 仓库不带密钥时 `npm run dev` 直接退出，无法离线启动 | 0 |
| 2 | 无配置时 `loadConfig()` 的 exit 无法被 catch | 静态服务起不来 | 0 |
| 3 | 工单执行**逐行**校验库存，未按物料汇总 | 同一物料多行可扣成**负库存** | 1 |
| 4 | 盘点 `parseFloat(...) \|\| 0` | 空输入把库存**静默清零** | 1 |
| 5 | 后续无位置的扫码覆盖早先的位置线索 | 30S 兜底线索被冲掉，退化为「无记录」 | 2 |
| 6 | `larkJson()` 未创建 `.build/` 目录 | 新检出时 `push` 与**直写飞书**全部崩溃 | 飞书 |
| 7 | `listAll()` 吞掉 lark-cli 报错返回空数组 | 拉取失败返回 HTTP 200 + 空表，页面**误报 🟢 已连接** | 飞书 |
| 8 | CLI 异步错误抛满屏堆栈 | 排错困难 | 飞书 |
| 9 | 切回「物料台账」页签不重渲染 | 表格可能显示与 state 不符的旧数据 | 4 |
| 10 | 定位抽查把 AUDIT 记录写入扫码历史 | 污染「最近扫码」时间与 30S 回查线索 | 4 |
| 11 | Excel 导入忽略「库存流水」sheet、库存变化不写流水 | 验收标准 5 不通过；往返丢流水 | 6 |

其中 #3、#4、#7、#11 会直接导致**数据错误或误判**，是本次开发最重要的修复。

## 五、可重复执行的验证方式

```bash
cd /srv/416mes

# 全量自动化测试（108 个用例：核心规则 + 飞书集成）
npm test

# 30S 定位抽查（验收标准 9，命令行可复现）
node scripts/locate-audit.mjs --seed-data
node scripts/locate-audit.mjs 416MES_备份.json 10 42   # 指定 seed，抽样可复现

# 飞书同步（无需凭据，用仓库自带的假 lark-cli 跑真实代码路径）
node --test test/feishu-sync.test.mjs

# 启动
npm run dev      # 真源服务，http://localhost:8000
```

浏览器人工验收要点见 `README.md` 各阶段章节。

## 六、遗留项与风险

| 项 | 说明 | 建议 |
|---|---|---|
| 飞书真机连通 | 代码路径已验证，但从未连过真实飞书服务器 | 在有 lark-cli 的机器跑 `node feishu-sync.mjs status`，见 README 排错表 |
| 飞书工单表缺 3 列 | 缺 `执行数量`/`执行批次`/`冲销记录`，直接启用会导致往返后**重复扣减** | 启用前补齐，步骤见 README；`test/feishu-sync.test.mjs` 有绊线测试守护 |
| 状态单选缺「部分执行」 | 飞书「状态」列若无该选项，写入会被拒绝 | 在飞书表里补该选项 |
| `api/feishu-sync.js` 硬编码 base_token | 不足以单独访问（仍需 app secret），但与「配置文件不含密钥」的说法不一致 | 建议改为读环境变量 |
| 多设备并发 | 仍是 localStorage 单写，多人同时操作会互相覆盖 | Roadmap 项，需先定真源方案 |
| 闲鱼取数 | 合并逻辑已完成并单测，但未连真实闲鱼 | 自行接入取数命令，见 README |
| `index.html` 非单文件 | 依赖 `mes-core.js` / `jsqr.min.js` / `nec-gallery.js` | 拷贝时整目录复制；缺文件时有红色提示而非静默出错 |

## 七、提交记录

```
67f2bcd  Phase 3  工单管理增强
a0bfb64  Phase 4  30S 精确定位收口
5513022  Phase 5  闲鱼数据同步
<最终>   Phase 6  Excel 流水往返修复 + 文档收口
1fb0c0a  飞书延后决策与缺口守卫
e08171a  飞书读取失败不再伪装成成功
b24dca5  工单与台账联动可见性
add2371  Phase 2  扫码闭环补全
2cbc570  Phase 1  核心库存逻辑与两个 bug 修复
7eda7a5  Phase 0  版本对齐与基线收口
```

每个阶段一个独立提交，可单独回滚。
