# 发布后浏览器验收报告（不含实物扫码）

## 环境
- 生产URL：https://mes.newenergycoder.club/index.html
- GitHub Actions #128对ac5b06b部署成功；实际页面已加载新ITM模块，APP_VERSION=2.34.0。
- 工具：ego-browser真实Chromium，1024×681；自动化本地另有390×844浏览器回归。
- 主动业务验证期间安装页面fetch写保护，拦截非GET；未点击物料/WIP写操作、未创建生产ITM、未改飞书。

## 结果

| 用例 | 结果 | 证据 |
|---|---|---|
| 新导航/资源 | 通过 | 查询、ITM作业按钮及12个section存在；item-*、ZXing资源均有decoded bytes，无page error |
| 飞书只读启动 | 通过 | 页面显示飞书真源已连接；读取到items=3/locations=189/containers=122/materials=5 |
| 旧ITM兼容 | 通过 | ITM:WP-001显示unknown、数量1、版本0、旧定位W01-G01，不冒充在库 |
| LOC查询 | 通过 | LOC:C-01-01-01显示容器数1、在库单件0，包含SC4-001 |
| LOC→CTN下钻 | 通过 | 点击SC4-001显示其当前库位C-01-01-01和在库单件0 |
| 名称模糊搜索 | 通过 | “万用表”返回items WP-002候选 |
| 不存在码 | 通过 | ITM:NONEXISTENT-VERIFY明确“该类型未找到编码” |
| 错序扫码 | 通过 | 在入库默认行先输入ITM:WP-001，提示当前请扫描LOC，确认按钮禁用 |
| 旧LOC状态阻断 | 通过 | 输入LOC:C-01-01-01提示库位未启用，未填写下游 |
| 正式接口门禁 | 通过 | GET item-operation?opId=read-only-verification 返回401 UNAUTHENTICATED |
| 数据副作用检查 | 通过 | 当前outbox计数0；未确认操作未入队 |
| 页面异常 | 通过 | window error收集为空 |
| 手机390px | 通过（非硬件） | 实际ego浏览器切移动视口，操作表、当前步骤、确认禁用状态可见；全部菜单可展开 |
| 新行/草稿/刷新恢复 | 通过（空步骤草稿） | 新建第二行，点击保存显示本机保存；刷新后恢复为2行，提示原opId不自动重发；未伪造已启用实物 |
| 旧流水/资源/工单/标签 | 导航冒烟通过 | 实际点击切换，未编辑/执行工单/打印；标签页183个选择框，pageErrors为空，outbox仍0 |
| 相机/实物条码 | 暂缓 | 按用户要求后续人工验证 |

## 安全观察
页面启动后的同步轮询使用POST `/api/feishu/incremental`，被本轮页面写保护拦截。这些是同步读协议的POST，不代表系统试图改库存；但因此本轮只读业务验证期间增量同步没有继续运行。页面初次打开在保护正确安装前已完成一次正常启动拉取；没有手动触发写业务。更严格的后续验收应使用代理按API语义放行只读POST、仅拦upsert/delete/stock/item-operation POST。

## 未执行或不通过生产门槛
- 正式ITM写入按设计返回401；真实认证和持久共享协调尚未部署。
- 用户新建飞书操作表的Table ID、字段类型/选项/ACL尚未核验。
- 旧LOC/CTN均为unknown，正式入库前需管理员核实启用。
- 未测试真实相机、扫码枪、打印标签或实物Code128。

## 用户后续人工扫码建议
1. 使用测试环境或已配置协调的受控账号；不要在未核实库位/容器上直接发货。
2. 先扫/输LOC二维码，再CTN二维码，再唯一ITM Code128；确认页面回显三码和数量1。
3. 连续扫同物品不应新增第二行；切行和重扫应清下游。
4. 权限拒绝、相机拒绝、弱网时命令不得显示已完成；按原opId查询。
5. 测试入库后查询ITM→CTN→LOC，再出库确认当前位置清空但历史保留。
