# 库位、容器、物品逻辑落地

> 修订版：以现有 IndexedDB＋飞书双向同步为基础的最小改造方案。
>
> 本版取代上一版“先上 PostgreSQL、切换库存真源”的默认路线。用户确认的是三码均对应唯一实体，不是要求重建整个库存系统。
>
> 本文基于前轮静态代码检查和多模型讨论，并按用户最新确认的部署方式修订：GitHub 提交触发 Vercel 自动 CI/CD；结合现有 `api/feishu/*.js`，生产 API 按 Vercel Functions 的执行模型设计，不假设固定常驻单进程。本次仅更新文档，未修改业务代码、数据库、飞书表或生产数据，未启动服务或执行测试。Vercel 项目具体配置、飞书实际列及权限仍待授权核验。

## 1. 直接回答三个问题

### 1.1 网页端有必要上 PostgreSQL 吗？

**没有必要为了本次关系、扫码和查询功能替换网页端 IndexedDB。** PostgreSQL 即使采用，也是服务端共享存储，不是浏览器本地存储的替代品。上一版把服务端并发仲裁选型过早变成业务开发前提，扩大了范围。

本版继续使用：

```text
网页 state／界面
  ↕ 本机持久化和草稿
IndexedDB mes416-state（现有 v1、七个 store）
  ↕ outbox＋现有 /api/feishu/*
飞书现有八表（扩必要字段）＋物品操作表（正式作业阶段新增）
  ↕ 全量／增量拉取
其他设备自己的 IndexedDB／网页
```

IndexedDB 能提供本机事务，保存关系、档案、草稿、待提交命令、快照和冲突，完全可以继续使用。它不能让 A 手机和 B 电脑共享一把事务锁。**“本地保存够用”与“多设备同时处置一件物品的仲裁够用”是不同问题。**

本版不安装 PostgreSQL，也不把它列为查询和扫码原型前置。生产部署已明确为Vercel Functions，正式多端写入所需的共享协调能力按第8章选型验证；是否使用事务存储取决于受控字段权威边界，不能以“用的人少”“最后写入覆盖”当作安全证明。

### 1.2 飞书新增表还是改现有表？

明确建议：

- **物品主档继续用现有 `items → 物品` 表**，不再新建第二张物品表；增容器、状态、版本、最后操作 ID。
- **容器继续用现有 `containers → 容器` 表**，保留当前库位，增版本、最后操作 ID及状态。
- **库位继续用现有 `locations → 库位` 表**，保留原字段，增状态。
- 其他五张表保留原业务，不因这次 ITM 作业强制改写 MAT 数量账。
- **正式作业阶段新增一张 `itemOperations → 物品操作` 表**，保存入库、出库、换箱、容器移库及恢复信息。
- 不复用旧 `transactions → 库存流水` 承载 ITM 操作，因为其 `seq/matCode/delta/balance` 有既定 MAT 回放语义。

只读查询阶段可以只扩旧表字段；需要可追溯的正式作业时再启用第九表。缺操作表时禁用正式作业，而不是假装操作已完成。

### 1.3 如何与现有项目衔接？

不更换数据库名，不推倒同步框架，不自动把全部 MAT 拆为 ITM，不新增一套普通档案 CRUD。

复用已有资源编辑、码生成、标签、`state.items/containers/locations`、`fsPushRecord` 普通字段同步、`fsPullState/fsIncrementalSync`、IDB outbox、对账和冲突 UI。新增的是：

1. 关系字段及校验；
2. 只读查询入口；
3. 严格三码作业状态机和一维条码适配器；
4. 受控 ITM 操作接口及操作日志；
5. 全量/增量同步对受控字段的专门处理；
6. 明确的异常恢复和多端写入限制。

---

## 2. 代码现状：本版以这些事实为起点

### 2.1 本机持久化不是空白

| 位置 | 已核实事实 | 改造约束 |
|---|---|---|
| `lib/store.js:22–25` | v1，七个 store | 增字段/逻辑表不等于升级 object store |
| `index.html:initLocalStore`，约2226行 | 实际打开 `mes416-state` | 不改成模块默认 `mes416-store`，避免出现空库 |
| `initLocalStore` | 从 `syncMeta['state-v1']` 恢复完整 state | 不能只写 records 却不接启动恢复 |
| `persistStateToIdb`，约2274行 | records 按硬编码表清单重建，`replaceAll`；流水独立保存 | 未加入清单的操作缓存会被下次保存清掉 |
| `persistStateToIdb` | 同一事务包含 records/transactions/baselines/syncMeta，不含 outbox | 新确认流程不能假定状态和命令已原子持久化 |
| `initLocalStore` dirty 分支 | IDB 落后时采用 LS 状态并继承 IDB 的 `__base` | 新命令恢复须避免旧 LS 覆盖已持久的命令和确认结果 |
| `lib/outbox.js:createAsync` | 已有 IDB 队列、失败计数和 needs_attention | 复用，不再造一个无关队列 |
| `lib/outbox.js:project` | 已有纯函数，但网页未接入调用 | 必须接显示链并验证，不能声称当前已实现 |

七个 store 的职责：

- `records([table,key])`：业务记录镜像；
- `transactions(seq)`：旧 MAT 流水；
- `outbox(id)`：待提交操作；
- `baselines(key)`：当前主要用于账本检查点，不等于网页三方合并基线；
- `conflicts(key)`：冲突；
- `syncMeta(key)`：完整 state、同步元信息；
- `deletionJournal(key)`：删除凭据。

网页三方合并基线主要是 `state.__base`。不要误把它与 `baselines` store 混同。

### 2.2 现有双向同步

```text
普通档案上行：资源编辑／生成器 → state → save
            → fsPushRecord／fsPush → fsEnqueue → fsExecItem
            → /api/feishu/upsert → upsertRecords → 飞书

全量下行：fsBoot → fsPullState → /api/feishu/state → pullState
        → fsMerge → CORE.mergeRemote → save

增量下行：fsIncrementalSync → /api/feishu/incremental
        → probe/pull/census → fsThreeWayApply／TWM.planMerge
        → 基线、冲突、水位和 state 保存
```

**全量目前不是与增量相同的三方合并。** 两条路径都要对新受控字段补处理。只在 `TABLE_DEFS` 加一列不能完成业务接线。

`feishu-server.mjs` 是云端 handlers 的薄适配；同一份逻辑不代表同一写入实例，必须区分公网多实例和本地常驻服务。

---

## 3. 业务范围冻结：不再扩大到全系统迁移

### 3.1 三码的语义

- LOC：一个真实库位；
- CTN：一个真实容器；
- ITM：一个真实单件；
- 同一 ITM 不存在可编辑 `qty`，行数量固定1；批次件数是去重后的物品行数。

现有 MAT 业务、工单、库存数量与旧库存流水**不是本次默认迁移对象**。ITM 可选关联 `materialCode` 作为分类/规格来源，但关联不自动扣减 MAT、不自动改变 `materials.qty`。

若某批实物已被 MAT 数量管理，同时又拟由 ITM 执行出入库，需要专项确定会计口径，防止同一实物重复计入；确认前仅登记关联，不建立自动数量桥接，也不能宣称两种口径已经统一。

### 3.2 关系来源

```text
items.container → containers.code
containers.loc  → locations.code
```

- 新的在库 ITM 必须有有效容器，容器必须有有效库位；
- 当前 ITM 位置通过容器归属得到；
- 旧 `items.loc` 保留兼容和历史线索，不删除列；新模式把它定义为派生显示，不允许独立修改关系；
- 容器移动后网页由关系即时算新库位，不需要逐件修改 ITM；飞书的兼容库位列可以异步刷新，但不得据它覆盖容器归属；
- 旧物品只有 loc 没有 container：显示“旧定位，容器待核实”，不是自动建一个容器，也不是自动当在库。

### 3.3 状态先保持最小集合

| 本地值 | 含义 | 来源 |
|---|---|---|
| `unknown` | 历史状态/归属待核实 | 旧记录空状态 |
| `pending` | 已登记、未正式入库 | 新建主档 |
| `in_stock` | 已确认在库且关系合法 | 正式入库/归还 |
| `out` | 已出库，身份仍保留 | 正式出库 |
| `retired` | 作废/报废终态 | 授权管理操作 |

缺字段不能默认 `in_stock`。借用、维修等复杂状态以后按确认业务扩展，首版可用原因说明，避免引入未定义状态机。

---

## 4. 飞书九表详细变更契约

### 4.1 现有八表逐张处理

| 本地键／飞书表 | 当前主要字段 | 本次处理 |
|---|---|---|
| materials／物料台账 | code/name/spec/loc/container/qty等 | 保留，数量写路径不变；不自动与 ITM 联动 |
| locations／库位 | code/kind/desc/grants | 增 status；原字段保留 |
| containers／容器 | code/type/spec/loc | 增 status/version/lastOpId；loc继续为当前库位来源 |
| items／物品 | code/name/spec/loc | 增 container/status/version/lastOpId；materialCode可选 |
| members／人员 | 人员资料 | 不变 |
| manuals／手册 | 手册资料 | 不变 |
| workorders／工单记录 | MAT计划及执行记录 | 不变；本期不把旧工单自动变为 ITM 出库 |
| transactions／库存流水 | seq/matCode/delta/balance等 | 不放 ITM 事件，不动账本算法 |

### 4.2 items 增列

| 本地字段 | 飞书列名 | 类型 | 初值／写权限 |
|---|---|---|---|
| `container` | 容器码 | 文本 | 旧记录空；仅受控操作写 |
| `status` | 状态 | 单选 | unknown/pending/in_stock/out/retired；空值按unknown读取 |
| `version` | 业务版本 | 数字整数 | 未核实为0；受控操作递增 |
| `lastOpId` | 最后操作ID | 文本 | 空；受控操作写 |
| `materialCode` | 关联物料码 | 文本，可选 | 仅分类关联，不触发MAT改数 |

既有 `code/name/spec/loc` 映射保留。单选值必须先在飞书建齐，列类型要校验。系统“最后更新时间”用于增量拉取与展示，不代替业务版本。

普通档案编辑只推 name/spec 等白名单字段；新建可由服务端设置 pending/0，不能由客户端任意指定 in_stock。code建档后不可随意改；一码不再分配给另一实物。

### 4.3 containers、locations 增列

容器新增：`status → 状态（active/disabled）`、`version → 业务版本`、`lastOpId → 最后操作ID`。保留 `loc → 当前库位码`，启用受控模式后改 loc必须走容器定位操作。

库位新增：`status → 状态（active/disabled）`。旧库位先保留已有信息，需核实或管理员确认后进入active；停用只阻止新作业，不自动迁走已有容器。已有容器的库位不允许通过普通删除移除。

### 4.4 新增 itemOperations／物品操作表

本地统一使用 `code=opId`，适配当前大量按 `code` 过滤和取键的代码，避免另造无法被 pullState 保留的主键字段。飞书业务键列叫“操作ID”，不是飞书原生唯一约束。

| 本地字段 | 飞书列 | 类型 | 用途 |
|---|---|---|---|
| code | 操作ID | 文本 | 全程重试不变；须双向映射 |
| kind | 操作类型 | 单选或文本 | receive/issue/transfer/placeContainer/moveContainer/verifyLegacy/retire |
| itemCode | 物品码 | 文本 | 容器操作可空 |
| containerCode | 容器码 | 文本 | 主要容器 |
| request | 请求内容 | 文本JSON | 来源/目标三码、expectedVersions、schemaVersion等；接受后不可改 |
| requestHash | 请求摘要 | 文本 | 规范化请求一致性检查，服务端计算 |
| before | 操作前快照 | 文本JSON | 恢复与审计 |
| after | 目标快照 | 文本JSON | 预期受控字段、目标版本、lastOpId |
| phase | 处理阶段 | 单选 | PREPARED/APPLIED/REJECTED/REPAIR_REQUIRED |
| progress | 执行进度 | 文本JSON | 受影响记录、回执、待核实步骤 |
| operator | 操作人 | 文本 | 服务端认证身份，不信任任意客户端姓名 |
| device | 设备 | 文本 | 排查来源 |
| requestedAt | 受理时间 | 日期时间 | 服务端时间，仅审计，不作为并发排序证明 |
| finishedAt | 完成时间 | 日期时间 | 终态时设置 |
| error | 错误与恢复说明 | 文本 | 人工处理依据 |

使用飞书系统记录 ID 回读具体行、检测同业务码多行。`code=opId` 只是业务约定，飞书不会自动保证唯一；发现重复或同键异内容必须隔离，不能任选一行。

操作表是“请求＋处理凭据”，不是强事务数据库，也不是纯不可变事件表：request/before/after受理后冻结，phase/progress由执行器受控更新。禁止普通用户删除或编辑它；需要更正时新建关联操作。

### 4.5 不复用旧流水的原因

旧 `transactions` 依赖 `seq` 和 MAT 余量链，`mes-core.js:replayAudit`、checkpoint、增量删除保护都已有业务语义。把 ITM 放入 matCode、delta填1会污染旧账。新操作表不进入 `state.transactions`，不触发 MAT checkpoint重建。

### 4.6 建表不是本次已执行事项

实施时先备份，再按版本化字段清单新增列和表，不删旧列、不改原表ID。检查列名、类型、单选选项、权限、系统更新时间、JSON文本大小和分页完整性。

缺 container/status/version/lastOpId/操作ID等关键列时**禁用正式ITM作业**；普通只读和旧业务仍可用。当前 `coerceFields` 会丢不兼容字段的行为，对这些关键列不能作为“安全降级”。

---

## 5. IndexedDB具体怎么接，不换库、不无谓升级

### 5.1 本地对象示例

```js
state.items = [{
  code: 'WP-001', name: '电钻', spec: '示例规格',
  loc: '历史或显示镜像', container: 'CTN-001',
  status: 'in_stock', version: 2, lastOpId: 'op-xxx'
}];
state.itemOperations = []; // 只读操作缓存，按 code合并，不允许普通编辑
```

新增字段可直接保存在 records.value 和完整state中，无需更改 keyPath，无需因此升级IDB。

### 5.2 最小存放方案

| 数据 | 现有位置 | 新规则 |
|---|---|---|
| 确认后的 items/containers/locations | records + syncMeta state-v1 | 保留 |
| 飞书操作日志缓存 | records中table=itemOperations + state.itemOperations | 加入持久化清单，设置分页/保留策略，不进MAT流水 |
| 未提交草稿 | syncMeta中 `itmDraft:<sessionId>` | 不放会被replaceAll清空的未知records命名空间 |
| 待提交命令 | outbox，`op:'itemOperation'` | code/opId稳定，载荷不可变，区分普通upsert |
| 待处理业务冲突 | conflicts | 记录级关系冲突，非字段自由拼接 |
| 同步水位/本地提交元信息 | syncMeta及现有state元信息 | 数据保存成功后才确认推进 |

不能为了统一名字把 `mes416-state` 改成 `mes416-store`。只有确实增加objectStore或索引时才考虑v2，并编写真实升级测试。

### 5.3 新作业的本地持久化事务

不直接套用旧资源编辑“先改state再慢慢save”的方式：

1. 三码、状态及权限初检；
2. 确认后生成稳定opId和冻结载荷；
3. 使用统一持久化队列，在执行到本次任务时取得最新state快照；
4. 同一IDB事务保存待提交命令、pending元信息、相关state快照及草稿状态；
5. 成功后才提示“本机已保存，待提交”，然后在事务外访问网络；
6. 飞书确认后，同一IDB事务保存确认后的实体、操作结果/基线，并移除或终结对应outbox；
7. 发布界面更新；IDB失败则不显示正式完成。

确认事实与pending目标分开：未提交时不把已确认container/status覆盖成新值。`Outbox.project`若复用，必须明确展示“当前已确认”和“待提交目标”，不能把乐观结果当成已生效。

普通save、拉取合并保存、命令入队和远端确认都通过同一持久化队列；保存不可变快照，避免跨await读取被改变的全局state。跨标签需要同origin互斥或明确只允许一个作业标签；不能只靠每页自己的Promise链。

### 5.4 启动恢复与LS兼容

保留既有LS兜底，但新命令和其处理结果以已成功持久的IDB命令记录为准。dirty标记不能让较旧LS覆盖已保存命令。启动先恢复state，再恢复draft和outbox，核对待确认opId，最后拉取。

IDB不可用时可继续旧只读查询和明确的临时草稿，但新ITM正式作业禁用，避免把未持久的命令交给远端。导出备份要包括草稿、命令和操作缓存，恢复时不能自动重发全部未知结果命令。

---

## 6. 双向同步逐场景说明

### 6.1 网页新增物品 → 飞书 → 另一设备

1. 复用资源页或物品生成器建档，唯一物品码只对应一件实物；状态pending，未入库。
2. 普通主档进入现有fsPushRecord/outbox，推送name/spec等允许字段及受控创建初值。
3. 服务端检查码重复和创建权限。码生成不能仅靠各浏览器本地max+1；推荐服务端受控编号，或设备唯一随机内部码。需短连续WP码时集中分配，未确认不得打印正式标签。
4. 飞书主档写成功后，本机标“主档已同步”。
5. 设备B通过全量或增量拉取加入state.items并落IDB；可以查询但pending不能出库。
6. 后续入库必须另走正式操作接口，不因为建档已有loc就视为已入库。

### 6.2 飞书新增物品／改名称 → 网页

- 飞书新增有码且无状态的行下行映射为unknown；可以显示，不能自动当在库。
- 重复物品码进入冲突，不合并成任意一条。
- name/spec等普通字段继续双向；无本地修改时接收远端，有本地待提交修改时按基线和字段冲突规则裁决。
- 用户显式清空普通字段与“列不存在/没拉到”必须区分，延续clearFields等明确清空契约，不能靠空值猜测。
- 核实为真实单件后，通过verifyLegacy确认状态和归属；不要让普通字段编辑顺便覆盖库存关系。

### 6.3 网页入库 → 飞书 → 其他设备

1. 扫目标LOC→目标CTN→ITM，读取已确认关系和版本。
2. pending/out物品才允许receive；unknown先核实，in_stock不能再次receive。
3. 确认并原子保存本机命令，状态“待提交”。
4. 新接口写操作表和物品受控字段，确认阶段APPLIED后返回结果。
5. 本机以远端回执更新snapshot；保留opId查重。
6. 其他设备拉取新物品快照和相应操作日志，校验version/lastOpId对应后显示。
7. 若两个表拉取时刻不同，显示“关联确认中”，重新拉取相关记录；不按客户端时间回放日志覆盖快照。

### 6.4 飞书直接改状态、物品容器或容器库位

这与“改名称”不同。直接改这些列会绕过三码校验和操作日志，不能同时允许任意改列又承诺唯一受控写入口。

推荐：飞书关键列只读；需要在飞书发起业务操作时使用受控表单/应用入口提交同一操作接口。表单提交是请求，服务端仍校验权限、状态和关系。首版若不提供飞书操作入口，则在飛书只允许普通档案编辑。

如果权限无法限制、检测到手改：

- 保留远端观察值和之前确认值；
- 标“外部改动待复核”，暂停相关实体写作业；
- 展示改了哪些字段、是否缺对应lastOpId/日志；
- 管理员选择经正式操作接受纠正，或通过受控修复恢复原关系；
- 不静默取本地/远端最后写者，也不自动生成一次入出库。

为识别同version的手改，需要比对确认字段快照和操作结果，不只比version。若手改伪造了版本/操作ID，日志请求和after不匹配同样冲突。

### 6.5 容器移库

新模式当前物品位置由容器loc决定。moveContainer只变容器loc/version/lastOpId并写日志，不把每件物品都当出库再入库。

网页关联查询立即按新容器位置展示。飞书items旧“库位码”若作为镜像异步刷新，其落后不影响业务真源；必须标明为派生列，优先用关联/公式或单独镜像维护，不能由镜像反向改container。

这里不自动修改MAT定位或数量。旧MAT是否也随容器移动属于旧业务兼容问题，单独列差异，不能顺手批量更新。

### 6.6 出库与空字段

issue后ITM状态out、container显式清空，历史来源写操作日志。由于当前upsert默认忽略空值，必须在专用处理器使用明确清空语义，不能仅传 `container:''` 就假定飞书已清空。

查询显示“当前不在库”；原位置仅作为历史，不当当前位置。下行必须接收真实清空，不能用旧的本地非空容器填回去。

### 6.7 离线、刷新与恢复

离线可以保存草稿和待提交意图，不能标成出库成功或据此发放实物。重新联网后检查新版本和归属；冲突停止并让人重扫。

请求超时保留原opId并标“结果待确认”。查询结果/恢复原操作，不换ID再次发。无结果记录不等于未处理，可能是尚在飞书执行；见下节未知结果屏障。

---

## 7. ITM操作接口与非原子恢复协议

### 7.1 新增受控接口，不污染MAT stock

建议在现有目录新增：

```text
POST /api/feishu/item-operation
GET  /api/feishu/item-operation?opId=...
```

生产接口由GitHub CI/CD部署到Vercel Functions；本地feishu-server只作开发适配，共用业务实现不等于共享运行进程。正式写入在handler内调用第8章选定并验证的共享协调能力，不能以模块级内存队列作为唯一保障。查询可按原有Functions路径运行。

请求示例：

```json
{
  "schemaVersion": 1,
  "opId": "稳定ID",
  "kind": "issue",
  "itemCode": "WP-001",
  "source": {"loc":"L-01", "container":"CTN-001"},
  "expected": {"itemVersion":2, "containerVersion":5},
  "reason": "领用"
}
```

receive使用目标三码；transfer含来源及目标；容器操作可以无itemCode。不得用MAT的delta/qty表达ITM状态变化。

### 7.2 执行顺序

在满足下一节的唯一受控执行条件后：

1. 认证、查关键列、类型、权限和业务码重复；
2. 按opId查已有日志并比对请求摘要；同键异内容拒绝；
3. 读取最新实体，核对状态、三码及期望版本；
4. 把冻结request/before/after写为PREPARED，并拿到记录ID；
5. 更新目标实体受控字段，将目标version及lastOpId一起写；
6. 回读实体校验目标值；
7. 日志置APPLIED，返回确认实体及原操作结果。

**这是多次飞书HTTP请求，不是跨表事务。** PREPARED日志先落库便于恢复，不等于原子占位或原生唯一约束。

### 7.3 阶段与故障

| 阶段 | 含义 | 处理 |
|---|---|---|
| 本地草稿 | 从未发送 | 可编辑/取消 |
| 本地待提交 | 命令已持久化 | 网络可用时受控提交 |
| PREPARED | 已有执行意图，快照是否完成需核查 | 不能视为成功或失败 |
| APPLIED | 快照和操作回读完成 | 同键重试返回原结果 |
| REJECTED | 明确没有业务副作用 | 可重新扫描并生成新命令 |
| REPAIR_REQUIRED | 部分成功、重复日志或请求结果不确定 | 阻断相关作业，进入恢复 |

日志建行本身超时、实体更新超时、日志终态更新超时都可能“请求已成功但回执丢失”。不能将未知标为REJECTED，不能因为重试次数用尽就删除命令。

### 7.4 恢复规则与未知请求屏障

- 实体lastOpId等于原opId且after一致：不重复执行实体变更，修复日志终态；
- 实体仍是before：只有在证明原请求不会再迟到生效后才能重试；一次回读没变化不能证明这一点；
- 实体已经是另一操作或版本不匹配：隔离，不用旧after覆盖；
- 同opId多个日志：暂停并核对，不能任意删除历史后继续；
- 函数冷启动、重试或部署切换后，都从共享持久状态恢复原操作；未决操作的隔离不能因实例退出而消失。

未知屏障必须存入经验证的共享持久设施，不能只存在浏览器syncMeta、函数内存或临时文件。日志创建未知、尚无可靠飞书日志时也必须能保留屏障；新的函数实例在无法确认屏障状态时拒绝相关写入。单次回读未见变化不证明原飞书请求已经终结；不能在租约过期后直接解冻。共享恢复能力尚未落地时，该协议仅用于隔离环境原型，不开放严格多端正式作业。

任何飞书晚到请求仍可能影响快照，因此本路线无法给出PostgreSQL事务式的原子强保证。阻断和人工复核以可用性换取保守执行，明确接受此限制后才可试点。

---

## 8. 已确认的 Vercel 部署与最小协调边界

### 8.1 生产事实与不能推导的假设

用户确认：上传项目到 GitHub 仓库后触发 Vercel 自动 CI/CD。结合项目现有 `api/feishu/*.js` handler，生产采用 Vercel Functions 执行 Node.js API，不把 `feishu-server.mjs` 当作固定常驻生产进程。后者保留为本地开发/兼容适配器。

单个项目、单个生产域名、单个区域，不等于所有请求共享同一个 Node 进程。无需假定生产已经发生多实例，也必须考虑函数并行、实例回收、请求重试和新旧部署重叠。`vercel.json` 的区域设置不提供全局互斥。

因此移除“常驻单进程内存队列”生产路线：不能用模块级 Map、Promise 链、进程变量或本地临时文件保存跨请求唯一锁、未完成命令及故障屏障。

### 8.2 保留的总体结构

```text
GitHub → Vercel CI/CD → 静态网页 + /api/feishu/* Functions
                           ↕
浏览器 IndexedDB           飞书现有八表 + 物品操作表
（每台设备独立）            （档案、关系快照、作业记录）
                           ↕ 正式多端写入的额外条件
                    最小共享操作协调能力（待选型验证）
```

IndexedDB不变、飞书普通档案双向同步不变、MAT旧业务不自动迁移。共享协调只针对ITM及其容器归属的受控命令，不借此重做全部MES。

### 8.3 功能与上线门槛

| 范围 | 是否需要新增共享协调设施 | 上线边界 |
|---|---|---|
| 扩列、关系展示、独立查询 | 不需要 | 完成双向映射、兼容和冲突验证即可 |
| 三码扫码、条码识别、本地草稿 | 不需要 | 不宣称入出库已完成 |
| 飞书操作日志和部分成功恢复原型 | 可先不增加 | 隔离测试数据验证，不作为严格多端写入保证 |
| 多设备正式出入库、换箱、容器移库，要求只接受一次合法处置 | 需要经验证的共享仲裁能力 | 仅靠IDB和飞书先查后写不满足 |

“只有一个操作者”可以是测试条件，但不是Serverless生产环境的并发保障：重复请求、旧页面和重试同样可能并行。

### 8.4 共享协调的最小职责

不预先强制PostgreSQL，但候选必须实际提供：

1. 持久的 `opId + requestHash` 唯一受理及结果查询；
2. 对关联实体的原子命令认领、期望版本校验和受控状态推进；
3. 持久未完成状态、恢复游标、未知结果屏障，不能依赖函数活着；
4. 不同函数实例共享同一仲裁范围；首期可全局串行，后续再优化；
5. 函数崩溃、重复消费、部署切换时仍能恢复，不因锁TTL到期直接放行冲突操作；
6. 关联容器移库与物品换箱遵守同一协议，不能只锁物品却忽略容器变化。

若需存储单件状态、当前容器及版本作为仲裁依据，这些字段会成为共享协调层的权威子集，必须明确批准其所有权及初始化/恢复规则。不能一边说“只加锁”，一边悄悄引入第二套库存真源。

### 8.5 候选能力比较，不以产品名代替正确性

| 候选 | 能解决什么 | 仍需证明什么 |
|---|---|---|
| 持久工作流／按键串行队列 | 执行调度、重试、流程状态 | 唯一受理、实体状态前置校验、重复消费及外部未知请求，不是有队列就原子 |
| 支持原子条件操作且配置可靠持久化的共享KV | 原子认领、去重和屏障 | 故障丢失语义、所有权过期、栅栏；飞书不支持栅栏校验，锁过期仍不能安全盲接管 |
| 小范围托管事务存储 | 命令结果与受控实体状态原子提交 | 需要额外服务与运维；飞书仍只是异步同步，不能跨系统原子 |
| 单进程内存锁／飞书version列 | 局部排队／冲突提示 | 不满足共享仲裁，不列为正式保证方案 |

PostgreSQL属于第三类的一种选择，不是唯一选择，也不是浏览器IndexedDB替代品。本次未选购、安装或验证任何第三方协调服务。

**正式方案的两种语义不能混写：**

- 若飞书仍为受控快照提交点：协调层管理命令执行；出现未知飞书写请求时必须保持阻断，飞书回读确认后才显示完成。第7章描述的是这条非原子路线。
- 若共享事务存储成为受控状态权威：命令在该存储提交即业务入账，飞书失败显示“已入账待同步”，由投影任务恢复；必须同步修改第6、7章及读写所有权契约后才实施，不能直接复用“飞书APPLIED才完成”的口径。

### 8.6 Functions 的具体工程要求

- 同一命令的冻结载荷和opId跨HTTP重试、刷新、部署保持不变；
- 函数响应结束后不依赖无人监管的后台Promise完成关键写入；后续处理使用明确的持久调度或新请求恢复；
- 长批次拆为有进度的小任务，考虑实际项目函数执行时限和飞书限流，不假设某个固定超时时长；
- 自动恢复入口只凭服务身份调用，业务冲突转人工，网络重试沿用原命令；
- Preview环境使用隔离飞书表和协调命名空间，默认不能写生产；
- 生产与预览的环境变量、schemaVersion、最低支持客户端版本分别配置；
- GitHub新部署期间旧页面仍可能存在，服务端契约和共享协调必须阻断旧写法；
- 回滚部署不回滚业务事实，也不能绕过协调层回到通用upsert；
- 所有写接口服务端鉴权，不能把CORS或客户端按钮禁用当权限控制。

结论：生产路线固定为Vercel，不再研究默认迁出到常驻Node。先交付现有架构上的关系/查询/草稿闭环；严格多端写入在最小共享协调能力验证通过后开启。

---

## 9. 具体改哪些文件、常量和函数

### 9.1 注册表不能机械全部加

当前 `lib/feishu-api.js:PUSH_ORDER` 名称像推送清单，实际还服务拉取等路径。新增操作表既要可拉取，又不能被普通批量push。因此实施中显式拆职责，例如：

```text
READ_TABLES：原八表＋itemOperations
GENERIC_WRITE_TABLES：允许普通编辑的原表（并带字段白名单）
OPERATION_TABLES：itemOperations，仅专用接口写
APPEND_PROTECTED_TABLES：transactions、itemOperations，不自动判删历史
```

这是建议新常量，不是现有功能。对每个PUSH_ORDER消费者逐一核实。单纯“不要加入PUSH_ORDER”会让某些读路径漏表；单纯“加入”又可能开通危险写入。

### 9.2 接入清单

| 文件/位置 | 必须修改的内容 |
|---|---|
| `lib/feishu-api.js:TABLES` | 配置第九表ID；环境变量FEISHU_TABLES部署映射同步更新 |
| `TABLE_DEFS` | 旧表新字段＋itemOperations主键code映射操作ID；双向mapUp/mapDown；类型和必需列校验 |
| `pullState/pullChangesBySort/censusTable` | 覆盖新表，完整性和日志删除保护；缺页不推进完整水位 |
| `upsertRecords/deleteRecords` | items/containers关键字段白名单；禁止通用写删操作表；历史有码实体用停用而非普通硬删 |
| `api/feishu/item-operation.js`（拟新增） | PREPARED/快照/APPLIED及恢复协议，操作结果查询 |
| `feishu-server.mjs:ROUTES` | 本地开发增加同名适配路由；正式生产仍走Vercel，测试不得旁路共享协调或使用生产凭据 |
| `index.html:migrateState` | 旧ITM缺状态unknown、container空、版本0；不得默认入库 |
| `initLocalStore/persistStateToIdb/save` | 保存队列、稳定快照、草稿namespace、完整state恢复、outbox确认原子持久化 |
| `fsExecItem/fsPush/fsEnqueue/fsFlushQueue` | op=itemOperation新分支；不退回stock；未知/冲突不盲重发 |
| `TABLE_LABEL/TABLE_KEY_FIELD/SYNC_TABLES` | 显示及读注册新增itemOperations，键code |
| `PUSHABLE` | 操作表不加入；items/containers普通推送剥受控字段 |
| `fsMerge/CORE.mergeRemote` | 全量路径对关系字段独立处理，不允许普通两方覆盖 |
| `fsThreeWayApply/TWM.planMerge` | 增量路径普通档案三方；关系字段组整体校验，操作日志走专门缓存合并 |
| `mes-core.js:MERGE_TABLES` | 区分识别表和普通合并表；不把操作请求当普通字段合并 |
| `lib/three-way-merge.js:MERGE_TABLES` | 确有独立清单；与核心/outbox的共享契约同步调整 |
| `lib/outbox.js:DEFAULT_TABLES/project` | 识别新逻辑表及itemOperation；接网页读视图，pending与已确认分离 |
| `lib/incremental.js:censusDecision` | 操作表与旧流水均禁止自动删本地历史；调整调用方保护 |
| `index.html:RES_TYPES/字段标签/比较字段` | 显示container/status/version等；只给普通字段编辑入口 |
| `resSave/resDelete/btnGenItm` | 唯一码受控建档、pending初值、禁普通改归属、禁码回收 |
| Excel导入导出、备份恢复 | 新字段往返；关系导入先预览复核，不直接覆盖；未知命令不自动重发 |
| `feishu-sync.mjs:MAPS/PUSH_ORDER` | 根目录CLI仍有独立映射与推送路径，必须同步字段并封关键字段旁路 |
| `api/feishu-sync.js` | 已复用pullState，核验响应契约，不复制第三套映射 |
| `scanItem/scanWhere/handleScan` | ITM关联查询和三码作业隔离，不误调旧WIP/MAT写操作 |

`records`持久化清单与飞书八表并非完全一致：它还含necOrders，MAT流水单独存。不能盲目用“原八表数组”替换所有清单。

### 9.3 关系字段合并策略

items的 `container/status/version/lastOpId` 作为一个受控字段组；containers的 `loc/version/lastOpId` 同理。

- 不能从本地取container、远端取status、另一个事件取version拼成新记录；
- 仅接受结构合法且能关联已确认操作的快照；
- 更高version可触发重新取日志验证，不是无条件可信；同version不同受控字段为冲突；
- `lastOpId`是随机身份，不按字符串大小比较新旧；时间戳也不是业务顺序证明；
- 操作缓存按code匹配、核对冻结载荷及合法阶段转换，不用普通字段“保留本地/远端”任意改日志；
- 普通名称编辑不能上传整条陈旧记录把受控字段覆盖回去。

---

## 10. 页面与扫码落地

### 10.1 复用现有页面，而非新造后台

- 物品、容器、库位资源页保留；扩列和跳转按钮。
- 原扫码页增加明确模式：“查询”“ITM作业”；保留旧MAT/WIP等入口，但模式内严格分流。
- 新增独立“库位/容器/物品查询”导航页，复用详情卡和数据源，不再维护一份台账。
- 操作页显示三码、名称/规格、当前状态、目标动作、固定数量1、确认按钮和同步状态。

### 10.2 严格三码

```text
选择入库/出库 → LOC → CTN → ITM → 显示关系和状态 → 确认本行
```

- 扫码只填草稿；收到错误类型不自动补库位、不跳旧MAT兜底；
- 更换库位清理下游，切行后旧相机/网络结果丢弃；
- 相机事件绑定sessionId/rowId/step/generation；
- 持续扫同一码不增加数量，批次重复ITM不能有两行待执行；
- 确认中行锁定，重复点击不生成新opId；
- 出库一定核对物品确实在来源容器；
- 换箱另加目标LOC、CTN，一次受控物品操作；
- 整箱移库是独立容器操作：原LOC→CTN→目标LOC，不要求虚构物品码。

### 10.3 条码

现有jsQR只解二维码。一维条码候选在真实设备上比较ZXing系或其他受维护库，BarcodeDetector仅作为能力可用时优化；本次未安装或实测。

LOC/CTN继续二维码。ITM建议唯一内部Code128标签；普通EAN/UPC通常是商品型号码，不能直接代表一个真实单件。只有经核实唯一的原厂序列码才能绑定ITM。

扫码枪/手输共用解析与步骤校验。回车结束码输入，不等于提交；聚焦数量或备注时不能被全局键盘监听误收。HTTPS、相机权限、飞书内置浏览器兼容性单独验证。

### 10.4 查询行为

- ITM：当前状态、container→loc、规格、操作履历、待提交/冲突/同步时间；out显示最后位置为历史。
- CTN：当前loc、in_stock物品列表及件数；点击物品详情。
- LOC：容器列表及在库ITM件数；点击容器下钻。
- name/spec/容器说明/库位说明模糊检索，码精确查；重复码不取第一条。
- 旧ITM只有loc：单独列“旧定位未绑定”，不伪装成正常容器库存。
- 查询不触发入出库，可记录观察日志，但观察不能反向覆盖当前位置。

---

## 11. 现有数据怎样迁移

本期是**补关系、补状态**，不是默认把全部MAT数量转换为ITM。

1. 备份飞书旧八表及本地IDB/LS/outbox；记录未完成操作。
2. 扩列后旧字段原样保留，旧ITM状态unknown。
3. 对现有ITM逐件确认唯一实物，扫描LOC→CTN→ITM，管理员确认后执行verifyLegacy生成一条初始关系操作。
4. 旧loc与容器loc矛盾则列冲突，不猜、不批量覆盖。
5. CTN旧loc有效且经核实后建立受控版本基线；孤儿CTN先定位。
6. 无容器/未知码不自动造容器或物品；在主档建好并同步后再作业。
7. MAT、旧流水和工单保持原样；若后续希望具体MAT改成逐件ITM，再制定范围专属迁移计划。
8. 保留旧未支持客户端可查询，但对已经启用受控关系的ITM/CTN普通改归属要服务端阻断。

没有默认“全部物料拆件”“停用旧MAT库存”的步骤。

---

## 12. 分期及验证门槛

### P0：真实部署与字段契约

生产已明确为GitHub→Vercel自动部署。核对生产/Preview环境变量隔离、实际Functions配置、飞书表ID/列/权限、活跃设备与CLI旁路；批准九表契约和受控字段。为正式多端写入明确最小共享协调能力验收标准，不再把常驻Node唯一执行器作为待确认前提。

### P1：现表扩列＋只读关系查询

复用items/containers/locations，新增字段映射、unknown兼容、完整下行和查询。此时不开放正式ITM写作业，不需要PG，不需要操作表已经上线。

### P2：本地草稿与持久化闭环

严格三码、一维条码原型、IDB原子命令入队、保存串行、LS启动恢复、pending视图、导出恢复。数据仍仅草稿。

### P3：第九表＋受控接口

飞书操作表、必需列检查、PREPARED/快照/APPLIED、结果查询、部分成功恢复、普通同步封口。先隔离测试环境。

### P4：Vercel共享协调验证与试点

保持现有Vercel部署，比较并验证第8章的最小共享协调方案。验收跨函数实例重复请求、冷启动/中断恢复、新旧部署并存、飞书超时迟到及持久屏障，再开放多设备正式写入。未通过时保留查询和草稿，不用“单操作员试点”替代Serverless并发保障；不得把P1/P2完成冒充该保障已完成。

### P5：双向同步与故障验收后扩展

实机、真飞书隔离表、超时迟到、重启、手改、旧客户端、分页和空值全测；留运维停写/恢复手册。

### 对应测试文件

| 文件 | 追加内容 |
|---|---|
| `test/store.test.js` | 新命令＋state/outbox全有全无；IDB失败不确认 |
| `test/persistence-invariants.test.js` | records重建不丢操作缓存；draft不丢；LS不覆盖已持久命令 |
| `test/outbox.test.js` | itemOperation识别、pending视图、未知结果不移除、表注册契约 |
| `test/feishu-api.test.js` | 新字段双向、缺列/错类型阻断、日志恢复和同键异内容 |
| `test/feishu-handlers.test.js` | 新接口认证、参数、阶段、结果查询 |
| `test/local-server.test.js` | 本地适配路由、禁写模式及不旁路协调；不把本地单进程测试当生产并发验收 |
| 拟新增Vercel集成用例 | 函数并行/中断、跨部署重试、共享状态恢复、Preview生产隔离 |
| `test/three-way-merge.test.js` | 普通字段双改；关键字段组冲突不能拼接 |
| `test/incremental.test.js` | 新表、同毫秒水位、截断、操作历史删除保护 |
| `test/feishu-sync.test.mjs` | CLI字段往返及受控字段禁写 |
| `test/mes-core.test.js` | ITM状态与关系纯函数，不回归污染MAT |
| 拟新增UI/实机用例 | 三码顺序、切行、迟到结果、码重复、Android/iOS/飞书相机 |

上述为待执行测试，不能把现有测试代码存在当作本次已通过。

### 必测的具体闭环

1. 网页建pending物品→飞书→另一设备，不自动变在库。
2. 飞书改名称→网页，本机待提交名称有冲突不丢。
3. 入库三码→操作APPLIED→跨设备看到同关系。
4. 出库容器清空真实往返，不被本地旧值补回。
5. CTN移库→ITM查询立即随容器，MAT数量不变。
6. 飞书直接改受控关系→标冲突，不能静默接受。
7. 日志成功实体失败、实体成功日志失败分别恢复，不重复处置。
8. 请求超时、迟到、重启→保持屏障，回读未见不贸然解冻。
9. 两个设备对同一件同时操作：若目标部署不能证明只一次生效，则不允许宣传/开放强一致多端模式。
10. IDB写失败、旧LS dirty、跨标签保存、records replaceAll均不丢命令。
11. 缺关键列、单选选项、重复码、新表配置缺失→禁用新作业，旧只读与不相关业务继续。
12. 全量与增量都保留字段组一致；操作表缺页不删历史，数据和水位一起落盘。

---

## 13. 回退与异常操作手册

- P1扩列是增量兼容；回退UI可保留列，不能删除新日志。
- P3后停用新操作入口时，先列未决opId并处理/隔离；不清空outbox或操作表假装完成。
- 已确认新关系不能让旧网页通用upsert覆盖；回退只读页面不等于恢复旧关键字段写权限。
- 超时未知：查操作和目标记录、比before/after/version/lastOpId；无法证明结果时停写并人工复核实物。
- 飞书不可用：查询缓存与草稿继续，正式作业停写；不在本地宣告成功再补账。
- IndexedDB损坏：导出可用日志，重建缓存后从飞书拉取；未上传草稿无法凭空恢复，备份和持久化失败提示必须真实。
- 发现实体重复码：冻结相关码，现场重贴及审计，不自动按最后更新时间选择。

---

## 14. 前轮多模型结论及本次部署修订

前轮 aicode 使用 DeepSeek V4.1 Flash、GLM 5.3、Opus 5、Kimi K3、GPT-6，分别研究本地存储、飞书表、无PG部署、页面场景及同步接线；之后由Opus 5和GPT-6交叉核对实际分歧，共7个任务。

采用的结论：保留mes416-state/v1；现表扩列＋一操作表；不动MAT账；分别补全量和增量；新增命令持久化；新表受控写；部署条件单列。

否决或修正：

- “为了新字段必须升IDB v2并改库名”：没有依据，反而可能丢入口数据。
- “project已经接入网页”“全量也是三方”：与代码不符。
- “缺关键列丢掉就安全”：会形成两边不同的业务事实，禁写。
- “新增ITM默认在库”“允许省略容器”“出库先扫ITM”：不符合用户规则。
- “最后写者胜出在小规模足够安全”：不能保证同一实物不被重复处置。
- “飞书version就是CAS”“opId列自动唯一”：不成立。
- “操作表不加PUSH_ORDER就行”：该数组混合多种职责，需拆注册表再接读写。
- “lastOpId可按大小防回退”：ID只标身份，不是序号。
- “把MAT qty批量变成ITM”：不是本次前置，需另确认实物与会计范围。
- “必须先安装PG”：撤回默认前提；仅保留为多端严格保障的一类后端候选。

## 15. 已确认事项与剩余决策

### 已确认，不再重复询问

- 三码各自对应唯一实体，ITM一码一实物；
- 生产通过GitHub提交触发Vercel CI/CD，按现有API目录采用Functions执行模型；
- 保留网页IndexedDB，不更换mes416-state库名；
- 不把安装PostgreSQL作为关系、查询和扫码原型的前置，也不改为默认自建常驻服务器。

### 实施前仍需核实或批准

1. Vercel生产与Preview的具体配置、函数时限、凭据及飞书表是否隔离；这是部署参数核验，不再是询问是否常驻Node。
2. 飞书关键列权限、普通档案双向编辑范围，是否需要飞书端发起受控入出库请求。
3. 新建一张物品操作表与给旧表加列的权限。
4. 现有items的实物贴码情况、数据规模、设备与条码样例。
5. 最小共享协调设施的能力、成本、运维与故障恢复验证；若承接受控实体状态，须另行批准该字段级权威边界，不悄悄切换全部真源。

关系字段、只读查询及不入账扫码原型可先推进；正式多设备写入等待共享仲裁和恢复测试通过。

**最终建议：沿GitHub→Vercel部署接完整现有IndexedDB与飞书，再对唯一物品命令增加经验证的最小共享协调能力。不是换库才能做业务，也不能把一个Vercel项目当成一把跨请求全局锁。**
