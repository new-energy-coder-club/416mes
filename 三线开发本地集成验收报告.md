# 唯一物品功能三线开发：本地集成验收报告

## 结论

三条独立路线已完成组织、阶段审查、比较和回收；选择GPT-6候选 `559237c399c48d3113c3f1e0c233aba8f016ffc6` 合入本地main。

**业务合并提交：`d4e5883`。本地代码集成与隔离验证完成，生产启用未完成。** 未push、未部署Vercel、未读取真实凭据、未写生产飞书。

## 三条路线实际结果

| 路线 | 保留分支/提交 | 结果 |
|---|---|---|
| GPT-6 | feature/unique-item-gpt6 / 559237c | S0–S5及定界S5.1完成，固定提交独立测试后采用 |
| GLM5.3 | feature/unique-item-glm53 / d5ce96e | S0–S2及部分S3交付，页面交互/API/最终E2E不完整；其LINE_REPORT如实回收，未合并 |
| Kimi K3 | feature/unique-item-kimik3 / 55d9297＋未提交领域文件 | 领域23测试通过，其他阶段未实现；因反复工具参数误用停顿，保留作者代码与日志，不合并 |

没有把三条都说成完成，也没有按测试数量决定胜负。主会话提供相同门槛与缺陷反馈；部分Kimi作者补丁由主会话机械应用，来源已登记。

## 已集成能力

- 一库位一码、一容器一码、一物品一码，ITM无可编辑qty。
- 复用IndexedDB `mes416-state` v1七store，不安装PostgreSQL；保持旧MAT/WIP数量规则。
- 九表字段契约及普通写保护，操作表独立受控；全量/增量关系字段组校验，缺日志/缺列/外部修改隔离。
- 命令和快照本机事务、草稿恢复、迟到ACK版本保护、未知结果保留。
- 独立查询入口、三码下钻、只读查询扫码；作业LOC→CTN→ITM，行身份/重复输入保护、多行表格。
- 实际ZXing Code128解码适配，二维码复用jsQR；相机停止/迟到授权/播放失败资源处理及手输降级。
- 服务端操作协议、实际飞书Repository、幂等、授权范围、未知结果屏障和保守恢复；默认正式写入关闭。
- 受控注册/启用、入库、出库、换箱/移库、管理员退役申请；使用fake认证/协调的合法闭环已测试。
- 导入命令原opId只读复核、冲突可见面板、旧批量生成器受控/非受控兼容门禁。

## 主会话真实验证证据

| 验证 | 结果 | 边界 |
|---|---|---|
| 原基线npm test（bash-24） | 511/511，exit0 | 未改项目基线 |
| 冻结cfc6b44 npm+browser（bash-58） | 577/577 + Chromium1/1，exit0 | 固定候选、外部请求全部拦截 |
| 559237c独立对抗脚本（bash-65） | 36/36，exit0 | 主会话自编probe，非候选自报 |
| 主工作区合入后npm test（bash-68） | **581/581，0失败/0跳过** | 含旧MAT/WIP、映射、IDB、UI、故障测试 |
| 主工作区合入后test:browser（bash-68） | **Chromium1/1，exit0** | 整页启动、真实IDB、390px、刷新草稿、拒写响应后命令保留，无pageerror |
| Repository成功链（bash-47/49等） | 通过 | fake-indexeddb→真实handler/Repository→localhost fake飞书→第二端pull；包括注册启用入出库 |
| 实际条码像素fixture | 通过 | 真ZXing解码合成Code128，不是手机实物实测 |

浏览器测试中检测到旧代码尝试访问生产域名，但测试在导航前以路由拦截中止，未访问生产。真实飞书、手机和认证/协调服务不得据上述模拟宣称通过。

集成时只有计划文档add/add冲突，已保留主会话最新授权条款。原始测试日志中一处空白行带尾空格导致全文件diff-check报错；保留历史原始日志不篡改，排除test-logs后的源码/文档diff-check通过。npm ci按锁文件安装29包，未运行依赖脚本。

## 生产启用前仍必须完成

1. **持久共享协调及认证**：当前提供协议/组合入口，不含已部署的真实适配器；默认handler返回401/503，不能上线就出入库。实施选择需验证Vercel跨实例、故障恢复、持久屏障与权限。
2. **飞书结构/权限**：用户截图已确认原Base内新建“物品操作”，但新Table ID、类型/选项/字段ACL未核验；还需按ITM_SCHEMA做授权只读核对和隔离表往返验证。
3. **实机**：Android/iOS/飞书内置浏览器、扫码枪、真实Code128打印与相机识别未验证。
4. **数据核实**：旧ITM空状态为unknown，不能批量猜在库；旧LOC/CTN需经核实启用。MAT不自动拆件。
5. **发布授权**：push main会触发生产CI/CD，本次没有push。需要发布时先确认这些门禁和环境隔离。

## 明确限制

- LOC v1只有unknown→active；停用/重新启用需要后续版本化契约，不用历史日志时间猜顺序。
- 冲突面板可以看原因、对象和两边事实并安全重拉，不提供任意选择本地/远端直接改受控关系。
- 导入备份命令只能只读检查，不自动跨设备恢复执行。
- 手机操作表采用横向滚动，不是专用移动卡片。
- 整页Chromium当前验证默认拒写链；成功飞书适配链在Node/fake-IDB层完成，两者不混称同一条完整真实浏览器成功链。

## 文件入口

- `index.html`：查询、作业和管理员复核UI。
- `api/feishu/item-operation.js`：受控接口，默认禁写。
- `lib/unique-items.js`：领域状态和关系。
- `lib/item-persistence.js`、`lib/item-sync.js`：本机保存和同步。
- `lib/item-operation.js`、`lib/item-repository.js`、`lib/item-runtime.js`：命令、飞书适配与部署组合。
- `ITM_SCHEMA.md`、`ITM_RUNTIME.md`、`LINE_REPORT.md`：字段、启用前提、候选范围。
- `evaluation/unique-item/`：主会话独立测试、路线比较与缺陷证据。

## 本次完成边界

完成的是用户要求的“三线隔离开发→逐阶段测试→主会话独立验证比较→择优本地集成”。不完整路线已如实回收；正式运营上线仍需上述外部条件，不在本报告中冒称完成。
