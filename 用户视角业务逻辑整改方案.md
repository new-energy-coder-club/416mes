# 416MES 用户视角业务逻辑整改方案

> 状态：待实施方案
> 基线：`2.24.0`，代码最近提交 `bc9db47`
> 研究依据：三路只读代码审查（8 表映射、前端能力、信息架构）+ 用户线上截图反馈
> 原则：先修业务逻辑闭环，再搬迁页面，最后做视觉优化；每批必须先通过验证门，才能进入下一批。

---

## 0. 先给结论

当前不是单纯的「页面不好看」，而是**数据表、业务任务、同步收场路径三者没有对齐**：

1. 8 张飞书表中只有 **3 张有自己的网页页面**：物料、人员、工单。
2. 库位、容器、物品、手册只能从「标签打印」页的类型条看到；这是打印页面兼职数据管理页面。
3. 库存流水藏在物料台账底部折叠区，只显示最近 50 条，没有筛选，也没有独立入口。
4. 同步状态、队列、冲突、健康、日志都是全站状态，却集中塞在物料台账页。
5. 对账只显示「本地多 N」，但没有告诉用户是哪几条，也没有让用户选择「保留本地 / 推回飞书 / 删除本地」的完整收场路径。
6. 工单重复记录可以在本地存在，而飞书按业务键折叠成一条，形成「网页 9、飞书 4」这种用户无法理解的状态。

因此，**当前核心逻辑不能直接宣布“完全没问题”**。已验证的库存计算、工单执行、冲销、盘点、CRUD 传输是正确的；但用户实际使用中暴露出的「工单重复、页面白屏、对账无法收场、表找不到」属于业务闭环问题，必须先处理。

---

## 1. 用户视角的验收原则

所有后续开发都不能只用函数单测或静态检查，必须模拟用户完成任务。

### 1.1 用户任务必须完整闭环

每个功能要按以下路径验证：

```text
找到入口 → 看懂对象 → 执行动作 → 看到结果 → 刷新/换页 → 结果仍在
→ 飞书对应表出现相同结果 → 再次操作不会重复或破坏数据
→ 出错时知道原因，并且有可恢复动作
```

任何一步失败，都不能标记为「功能通过」。

### 1.2 每个页面必须回答四句话

页头必须明确：

1. **我现在管理什么对象？**
2. **这个动作会不会改变库存？**
3. **数据会写到哪张飞书表？**
4. **失败后我应该怎么处理？**

### 1.3 每个表必须有可见的同步状态

最低要求：

- 本地记录数；
- 飞书记录数；
- 本地多出的具体业务键；
- 飞书多出的具体业务键；
- 未推送、冲突、待人工删除数量；
- 点击后能进入具体收场动作。

「只显示本地多 5」不算可用的同步功能。

---

## 2. 8 张飞书表的目标归属

后端已通过通用 `upsertRecords` / `deleteRecords` 支持 8 张表，后续主要是前端页面和逻辑补齐，不重复造后端 CRUD。

| 飞书表 | 目标网页归属 | 目标能力 |
|---|---|---|
| 物料台账 | 物料台账 | 增删改查、库存调整入口、库存状态、同步状态 |
| 人员 | 成员名录 | 增删改查、职务下拉、操作人选择 |
| 工单记录 | 库存工单 | 建单、修改未执行计划、执行、部分执行、冲销、取消、删除、同步 |
| 库存流水 | **库存流水**独立页 | 全量浏览、筛选、审计、导出、修数；只增为主 |
| 库位 | **资源档案**页中的「库位」 | 浏览、搜索、新增、修改、删除、同步 |
| 容器 | **资源档案**页中的「容器」 | 浏览、搜索、新增、修改类型/规格/库位、删除、同步 |
| 物品 | **资源档案**页中的「物品」 | 浏览、搜索、新增、修改、删除、同步 |
| 手册 | **资源档案**页中的「手册」 | 浏览、搜索、新增、修改版本/库位、删除、同步 |
| `state.necOrders` | NEC 任务 | 本地 NEC 单，明确标注「不改库存、不在飞书 8 表」 |

### 2.1 明确不能继续接受的现状

- 库位/容器/物品/手册不能再只依赖标签打印页浏览。
- 库存流水不能再默认折叠且只展示 50 条。
- 同步检查不能再放在物料台账的业务内容下面。
- 标签打印页不能再承载业务删除。
- 业务表不能只靠 Excel 导出/导入才能修改。

---

## 3. 分阶段实施方案

### 阶段 0：线上数据安全与当前异常收敛（必须先做）

**目标：不改页面结构，先处理当前用户已经看到的异常。**

#### 0.1 本地重复工单诊断

新增只读诊断，按表和业务键分组：

```text
workorders / LL20260917004 / 本地 6 行 / 飞书 1 行
```

诊断必须列出每一行的：

- 本地索引；
- 业务键；
- 计划明细；
- 状态；
- 执行数量；
- 是否已经执行；
- 是否与飞书当前行一致。

#### 0.2 重复行收场

规则必须明确：

- **同业务键、内容完全相同、均未执行**：允许用户选择保留 1 行，删除其余本地行。
- **同业务键、内容不同**：禁止自动合并数量；必须逐行展示并要求用户选择真实记录。
- **已执行或已取消**：禁止静默删除，必须人工确认并显示库存影响。
- 删除本地重复行时，**不得调用 `fsPushDelete('workorders', [code])`**，因为该接口按业务键删除，会把飞书唯一记录一并删除。
- 收场后立即重新对账，显示工单本地/飞书数量和具体键。

#### 0.3 工单生成防重

已实施的 B10 继续保留并补完整验收：

1. `fsNextWorkorderSerial` 在 `await` 后重新读取本地计数器；
2. 取号采用 max 语义并立即持久化；
3. `btnGenWip` 使用 `disabled + if disabled return + try/finally`；
4. `CORE.createOrder` 按工单号做最后查重。

阶段 0 验收门：

- 用户连续快速点击 6 次只能建出 1 张工单；
- 连续建两张不同工单号可以成功；
- 同号第二次明确提示「工单号已存在」；
- 取号失败时按钮恢复可用；
- 已有 6 条同号数据收场后，本地与飞书不再显示「本地多 5」；
- 正常工单执行、刷新、重新打开均不受影响。

---

### 阶段 1：同步问题可见且可收场（P0，先于页面搬迁）

**目标：任何「本地多 / 飞书多 / 冲突」都不能只报数字，必须能处理。**

#### 1.1 对账结果从计数升级为记录级

服务端已经返回 `localOnly` / `remoteOnly`，前端必须实际使用：

```text
工单记录：本地 9 / 飞书 4
本地多出的记录：
  - LL20260917004（第 1 行）
  - LL20260917004（第 2 行）
  ...
飞书多出的记录：无
```

同时新增 `localDuplicateGroups()`，不要用 `Set` 把重复业务键折叠掉。

#### 1.2 每类异常提供明确动作

| 异常 | 动作 |
|---|---|
| 本地新建、飞书没有 | 「推送到飞书」 |
| 飞书删除、本地仍有 | 「确认删除本地」或「保留本地并推回飞书」 |
| 本地同键重复 | 「查看重复行」「保留一行并删除其余」 |
| 两边字段冲突 | 「查看差异」「保留本地」「采用飞书」「预览」 |
| 流水异常 | 进入库存流水页/账本修数，不显示一个点不了的推送按钮 |

所有动作完成后必须：

1. 清除对应待处理状态；
2. 更新 `__syncedKeys` / `pendingDelete` / outbox；
3. 调 `runReconcile()`；
4. 重新渲染当前面板；
5. 写清晰的操作日志；
6. 失败时保留原异常，不得假装成功。

#### 1.3 对账状态必须持久化

`_censusPending` 和 `_lastReport` 不能在每轮自动对账时无条件清空。应按表合并保存，并显示：

- 本次是否完整；
- 扫描到多少/预计多少；
- 上次核对时间；
- 当前待人工数量；
- 为什么不能自动判断。

#### 1.4 阶段 1 验收门

- 构造本地多 5 条时，面板能列出具体 5 条；
- 能选择「删除本地重复行」；
- 选择后数字立即刷新；
- 不能误删飞书唯一正确行；
- 对账不完整时明确显示 `X/Y`，不出现无解释的按钮闪烁；
- 流水不再显示「全部推送」但实际永远推不动的按钮；
- 关键异常刷新页面后仍可继续处理。

---

### 阶段 2：建立「同步与健康」全站状态区（M1）

**目标：同步/队列/冲突/健康/日志是全站能力，不属于物料台账。**

新增默认折叠的：

```html
<details id="statusDock">
```

放置：

- `cloudGapBox`；
- `queueBox`；
- `conflictBox`；
- `healthBox`；
- `scanLogWrap`。

必须保留的 id：

- `queueBox`（已有测试断言）；
- `cloudGapBox`；
- `conflictBox`；
- `healthBox`；
- `scanLog`；
- `scanLogLast`。

行为要求：

- 顶栏队列角标在任意页签点击都能打开 `statusDock`；
- 有异常时自动展开；
- 无异常默认折叠；
- 摘要行显示在线状态、队列数、冲突数、待人工删除数；
- 操作日志在当前页可展开，不要求用户跳到扫码页找错误原因。

这一阶段主要是**纯搬迁 + 少量接线**，不改库存逻辑。

验收门：任意页签点击队列角标、冲突摘要和待人工摘要都能打开正确面板；刷新后状态仍正确。

---

### 阶段 3：库存流水独立页（M2）

新增 `data-tab="txn"` / `#tab-txn`，职责只有：

- 浏览库存流水；
- 按物料码、流水号、类型、操作人、时间筛选；
- 查看单条流水详情；
- 回放校验；
- 账本修数；
- 审计日志导出。

必须修正：

- `renderTxns().slice(0, 50)` 改为分页/加载更多；
- 不允许把「只增」误解为「完全不能浏览或追踪」；
- 不允许在库存流水上显示无法执行的批量推送按钮；
- 保留 `txnTable`、`txnSummary`、旧 hash 深链；
- `#ledger?view=transactions` 自动跳转或兼容到 `txn` 页。

验收门：19 条以上流水可以全部浏览；第 51 条仍可找到；按物料筛选结果正确；回放、修数、导出都在同一页可找到；页面刷新后仍在该页。

---

### 阶段 4：资源档案页（M3）

新增 `data-tab="res"` / `#tab-res`，内部四段：

```text
库位 | 容器 | 物品 | 手册
```

吸收原 `gen` 的对应建档功能，避免同一对象有两个入口。

每段都必须有：

- 全量列表；
- 搜索/筛选；
- 新增；
- 编辑；
- 删除；
- 业务键详情；
- 当前同步状态；
- 相关标签打印跳转。

特别要求：

- 容器可以修改类型、规格、所在库位；
- 库位可以修改类型、说明、授权人员；
- 物品可以修改名称、规格、库位；
- 手册可以修改名称、版本、库位；
- 编辑后先写本地，再进入 outbox/同步链，刷新后仍存在；
- 删除必须显示影响范围与同步状态；
- `label` 页只保留打印选择，不再承担业务删除。

验收门：四类对象分别做「新增→刷新→修改→刷新→删除→确认飞书」；每张表至少 1 条完整 CRUD 往返；容器库位变更必须能在网页端完成。

---

### 阶段 5：工单完整生命周期与上下文（M4）

在 B9/B10 修复基础上继续补齐：

- 建单入口固定在库存工单页；
- 列表行明确提供「详情」，详情顶部明确提供「返回列表」；
- 删除入口在用户能看见的位置，并在说明中写明；
- 未执行工单可修改计划明细、数量、日期（如业务允许）；
- 已执行工单不可直接删除，必须先冲销；
- 部分执行工单冲销只回退已执行数量；
- 删除、取消、冲销后都要返回正确列表状态；
- 当前对象条不能指向已删除/已同步替换的幽灵工单；
- 工单与关联流水、飞书记录三方一致。

验收门：新建、刷新、编辑、部分执行、继续执行、部分冲销、取消、删除、重新打开详情各走一遍；任何异常不出现白屏。

---

### 阶段 6：标签打印与本地特殊服务收尾（M5）

- `label` 回归纯打印；
- 手册/图纸查看和二维码明确显示对象来源；
- 真实摄像头路径在有设备时复测，无设备路径保留降级提示；
- `btnSeed`/`btnWipe` 只在独立环境做破坏性实测；
- 闲鱼真实取数命令按配置接入，不把 mock 结果当真机结果；
- InvenTree 真机同步等拿到地址/Token 后再验；
- 标签打印真实出纸由现场打印机验收，网页端负责尺寸、内容、条码值和模式守卫。

---

## 4. 测试文档必须改造

现有 `逻辑功能测试文档.md` 的问题不是没有用例，而是：

- 有些用例是「口头说覆盖」，表格没有结果；
- 以功能按钮为单位，缺少用户任务闭环；
- 8 张飞书表与网页页面没有独立映射章节；
- 没有把「本地 9 / 飞书 4」这种真实用户故障列为验收案例；
- 没有强制要求刷新、换页、回读飞书后的复验。

必须新增以下章节：

### 4.1 《飞书 8 表—网页页面映射》

每张表固定写：

- 页面入口；
- 列表位置；
- 新增入口；
- 修改入口；
- 删除入口；
- 搜索入口；
- 同步状态入口；
- 当前缺口。

### 4.2 《用户任务闭环用例》

至少包括：

1. 新建物料 → 刷新 → 飞书回读；
2. 新建/修改/删除库位；
3. 新建/修改/删除容器并移动库位；
4. 新建/修改/删除物品；
5. 新建/修改/删除手册；
6. 新建工单 → 打印 → 扫码 → 部分执行 → 冲销；
7. 盘点 → 账本流水 → 飞书回读；
8. 对账发现本地多 → 查看具体记录 → 选择收场动作；
9. 备份 → 合并 → 拒绝推送 → 再次读取；
10. 离线写入 → 刷新 → 恢复网络 → 队列冲刷。

### 4.3 每个用例的通过条件

必须同时满足：

- UI 有入口；
- UI 有明确结果；
- 刷新后结果保留；
- 飞书对应表结果一致；
- 重复点击不会重复数据；
- 失败后用户有可理解的收场路径；
- 不破坏账本不变量。

---

## 5. 每阶段必须执行的验证门

每个阶段实施后必须按固定顺序执行：

1. `npm test` 全量；
2. 静态不变量/变异测试；
3. 浏览器操作真实用户流程；
4. 直接读取 `/api/feishu/state` 核对对应表；
5. `replayAudit` 核对账本；
6. 刷新页面再复测；
7. 检查 outbox、冲突、待人工、同步状态；
8. 测试数据清理并再次核对生产计数；
9. 更新 `逻辑功能测试文档.md`；
10. 只有全部通过才进入下一阶段。

禁止以下替代行为：

- 只跑 `npm test` 就宣布网页功能通过；
- 只看物料和人员就宣布 8 表同步通过；
- 只看计数不看具体记录；
- 只在标签打印页看到了记录就宣布资源表 CRUD 完整；
- 用本地 mock 冒充 InvenTree 真机；
- 测试脚本超时后不核对页面是否仍在后台继续执行；
- 测试数据没有确认清理就进入下一阶段。

---

## 6. 当前状态与推荐执行顺序

**已经完成的修复：**

- B9：工单删除/失效详情导致白屏；
- B10：连续点击生成同号工单；
- B1~B8：前序同步、账本、导入、冲突、哨兵清理等问题；
- B4：账本修数工具；
- `2.23.0`：飞书 `图片链接`、`设备` 字段接入；
- `2.24.0`：B9/B10 修复上线。

**下一步严格按以下顺序：**

```text
阶段 0：诊断并收敛用户当前 6 条同号工单
   ↓
阶段 1：同步异常记录级展示 + 人工收场（P0）
   ↓
阶段 2：同步与健康状态坞（M1）
   ↓
阶段 3：库存流水独立页（M2）
   ↓
阶段 4：资源档案页 + 四表 CRUD（M3）
   ↓
阶段 5：工单生命周期和上下文补齐（M4）
   ↓
阶段 6：标签、相机、InvenTree、出厂数据等特殊服务（M5）
   ↓
阶段 7：UI 视觉优化
```

**UI 视觉优化必须最后开始**。在阶段 0~5 完成前，只能做影响业务安全的布局止血，不能大规模改导航和组件，否则无法判断问题来自旧业务逻辑还是新 UI。

---

## 7. 需要用户确认的唯一事项

阶段 0 收敛当前重复工单时，若 6 条 `LL20260917004` 内容确实完全相同且均未执行，建议：

- 保留 1 条本地记录；
- 删除其余 5 条**本地重复记录**；
- 不删除飞书唯一的 `LL20260917004`；
- 收敛后再核对工单本地 4 / 飞书 4。

如果 6 条内容不完全相同，则不自动处理，先把每条差异列出来让你选择。

除此之外，方案不要求你先决定页面样式；页面结构、表归属和验收规则已经明确。
