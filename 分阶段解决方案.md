# 416MES 遗留问题 · 分阶段解决方案

> 依据：三路只读审查 + 五路只读研究（批量冲突 / 存储分层 / 前端健壮性 / 旁路模块 / 待定项）
> 原则：**先别再丢 → 再别写错 → 再更快**。每阶段完成后跑完整测试，通过才进下一阶段。
> 基线：HEAD `248c730`，`npm test` = 284/284
>
> **进度（2026-09-16）**：P1 ✅ `3b46980`（2.8.0，已上线实测）· P2 ✅ `2ee29b0`（2.9.0）
> · P3 ✅（2.10.3）　· P4 ✅（2.11.0）　· P5 ✅（2.12.0）　`npm test` = **363/363**。下一步进 P6。

---

## 总览

| 阶段 | 目标 | 关键交付 | 验证门 |
|---|---|---|---|
| **P1 止血** ✅ | 正在流血的问题 | 三方合并流水顺序+txnSeq / 闸门②改OR / Math.max改reduce / fetch超时 / 探测失败自愈 | 新增断言 + 全量回归 |
| **P2 持久化可信** ✅ | 本地存储不再静默失败 | 配额告警+scanLog封顶 / IDB-LS取较新者+pagehide / fsEnqueue全不抛 / 推送成功回填syncedKeys | 故障注入测试 |
| **P3 数据身份与账本** ✅ | 账本只有一个身份 | 本地seq回写服务端seq / deletionJournal+全量删除闸门 / 冲突池持久化 | 多设备账本不重复 |
| **P4 批量冲突主路径** ✅ | 补上方案唯一没做的主路径 | qty用delta+走账本 / 批量对账模式写入闸门 / dryRun预览 / 快照含基线 | 5000+20 场景断言 |
| **P5 可收场+导入一致** ✅ | 人能把事收干净 | 队列面板+重试/放弃入口 / 停止无限重试 / 来历不明不变量 / 导入原子化+业务键闸门 / 默认值不回退 | 模板往返用例 |
| **P6 旁路模块** | 第二套实现跟上 | nextcode type修正 / NEC编码空间 / feishu-server本地模式 / inventree分页 / xianyu校验 / 标签模块 | 各模块新测试 |
| **P7 性能与存储分层** | 几万条下可用 | 预读并行 / 自动编号 / LS收敛+checkpoint消费 | 计时对比 |

---

## P1 止血（正在流血）

| # | 问题 | 证据 | 后果 |
|---|---|---|---|
| 1 | **增量三方合并不排序流水、不推进 txnSeq** | `fsThreeWayApply` 只调 planMerge/applyMerge，缺 mergeRemote 的流水后处理 | 已实测复现：order `[5,4,6,7]`、txnSeq 停在 5 → **本地下一笔会拿到已被远端占用的 seq**，Phase 0 撞号 bug 复活；且"最近 50 条"显示不到新流水 |
| 2 | **闸门② AND 而非 OR** | `lib/incremental.js:123` `Math.max(abs, ceil(size*ratio))` | 实测：**4 行表丢 3（75%）判 ok 自动删**；所有 ≤1000 行的表 5% 分支恒不生效 |
| 3 | **`Math.max(...大数组)` 会 RangeError** | `index.html:879` 在 `load()` 里 | Node 实测 20 万条抛栈溢出；**抛错中断整个顶层脚本 → 整页不可用**。服务端 `lib/feishu-api.js:402` 同款 |
| 4 | **写路径 fetch 无超时** | `fsPost`/`fsPostIncremental`/`nextcode` 都没有 signal | 网络挂起 → promise 永不 settle → **永久堵住该表后续所有写入**；界面已改、本地未落盘 |
| 5 | **探测失败被完全吞掉** | `syncChanges` 把每表错误塞进 `probe.tables[k].error` 后仍返回 `ok:true`；客户端**从不读 `d.probe`** | **8 张表全探测失败 = 表现为"零变化"**，静默、连日志都没有；方案 §五"探测连续失败→全量拉"未实现 |

---

## P2 持久化可信

| # | 问题 | 证据 |
|---|---|---|
| 1 | `save()` 空吞 localStorage 异常 | `catch (e) { }`；配额满后每次 save 静默失败，用户零提示 |
| 2 | `scanLog` 无上限 + 每条日志触发一次全量 state 写 | `log()` → `unshift + save()`；数组永不裁剪（只显示 200 条） |
| 3 | `initLocalStore` 无条件用 IDB 覆盖较新的 LS | 无新旧比较；IDB 落盘是 `setTimeout(...,0)` 且失败只 log；**无 pagehide 冲刷** |
| 4 | IDB 命中路径漏跑迁移 | `hydrateState` 少了 load() 的 minQty/cat/img 兜底与 C_LAYOUT/MODULE_ZONES 补种 |
| 5 | `fsPush` 的 `await fsEnqueue` 会 reject，25 个调用点 0 个 catch | 入队失败 = 界面已改、本地没落盘、下次被飞书覆盖 |
| 6 | 手动改库存顺序反了 | `await fsPushRecord` 排在 `applyManualAdjust` 之前 → 挂起时调整永不落地 |
| 7 | **推送成功后不更新 `__syncedKeys`** | `fsPush` 只 `removeById + save()` → 每笔刚推成功的记录在下次拉取前都是**假"来历不明"** |

**P2 完成情况**（`2ee29b0`，新增 10 条 `test/persistence-invariants.test.js` 断言）：

- 迁移归一化抽出 `migrateState()`，`load()` 与 IDB 恢复路径**共用**（旧实现 IDB 路径
  只调残缺的 `hydrateState()`，静默漏内置库位与字段默认值）
- `save()` 递增 `__rev`，并把「IDB 落后到哪个修订号」与 LS 一起落盘（`LS_DIRTY_KEY`）；
  启动按它判新。以 LS 为准时**继承 IDB 的 `__base`**，否则三方合并退回无基线会满屏假冲突
- `pagehide` / `visibilitychange` 同步落盘
- 配额错误不再吞：`QuotaExceededError` → 常驻告警条 + 处置指引；`scanLog` 封顶 500
- `log()` 去抖（旧实现每行一次全量 save）
- `fsEnqueue` 三级降级（IDB → LS 队列 → 失败日记），**绝不抛**；`fsPush` 不把调度异常抛给调用方
- 离线队列迁移后删 LS 源（否则队列一空、下次启动守卫失效 → 重复推送）；
  冲刷前把 LS 残留搬进 IDB（否则永不被重试）
- 推送成功回填 / 删除成功摘除 `__syncedKeys`
- 手工改库存改为**先本地算账落盘、再 await 网络**

**P2 遗留**（转入 P5）：`needs_attention` 仍无法出队（`fsGiveUpItem` 没有入口、
chip 不可点击、自动冲刷不过滤 status）。P2 只保证了"不会静默丢"，"人能把事收干净"留给 P5。

---

## P3 数据身份与账本

| # | 问题 | 证据 |
|---|---|---|
| 1 | **本地 seq 与服务端 seq 不回写 → 账本重复** | `recordTransaction` 本地自增；服务端另分配；`fsExecItem` 丢弃 `d.seq`。多设备必然错位，同一笔操作在本地变两条；且永久占 `pendingSummary`，累积超 50 后 `autoPushPending` 整体罢工 |
| 2 | **全量路径删除无闸门无账** | `pullState` 不校验 total（`.filter(r=>r.code)` 会静默丢行）；`mergeRemote` 有删除权 → 一次半截返回就能大规模删本地且不留凭据 |
| 3 | `deletionJournal` / `conflicts` store 建了从未读写 | 冲突池全在内存，刷新即丢；`__censusMissing` 是判定状态、会被覆盖，**不能当删除凭据** |

---

**P3 完成情况**（2.10.0 新增 12 条：mes-core 6 + feishu-api 3 + ledger-identity 6，其中 3 条为改写旧断言）：

- **P3-1 流水号身份回写**：`CORE.reconcileTxnSeq(state, {localSeq, serverSeq, opId})`；
  `fsPushStock` 带上本地乐观 seq，`fsExecItem` 拿服务端返回的 `d.seq` 回写并把新号记进
  `__syncedKeys.transactions`。`executeOrder`/`reverseOrder` 的 `applied` 补上 `txn` 透出。
  **第一个新用例就是原 bug 的复现**：不回写时一次操作在本地变成两条流水。
- **P3-2 全量拉取完整性 + 删除闸门**：
  - `listRecordsEx` 如实报告 `complete`（收到数 vs `total`）；`pullState` 把它随数据交出去。
    **不完整时不抛错** —— 分页期间有人新建一行就会让计数不符，抛错会把「少判几次删除」
    降级成「完全同步不了」；保护放在下游。
  - `mergeRemote` 新增 `opts.allowDelete`（**默认 false**）与 `opts.complete`；
    只有「显式允许 **且** 该表被证明拉全（`complete === true`）」才真的删。
    没删的记进 `stat.pendingDelete`，且**键保留在基线里** —— 否则下一轮会被
    `autoPushPending` 当成「本地新建」重新推回飞书，等于把飞书刚删的又建回来。
  - `fsMerge` 显式 `allowDelete: false`，把 `pendingDelete` 并进现有的
    「待人工删除 → 确认删除」入口，与对账路径共用同一套删除代码与闸门。
- **P3-3 冲突池与删除凭据持久化**：
  - 冲突池落 IDB 的 `conflicts` store（**明确不借用 `baselines`**：`persistStateToIdb`
    每次流水条数变化都会 `replaceAll` 掉它），启动两条恢复分支都捞回来。
  - `deletionJournal` 终于被真正读写：人工确认删除 / 自动对账判删 / 清空本机
    三条路径都写凭据（含时间、设备、操作人、原因、具体键），上限 500 批；
    「对齐」面板显示摘要并提供**查看/导出**入口 —— 不做读取入口就等于没存。

**P3 追加（验收时在线上发现的真实账本缺陷）**：`replayAudit` 报出 3 条链式不一致，
其中 **id#18 是当天新产生的**（手工调整 delta=+1、余量写成 13、真实余额 14）。
根因：建流水的「余量」写的是 `before + actualDelta`，而 `before` 是对物料行的
**非原子读** —— 上一次写入的 qty 没落/被覆盖时它就是旧值。qty 那条路早就改成按账本
收敛（`opening + Σδ`），但**流水的余量列没跟上**，于是两者长期可以不一致。
已修：建行时余量改为账本推导 `opening + ΣBeforeΣ + Δ`；收敛后若仍与 `want` 不同
再补正一次（非并发路径零额外请求）。用一个「物料行 qty 故意过期」的用例复现并钉死。

**P3 线上验收（2.10.3，浏览器实测）**：

| 验的是什么 | 结果 |
|---|---|
| 8 张表的完整性上报 | `4/4 · 26/26 · 18/18 · 2/2 · 188/188 · 2/2 · 2/2 · 122/122` 全 ✅（也证实真飞书 list 接口确实返回 `total`） |
| 删除闸门端到端：喂一份「少一条」的远端快照 | **删除 0 条**，本地 4 条物料原样保留，缺的那条转成「待人工核删」，且基线键**保住**（不会被当成本地新建推回飞书），无幽灵待推送 |
| 删除凭据 | 写入 → 从 IDB 读回 → 面板显示摘要 → 查看/导出按钮就位 → 清理，全通 |
| 冲突池刷新存活 | 刷新后从 IDB 恢复 1 条、**面板自动弹出**、两个裁决按钮在；清空后 store 记录被删、面板收起 |
| 流水号回写（真实写入） | 人为把本地计数器压低制造错位 → 服务端仲裁后日志打出 `🔢 流水号已对齐服务端：#19 → #21`，**无重复 seq** |
| 余量按账本推导 | 两条新流水 `#20 余量16 / #21 余量15` 全部链式自洽，`replayAudit` **没有新增不一致** |
| 两边账本一致性 | 本地 20 条 / 飞书 20 条，**序号集合完全相同** |

> 验收在线上账本留下了 2 条本人为验证写入的记录（`#20` +1、`#21` -1，原因分别标注
> 「P3验收·序号回写」「P3验收·冲正」，净变化为 0）。它们链式自洽，
> 已在《待定.md》说明，可按需清理。

**P3 遗留**：`pullState` 的 `total` 依赖飞书「列出记录」接口返回该字段（已按真机响应实现，
mock 也对齐）。若某天该字段缺失，`complete` 会如实报 `null`，此时全量路径一律不判删 ——
安全侧失效，不会误删。

---

## P4 批量冲突主路径（方案唯一整块没做的）

| # | 问题 | 关键点 |
|---|---|---|
| 1 | 冲突面板 qty 用绝对值覆盖 | 「保留本地」正确语义 = `remote + (local − base)`，提交 **delta 不提交 qty**；必须同时堵 `fsPushRecord('materials')` 带绝对 qty 的旁路；修 `outbox.project` 对 delta-only 项投影出 `undefined` |
| 2 | 无「批量对账模式」 | 超阈值（>200 或 >10%）暂停 outbox flush。**必须同时给 `fsPush` 加门禁** —— 扫码主路径是直写，只改 flush 等于没停 |
| 3 | 无 dryRun 预览 | 逐组 + 样例≤20 + 整体摘要零网络；qty 走 stock dryRun，其余走 upsert dryRun |
| 4 | 快照只 1 份且在内存 | 用空闲的 `conflicts` store（**不能用 `baselines`**：它每次流水条数变化就 `replaceAll`）；回滚**必须同时回退 `__base`** |

**P4 完成情况**（新增 20 条：three-way-merge 9、p4-conflict-paths 9、feishu-api 1、handlers 1 改写，共改写 3 条旧断言）：

- **P4-1 qty 冲突用增量、走账本**：`applyMerge` 对 `materials.qty` 的裁决不再写绝对值，
  而是 `远端 + (本地 − 基线)`，并产出 `stockWrites` 交给 `fsPushStock`（记流水、按账本收敛）。
  缺基线（降级模式）换算不出增量 → 不猜，记进 `needsStocktake` 并要求用「盘点」。
- **P4-1 堵死绝对 qty 旁路**：审计确认原实现有 **6 处**物料 upsert 会带绝对 qty，
  其中 5 处**没有**配套的库存直写 —— 等于绕过账本覆盖库存、零流水凭据。
  现在三道闸门：① `fsPush` 入队前剥除；② `fsExecItem` 提交前再剥（补上队列历史条目
  绕过 fsPush 的旁路）；③ `upsertRecords` 服务端兜底，**只拦已存在记录的更新**
  （新建物料时期初库存必须能从建档带进来，一刀切会把期初抹成 0）。
  剥掉的字段如实进 `dropped`，但**不进 `blocked`** —— 进了保护名单会让合并永远不采纳
  飞书的库存值，本地库存反而长期偏离。
- **P4-1 `applyMerge` 的 qty 闸门**：`plan.writes` 里没有来源标记的「更新型」绝对 qty 一律剥掉；
  `planMerge` 的「本地没动 → 采纳远端」打上 `from:'remote-ff'` 放行 ——
  否则增量同步再也同步不回飞书的库存数量（这一版我自己先写错、被审计指出后修正）。
- **P4-2 批量对账模式（写入闸门）**：冲突 >200 条、或 ≥20 条且占本地 >10% 时自动进入
  `state.__bulkHold`，所有写入只入队不提交，直到人处理完冲突点「恢复写入」。
  闸门放在 **`fsPush`**（直写主路径）而不是只拦队列冲刷 —— 扫码与手工调整走的是直写。
  状态存在 `state` 里（刷新不能绕过）。比例阈值带 `BULK_HOLD_MIN=20` 下限：
  26 行的表里 3 条冲突不该把人锁住。
- **P4-2 闸门期间不清「待推送」列表**：`pushPending` 在闸门期间返回 0，
  否则 `autoPushPending` 会清空列表、面板显示「已一致」而飞书一条都没收到（审计发现）。
- **P4-3 dryRun 预览**：每组一个「预览」按钮，对「采用飞书 / 保留本地」各跑一次：
  本地侧**在 state 的浅克隆上真跑一遍 `applyMerge`**（不是另写一套推演逻辑，
  否则会出现「预览与实际不一致」，比没有预览更坏）；服务端侧 qty 走 `stock dryRun`、
  其余走 `upsert dryRun`，样例封顶 20 条。全程零写入。
- **P4-4 快照含基线**：`applyMerge` 的 snapshot 增加 `hadBase`/`base`，`undoMerge` 一起回退。
  不回退基线的话，「本地其实没改」会在下一次合并里被误判成「本地改过」，
  回退出来的旧值会被当成新改动推回飞书、把对方的修改覆盖掉 —— 回滚反而制造损坏。
  `hadBase` 保证「原本没有 `__base` 键」时回滚后也不凭空多一个（deep-equal 断言抓到的）。

**P4 线上验收（2.11.0，浏览器实测，全部零生产写入）**：

| 验的是什么 | 结果 |
|---|---|
| P4-1 剥除真的生效 | `fsStripMaterialQty` 把 `{code,qty,name}` 变成 `{code,name}` ✅ |
| P4-2 闸门挡住提交但不丢数据 | 进入闸门后推一条物料：队列 0→1 条、**条目仍在队列里且 qty 已剥** |
| P4-2 闸门期间 `pushPending` | 返回 **0**（面板不会被误清成「已一致」）✅ |
| P4-3 预览零写入 | 预览前后：流水数、库存、队列**三项全未变** ✅；两个选择各算出正确结果（103 / 105），服务端 dryRun 回报「将记一条流水（变动 +2，余量 102，预分配流水号 #22）」 |
| P4-1 裁决走库存直写 | 冲突（基线 100 / 本地 102 / 飞书 103）裁决「保留本地」→ 本地 qty **105** = 103+(102−100)，队列里的条目是 **`delta=2`** 而不是把 102 直接写进去 ✅ |
| P4-4 回滚连基线一起退 | 裁决后 105 → 回滚后 **100**；期间人为用 `fsUpdateBase` 把基线推进到 9999，回滚后基线**回到裁决前**（不是 9999）✅ |

> 验收用的是「先进闸门再裁决」的办法 —— 闸门本身就是写拦截器，于是整轮验证
> **没有向飞书写入任何数据**。生产计数与 P3 结束时完全一致（流水仍 20 条）。

---

## P5 可收场 + 导入一致

| # | 问题 | 证据 |
|---|---|---|
| 1 | `needs_attention` 永远无法出队 | `fsGiveUpItem` 无调用者；chip 不可点击；且自动冲刷**不过滤 status** → 偷偷无限重试 |
| 2 | 「来历不明」不变量缺失 | 方案 §三要求报警；`project()` 写好但 UI 未接；`mergeRemote` 把「未决」与「来历不明」混成一个 pending |
| 3 | 模板/导出/导入三套表头 | 模板缺「模块区」「PIN码」→ **用官方模板填完导回会静默清空这两个字段**（pin 用于登录） |
| 4 | `col()`/`codeIdx()` 静默回退 | 物料码列改名 → 产出 `code:''` 脏数据，永久占「待推送」 |
| 5 | 导入非原子 | 逐 sheet 就地替换，中途异常留半成品并已 save() |
| 6 | 默认值覆盖 | `Object.assign(old,{minQty:0},m)` 备份无该字段时静默清零安全库存 |

**P5 完成情况**（新增 10 条：p5-import-queue 8、outbox 2，共改写 3 条旧断言）：

- **P5-1 人能收场**：`fsFlushQueueInner` 现在**跳过 `needs_attention`**（旧实现不过滤状态，
  每一轮还在偷偷重试一个已知不会成功的请求，还占着串行链拖慢其它条目）；
  `fsPush` 里再次触发同一条目时也不重试。顶部角标从「不可点击的 span」变成入口，
  点开是队列面板：逐条显示状态/失败次数/原因，带「重试」（复原 tries/status）与
  「放弃并导出」（`fsGiveUpItem` —— 这个函数以前**一个调用者都没有**）。
- **P5-2「来历不明」不变量**：新增 `fsAuditIdentity`，把每条本地记录归入
  已确认（在 `__syncedKeys`）/ 未决（离线队列里有对应操作）/ **来历不明 → 报警**。
  两个防误报前提：没有基线时**不判定**（否则全新设备上每条都「来历不明」）；
  队列读不到时不把「未决」误算成「来历不明」。结果显示在「对齐」面板，
  并提供「确认并全部推到飞书」入口。
  另外修掉 `outbox.project()` 的潜伏 bug：**delta-only 的库存条目会把 qty 投影成
  `undefined`**（旧实现无条件 `{qty: it.qty}`），现在没有 qty 就用「快照值 + 增量」。
  `project()` 在生产里没有消费者（本地 state 本身就是乐观投影，再叠一层会重复计数），
  这一点在文档里写清楚，不再假装"已接线"。
- **P5-4 业务键闸门**：`codeIdx` 找不到表头就 `return 0`、`col` 找不到列名返回 `''` ——
  于是「物料码」列被改名后每一行都变成 `code:''` 的**幽灵记录**：对同步完全隐形
  （索引时被跳过、不进待推送、不进对账），永久留在地里且毫无提示。
  现在**预检阶段**就校验全部 9 张表的业务键列（含物料表必须有的「库存数量」），
  缺了就直接拒绝导入、**一个字都不改**；同时兼容真正无表头的老模板。
  再加一道：空业务键的行一律丢弃并在导入告警里说明。
- **P5-5 导入原子化**：改动前对 12 个键拍快照，`catch` 里整体回滚 + 落盘 + 重画。
  旧实现逐 sheet 就地替换、`catch` 只弹窗 —— 第 5 张表抛错时前 4 张已换掉，
  屏幕上却还是旧值，而这**半成品状态**会被 pagehide 的 `flushStateOnHide()`
  或后续任意一次 `save()` 落盘，之后再没人知道它是半成品。
- **P5-6 默认值不再回退**：`mergePackage` 的 `Object.assign(old, {minQty:0}, m)` 会在
  备份记录**没有 `minQty` 键**时把本机安全库存静默清零（同一文件里 PIN 码的注释
  明确依赖「键不存在就保持原机原值」，这条违反了它）。改成先合并、再只在确实没值时补 0。

**P5 遗留**：`needs_attention` 条目的「本机账目要不要跟着回退」没有自动化 ——
放弃同步只保证不再提交，本机 qty 已经乐观改过了。这属于「人工决定」范畴，
面板文案里已明确说明。

---

## P6 旁路模块

| # | 问题 | 严重度 |
|---|---|---|
| 1 | **nextcode `type='LL'` 与飞书选项 `'LL 领料'` 不匹配** | 🔴 跨设备防撞号**实际失效**（测试恰好用了全名所以没暴露） |
| 2 | NEC 与 WIP 共用编码空间、两套计数器、双双含 LL | 🔴 扫 NEC 领料标签会被当 WIP 执行，**真实动库存** |
| 3 | `feishu-server.mjs` 只 3 条路由，本地 `writeStock` 丢弃 opId | 🔴 本地模式增量从不工作、写入全进队列、幂等失效 |
| 4 | `inventree-sync.mjs` 分页只取首页 | 🔴 重复建 Part/库位 |
| 5 | `xianyu-sync.mjs` TSV 全局换逗号 + priceDivisor 无校验 | 🟡 含逗号错列；除 0 成本静默变 null |
| 6 | 标签模块：模块区混进库位页签、zone↔纸张不恢复、无打印守卫 | 🟡 打错标签/白页 |

---

## P8 本地模式（先止血 ✅ / 再重写）

只读研究已确认：本地模式（`feishu-server.mjs`）与云端**不共用实现**，`writeStock` 有 8 处
行为分歧（丢 opId、无流水号仲裁、先改库存后写流水、全表扫阻塞单线程…），
缺 upsert/delete/incremental/nextcode/reconcile/schema/changes 七条路由，
且 `decodeURIComponent('%')` 能直接打掉进程。

| # | 事项 | 状态 |
|---|---|---|
| 1 | 止血：`/stock` 停用回 501；未实现路由回 **501 JSON**（不是静态 404 HTML） | ✅ `2ee29b0` |
| 2 | 止血：修 `decodeURIComponent` 崩溃、路径守卫改比对 `DIR + 分隔符`、流水判存条件与 lib 对齐 | ✅ `2ee29b0` |
| 3 | 止血：前端统一后端域名（`api.newenergycoder.club` 实测 Cloudflare 530，隧道已死） | ✅ `2ee29b0` |
| 4 | 重写：把 `feishu-server.mjs` 改成 `lib/feishu-api.js` 的薄 HTTP 适配层，一份实现两套宿主 | ⬜ 待做 |
| 5 | 重写后：handler 契约测试同时跑在两套适配器上；`scripts/feishu-crud-check.mjs --site` 端到端 | ⬜ 待做 |

> 前置已解决：`库存流水.操作ID` 列**已存在**（实测），幂等键可用。
> 仍待确认：局域网机器是否允许持有 `FEISHU_APP_ID/SECRET`（决定第 4 项能否走"离线也可信"）。

---

## P7 性能与存储分层

| # | 事项 | 预期 |
|---|---|---|
| 1 | 预读 4 次并行（O1） | 11-12s → ~9.5s。**安全性依据**：`opening` 已改为账本决定（与读取时刻无关），并行不再触发并发用例失败 |
| 2 | 去二级确认读（O2） | 再省 ~0.6-0.9s；若 8 并发用例抖动就回退 |
| 3 | 飞书自动编号（O3） | 再省 ~2s 且**消除** seq 撞号。前置：P3-1；需先做飞书只读探测 |
| 4 | LS 收敛为 boot snapshot + checkpoint 消费 | 真正拆掉 5MB 墙；账本重放从 checkpoint 起算 |

---

## 用户决定（2026-09-16 已答复）与仍待决定

**已答复并落地：**

1. **PIN 码不明文导出** ✅ —— Excel 只写「已设置/未设置」；导入侧 4 位数字=设置、
   「清空」=清除、其它/缺列=保持原值；JSON 备份默认删 `pin` 键（不是写空串）
2. **飞书新列** ✅ —— 实测**代码要用的列已经全齐（0 缺列）**，答复为「只加 `物料台账.分类`」
   （换设备后所有物料退化成「QT 其他」的根因）。`设备` / `图片链接` 本次不加
3. **人员「职务」** ✅ —— 网页端改成锁定 负责人/成员/本科生 的下拉；导入越界值收敛并告警
4. **本地模式** ✅ —— 先止血（已完成）→ 再重写成 `lib/feishu-api.js` 薄适配层（P8）
5. **验证方式** ✅ —— 不在局域网机器上验证：部署到 GitHub → 在线上网站验证

**仍待决定：**

1. **飞书自动编号（P7-O3）** —— 直接改现有「流水号」列类型会导致重编号与历史 seq 撞号，
   需要**新增一列**。等 P3-1（本地 seq 回写）做完再谈
2. **`FEISHU_APP_ID/SECRET` 归属** —— 该应用凭证只在 Vercel 环境变量里，仓库内任何文件都没有
   （`feishu-backend.config.json` 只有 `base_token` 与 8 个表 ID，且被 gitignore）。
   P8-4 重写本地模式需要它，见对话中的说明
