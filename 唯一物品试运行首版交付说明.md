# 唯一物品试运行首版交付说明

## 目标
本版按用户要求优先“能跑通、现场再验证”，不把身份认证或共享数据库作为本轮前提。保留严格模式以便后续升级。

## 已实现
- 默认`feishu-trial`：无需登录即可提交受控ITM命令；`ITM_OPERATION_MODE=disabled`停写，`strict`恢复严格门禁。
- 飞书操作表用于opId/载荷查重、PREPARED/APPLIED/REJECTED/REPAIR_REQUIRED、before/after和错误记录。
- 写前重新读取状态和计划；同一时间有未决操作则停新写；写/回读不确定进入REPAIR_REQUIRED且不自动重放。
- 旧LOC/CTN空状态保留unknown，页面提供核实启用；旧物品verifyLegacy，新物品pending入库。
- 表结构不齐时返回明确503，不写实体。
- 第九表ID已核实为tblWyVuqDDBnU05t并为默认Base接入。
- 新查询/作业UI：中文结果卡、独立查询反馈、动态步骤、作业清单、待处理卡、折叠管理与复核、390px响应式布局。

## 试运行限制
飞书没有唯一约束或CAS，本模式不提供严格多设备事务。请一名操作员使用，同一时间只提交一条，等到APPLIED/REJECTED再继续；未知结果只查询原opId；不要直接编辑状态、容器码、版本、最后操作ID或操作表日志。页面常驻显示该提示。

## 飞书只读核查结果
Base NpWBb0RXYayqfosYqnScWdlDnMb；操作表tblWyVuqDDBnU05t。物品和库位字段基本齐全；容器及操作表仍需用户补字段，详见《试运行首版飞书核查与开发记录.md》。已有行状态留空即可，不批量填active。

## 自动化验证
- `npm test`：594/594通过，0失败；日志test-logs/trial-ui-final.log。
- `npm run test:browser`：Chromium1/1通过，真实IDB、390px、刷新恢复，无pageerror。
- feishu-trial隔离HTTP：4/4通过；包括首次注册/启用/入出库、冷启动重试、故障REPAIR_REQUIRED、缺schema零写。
- 目标UI/严格模式回归定向测试30/30通过。

## 用户现场验证顺序
1. 按飞书缺项清单补列，不填现有状态。
2. 页面先核实启用一个库位，再核实该库位下一个容器。
3. 建档一个测试物品或核实旧物品。
4. 入库：LOC→CTN→ITM，确认并主动提交；等待APPLIED。
5. 查询物品/容器/库位关系。
6. 出库同一ITM并等待APPLIED；确认当前位置清空、历史仍在。
7. 最后测试相机/扫码枪；若失败先用手输继续记录问题。
